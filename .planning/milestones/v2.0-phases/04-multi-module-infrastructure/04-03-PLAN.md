---
phase: 04-multi-module-infrastructure
plan: 03
type: execute
wave: 2
depends_on: ["04-02"]
files_modified:
  - learn/lib/prompt-templates.cjs
  - learn/content/prompts/markdown-anatomy.prompt.md
  - learn/tests/prompt-templates.test.cjs
autonomous: true
requirements: [PIPE-02, PIPE-03]

must_haves:
  truths:
    - "assembleMarkdownPrompt replaces all {{KEY}} placeholders with context values"
    - "assembleMarkdownPrompt throws on missing keys (fail-loud)"
    - "assembleMarkdownPrompt supports dotted keys like {{frontmatter.description}}"
    - "markdown-anatomy.prompt.md template exists and works with parseMarkdownFile output"
    - "Existing assemblePrompt function is completely unchanged"
  artifacts:
    - path: "learn/lib/prompt-templates.cjs"
      provides: "assembleMarkdownPrompt alongside existing assemblePrompt"
      exports: ["assemblePrompt", "assembleMarkdownPrompt"]
    - path: "learn/content/prompts/markdown-anatomy.prompt.md"
      provides: "Template for teaching markdown source files"
      contains: "{{fileName}}"
    - path: "learn/tests/prompt-templates.test.cjs"
      provides: "Tests for both template functions"
  key_links:
    - from: "learn/lib/prompt-templates.cjs"
      to: "learn/content/prompts/"
      via: "fs.readFileSync templateName.prompt.md"
      pattern: "readFileSync.*prompt\\.md"
    - from: "learn/content/prompts/markdown-anatomy.prompt.md"
      to: "learn/lib/markdown-parser.cjs"
      via: "Template keys match parseMarkdownFile output fields"
      pattern: "\\{\\{fileName\\}\\}"
---

<objective>
Add generic {{KEY}} template replacement system (assembleMarkdownPrompt) and create the markdown-anatomy prompt template for teaching .md source files.

Purpose: Phase 5 lesson generation needs a template system that works with parsed markdown data. The new function sits alongside assemblePrompt (which stays untouched for CJS sources).
Output: Updated prompt-templates.cjs with new export, new markdown-anatomy.prompt.md template, updated tests.
</objective>

<execution_context>
@C:/Users/18182/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/18182/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-multi-module-infrastructure/04-CONTEXT.md
@.planning/phases/04-multi-module-infrastructure/04-RESEARCH.md
@.planning/phases/04-multi-module-infrastructure/04-02-SUMMARY.md

<interfaces>
<!-- Existing prompt-templates.cjs API (DO NOT MODIFY assemblePrompt) -->

From learn/lib/prompt-templates.cjs:
```javascript
function assemblePrompt(templateName, context) {
  // Hardcoded replacements: FILE_NAME, MODULE_DOC, EXPORTS, FUNCTIONS, etc.
  // Uses defaults (empty string, 'None') for missing keys
  // Returns assembled prompt string
}
module.exports = { assemblePrompt };
// After this plan: module.exports = { assemblePrompt, assembleMarkdownPrompt };
```

<!-- parseMarkdownFile output shape (from Plan 02) -->
From learn/lib/markdown-parser.cjs (created in Plan 02):
```javascript
parseMarkdownFile(filePath) returns:
{
  filePath: string,
  fileName: string,
  fileType: 'command-spec' | 'workflow',
  frontmatter: { name: string, description: string, ... },
  sections: { objective: string, process: string, ... },
  fileReferences: string[],
  codeBlocks: [{ language: string|null, code: string }],
  body: string,
}
```

<!-- The markdown-anatomy template will use these keys from a flat context object -->
<!-- Format functions (not in this plan) will flatten parseMarkdownFile output into context -->
<!-- e.g., context = { fileName: '...', fileType: '...', frontmatter.name: '...', sections_formatted: '...', ... } -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add assembleMarkdownPrompt with fail-loud {{KEY}} replacement</name>
  <files>learn/lib/prompt-templates.cjs, learn/tests/prompt-templates.test.cjs</files>
  <behavior>
    - assembleMarkdownPrompt('template-name', { key1: 'val1' }) reads template-name.prompt.md and replaces {{key1}} with 'val1'
    - assembleMarkdownPrompt with missing key throws Error with message containing the key name and template name
    - assembleMarkdownPrompt with dotted key: context = { 'frontmatter.name': 'gsd:echo' } replaces {{frontmatter.name}}
    - assembleMarkdownPrompt with nested object: context = { frontmatter: { name: 'gsd:echo' } } resolves {{frontmatter.name}} via dot-path traversal
    - assembleMarkdownPrompt replaces ALL occurrences of same key (global replace)
    - assemblePrompt still works exactly as before (no changes to it)
  </behavior>
  <action>
    Add `assembleMarkdownPrompt(templateName, context)` to prompt-templates.cjs:
    - Read template from `learn/content/prompts/{templateName}.prompt.md`
    - Use regex `/\{\{(\w+(?:\.\w+)*)\}\}/g` to find all placeholders
    - For each match, resolve the key against context:
      1. First check if exact key exists in context (e.g., `context['frontmatter.name']`)
      2. If not, try dot-path traversal (e.g., `context.frontmatter.name`)
      3. If neither resolves, throw `Error('Missing template key: {key} in template: {templateName}')`
    - Convert resolved value to string via `String(value)`
    - Add helper `resolveDottedKey(obj, key)` that traverses dot-separated path
    - Export assembleMarkdownPrompt alongside existing assemblePrompt

    **DO NOT modify assemblePrompt or its helper functions (formatExports, formatFunctions, formatRequires)**. The two systems are parallel per user decision.

    Add new test cases to existing prompt-templates.test.cjs:
    - Test basic replacement
    - Test missing key throws
    - Test dotted key resolution (both flat and nested)
    - Test multiple occurrences of same key
    - Test existing assemblePrompt tests still pass (regression check)
  </action>
  <verify>
    <automated>node --test learn/tests/prompt-templates.test.cjs</automated>
  </verify>
  <done>
    - assembleMarkdownPrompt exported and works with {{KEY}} replacement
    - Throws on missing keys with descriptive error message
    - Supports dotted keys for nested context access
    - assemblePrompt completely unchanged
    - All prompt-templates tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Create markdown-anatomy prompt template</name>
  <files>learn/content/prompts/markdown-anatomy.prompt.md</files>
  <action>
    Create `learn/content/prompts/markdown-anatomy.prompt.md` -- a prompt template for generating lessons that teach GSD .md source files (command specs and workflows).

    The template should use {{KEY}} placeholders that align with what a format function would produce from parseMarkdownFile output. Key placeholders to include:

    - `{{fileName}}` -- the source file name being taught
    - `{{fileType}}` -- 'command-spec' or 'workflow'
    - `{{frontmatterFormatted}}` -- pre-formatted frontmatter section (format function renders this before template fill)
    - `{{sectionsFormatted}}` -- pre-formatted XML sections listing
    - `{{fileReferencesFormatted}}` -- pre-formatted file references listing
    - `{{codeBlocksFormatted}}` -- pre-formatted code blocks listing
    - `{{sourceCode}}` -- full raw source content
    - `{{lessonNumber}}` -- lesson number in module
    - `{{lessonTitle}}` -- title for this lesson
    - `{{focus}}` -- what aspect of the file to focus on

    Template structure (similar to existing source-dive.prompt.md but for markdown):
    1. Role: You are creating a lesson for the GSD Learn interactive CLI tool
    2. Source context: The file being taught, its type, key structural elements
    3. Teaching instructions: How to explain the file's structure, sections, and purpose
    4. Output format: JSON lesson structure matching the existing lesson schema (id, title, lessonNumber, objective, content blocks, conceptMap, successCriteria)
    5. Quality criteria: Accuracy, clarity, progressive depth

    Keep the template focused on the two file types: command specs (with frontmatter) and workflows (without frontmatter). The template should guide Claude to highlight the structural patterns that make these files work (YAML frontmatter fields, XML section purposes, @file references as wiring, code blocks as implementation).
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const t = fs.readFileSync('learn/content/prompts/markdown-anatomy.prompt.md', 'utf-8'); const keys = t.match(/\{\{\w+(?:\.\w+)*\}\}/g); console.log('Keys found:', keys.length); if (keys.length < 5) { process.exit(1); } console.log('Template valid');"</automated>
  </verify>
  <done>
    - markdown-anatomy.prompt.md exists in learn/content/prompts/
    - Contains {{KEY}} placeholders matching parseMarkdownFile output fields
    - Structured to generate lessons teaching GSD .md file anatomy
    - Template is specific to command spec and workflow file types
  </done>
</task>

</tasks>

<verification>
- `node --test learn/tests/prompt-templates.test.cjs` -- all template tests pass
- markdown-anatomy.prompt.md exists and contains expected {{KEY}} placeholders
- `node --test learn/tests/*.test.cjs` -- full suite green
</verification>

<success_criteria>
- assembleMarkdownPrompt works with generic {{KEY}} replacement and throws on missing keys
- markdown-anatomy.prompt.md template exists with correct placeholders
- assemblePrompt remains completely unchanged (parallel systems per user decision)
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/04-multi-module-infrastructure/04-03-SUMMARY.md`
</output>
