---
phase: 04-multi-module-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - learn/lib/progress.cjs
  - learn/lib/concept-map.cjs
  - learn/lib/verifier.cjs
  - learn/lib/lessons.cjs
  - learn/bin/gsd-learn.cjs
  - learn/content/modules/command-lifecycle/module.json
  - learn/content/modules/command-lifecycle/concept-map.txt
  - learn/content/modules/gsd-commands/module.json
  - learn/content/modules/gsd-commands/concept-map.txt
  - learn/tests/progress.test.cjs
  - learn/tests/concept-map.test.cjs
  - learn/tests/verifier.test.cjs
  - learn/tests/lessons.test.cjs
autonomous: true
requirements: [INFRA-01, INFRA-02, INFRA-03, INFRA-04, MOD2-01]

must_haves:
  truths:
    - "Existing v1 progress.json auto-migrates to v2 per-module schema with no data loss"
    - "Concept map renders from module-owned files, not hardcoded constant"
    - "Verifier resolves ~/path to home directory for artifact checks"
    - "gsd-learn starts in Module 1 Lesson 1 by default"
    - "Command Lifecycle appears as Module 2 via order field, ID unchanged"
  artifacts:
    - path: "learn/lib/progress.cjs"
      provides: "v1-to-v2 migration in loadProgress"
      contains: "migrateV1toV2"
    - path: "learn/lib/concept-map.cjs"
      provides: "Module-owned concept map loading"
      contains: "renderConceptMap"
    - path: "learn/lib/verifier.cjs"
      provides: "Tilde path resolution"
      contains: "os.homedir"
    - path: "learn/lib/lessons.cjs"
      provides: "Order-based module sorting"
      contains: "order"
    - path: "learn/content/modules/command-lifecycle/module.json"
      provides: "Module 2 ordering"
      contains: "order"
    - path: "learn/content/modules/gsd-commands/module.json"
      provides: "Module 1 placeholder"
      contains: "order"
    - path: "learn/content/modules/command-lifecycle/concept-map.txt"
      provides: "Command Lifecycle concept map ASCII art"
  key_links:
    - from: "learn/lib/progress.cjs"
      to: "progress.json"
      via: "migrateV1toV2 inside loadProgress"
      pattern: "migrateV1toV2"
    - from: "learn/lib/concept-map.cjs"
      to: "module directory"
      via: "fs.readFileSync concept-map.txt"
      pattern: "concept-map\\.txt"
    - from: "learn/lib/verifier.cjs"
      to: "os.homedir()"
      via: "resolvePath helper"
      pattern: "os\\.homedir"
    - from: "learn/bin/gsd-learn.cjs"
      to: "learn/lib/lessons.cjs"
      via: "loadModule with default module"
      pattern: "gsd-commands"
---

<objective>
Upgrade gsd-learn infrastructure to support multiple modules: progress migration (v1 to v2), module-owned concept maps, tilde path resolution in verifier, module renumbering via order field, and default startup pointing to Module 1.

Purpose: Phase 5 (Module 1 Lessons) needs this plumbing in place -- per-module progress, filesystem-driven concept maps, and correct module ordering.
Output: Modified learn/lib/*.cjs files, new Module 1 placeholder directory, updated Module 2 module.json.
</objective>

<execution_context>
@C:/Users/18182/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/18182/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-multi-module-infrastructure/04-CONTEXT.md
@.planning/phases/04-multi-module-infrastructure/04-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From learn/lib/progress.cjs:
```javascript
const DEFAULT_PROGRESS = {
  version: 1,
  currentModule: null,
  currentLesson: 0,
  modules: {},
};
function loadProgress(cwd) { /* reads progress.json, merges with defaults */ }
function saveProgress(cwd, progress) { /* writes progress.json */ }
module.exports = { loadProgress, saveProgress };
```

From learn/lib/concept-map.cjs:
```javascript
const CONCEPT_MAP = `...`; // Hardcoded ASCII art
function renderConceptMap(currentSection) { /* renders map with YOU ARE HERE marker */ }
module.exports = { renderConceptMap, CONCEPT_MAP };
```

From learn/lib/verifier.cjs:
```javascript
function verifyArtifact(filePath, checks) { /* checks file exists + structural patterns */ }
function runVerification(cwd, specPath) {
  // Line 44: const filePath = path.join(cwd, artifact.path);  <-- needs tilde expansion
}
module.exports = { verifyArtifact, runVerification };
```

From learn/lib/lessons.cjs:
```javascript
function loadModule(moduleId, contentDir) {
  // Returns { id, title, description, lessons }
  // Currently no order field support
}
module.exports = { loadModule };
```

From learn/bin/gsd-learn.cjs (line 43):
```javascript
const moduleId = flags.module || 'command-lifecycle';  // <-- needs to become 'gsd-commands'
```

From learn/content/modules/command-lifecycle/module.json:
```json
{ "id": "command-lifecycle", "title": "Command Lifecycle", "description": "..." }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Progress migration, verifier tilde expansion, and module ordering</name>
  <files>
    learn/lib/progress.cjs,
    learn/lib/verifier.cjs,
    learn/lib/lessons.cjs,
    learn/tests/progress.test.cjs,
    learn/tests/verifier.test.cjs,
    learn/tests/lessons.test.cjs
  </files>
  <behavior>
    - loadProgress with v1 data ({ version: 1, currentModule: "command-lifecycle", currentLesson: 5, modules: {} }) returns v2 shape ({ version: 2, currentModule: "command-lifecycle", currentLesson: 5, modules: { "command-lifecycle": { currentLesson: 5, started: true } } })
    - loadProgress with v2 data returns it unchanged
    - loadProgress with no file returns v2 defaults (version: 2)
    - Migration writes back to disk (one-time migration)
    - runVerification resolves "~/some/path" to os.homedir() + "/some/path"
    - runVerification resolves "~\some\path" to os.homedir() + path-joined (Windows compat)
    - runVerification still resolves relative paths with cwd as before
    - loadModule returns order field from module.json when present
    - New listModules(contentDir) returns all modules sorted by order field
  </behavior>
  <action>
    **progress.cjs:**
    - Change DEFAULT_PROGRESS to version: 2
    - Add `migrateV1toV2(progress)` function: if version < 2, copy currentModule's lesson position into modules object, set version to 2
    - Modify `loadProgress()` to call migrateV1toV2 after parsing, then immediately saveProgress if migration happened (one-time)
    - Export migrateV1toV2 for testing

    **verifier.cjs:**
    - Add `const os = require('os');` at top
    - Add helper `resolvePath(cwd, artifactPath)`: if path starts with `~/` or `~\`, return `path.join(os.homedir(), artifactPath.slice(2))`, else return `path.join(cwd, artifactPath)`
    - Replace line 44 `path.join(cwd, artifact.path)` with `resolvePath(cwd, artifact.path)`
    - Export resolvePath for testing

    **lessons.cjs:**
    - Modify loadModule to include `order` field from module.json in return object (default to 999 if missing)
    - Add `listModules(contentDir)` function: reads all subdirectories of `modules/`, loads each module.json, returns array sorted by `order` field ascending. Returns `[{ id, title, description, order }]`
    - Export listModules
  </action>
  <verify>
    <automated>node --test learn/tests/progress.test.cjs && node --test learn/tests/verifier.test.cjs && node --test learn/tests/lessons.test.cjs</automated>
  </verify>
  <done>
    - v1 progress auto-migrates to v2 with per-module state, written back to disk
    - Tilde paths resolve to home directory in verifier
    - loadModule returns order, listModules returns sorted modules
    - All existing tests still pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Module-owned concept maps, module directories, and default startup</name>
  <files>
    learn/lib/concept-map.cjs,
    learn/bin/gsd-learn.cjs,
    learn/content/modules/command-lifecycle/module.json,
    learn/content/modules/command-lifecycle/concept-map.txt,
    learn/content/modules/gsd-commands/module.json,
    learn/content/modules/gsd-commands/concept-map.txt,
    learn/tests/concept-map.test.cjs
  </files>
  <behavior>
    - renderConceptMap(moduleDir, currentSection) loads ASCII art from concept-map.txt in moduleDir
    - renderConceptMap with missing concept-map.txt returns empty/fallback string (no crash)
    - sectionMap loaded from module.json "sectionMap" field
    - renderConceptMap still applies YOU ARE HERE marker using sectionMap
    - gsd-learn defaults to 'gsd-commands' module (not 'command-lifecycle')
  </behavior>
  <action>
    **concept-map.cjs:**
    - Remove hardcoded `CONCEPT_MAP` constant and inline `sectionMap`
    - Change signature to `renderConceptMap(moduleDir, currentSection)` where moduleDir is the absolute path to the module directory
    - Load ASCII art from `path.join(moduleDir, 'concept-map.txt')` -- if file missing, return a simple "No concept map available" message
    - Load sectionMap from `module.json` in the same moduleDir -- if missing or no sectionMap field, skip YOU ARE HERE marker
    - Keep the same ANSI styled "Architecture Overview:" header and YOU ARE HERE logic
    - Export `renderConceptMap` (remove CONCEPT_MAP export since constant is gone)

    **Module directories:**
    - Update `learn/content/modules/command-lifecycle/module.json`: add `"order": 2` and `"sectionMap": { "entry-point": "Command Spec", "command-spec": "Command Spec", "workflow": "Workflow", "tool-dispatch": "Tool Dispatch", "state": "State", "config": "Config", "phase": "Phase" }`
    - Create `learn/content/modules/command-lifecycle/concept-map.txt` with the existing ASCII art (extracted from the old hardcoded CONCEPT_MAP constant)
    - Create `learn/content/modules/gsd-commands/module.json`: `{ "id": "gsd-commands", "title": "GSD Commands & Workflows", "description": "Learn how GSD slash commands dispatch through command specs to workflow files.", "order": 1, "sectionMap": {} }`
    - Create `learn/content/modules/gsd-commands/concept-map.txt` with a placeholder diagram (Phase 5 will fill with real content)

    **gsd-learn.cjs:**
    - Change default moduleId from `'command-lifecycle'` to `'gsd-commands'` (line 43)
    - Update renderFn to pass moduleDir to renderConceptMap if the renderer uses it (check how renderer calls concept-map -- if renderer calls renderConceptMap internally, the moduleDir needs to be passed through). Alternatively, pass moduleDir into the navigation context.
    - NOTE: Check how renderConceptMap is currently called. If it's called from renderer.cjs, that call site needs updating too. Trace the call chain and update all callers.

    **Tests (concept-map.test.cjs):**
    - Rewrite tests for new signature: renderConceptMap(moduleDir, currentSection)
    - Test with real concept-map.txt file in temp directory
    - Test with missing file returns fallback
    - Test YOU ARE HERE marker still works with sectionMap from module.json
  </action>
  <verify>
    <automated>node --test learn/tests/concept-map.test.cjs</automated>
  </verify>
  <done>
    - Concept map loads from module directory files, not hardcoded constant
    - command-lifecycle has order: 2 and sectionMap in module.json
    - gsd-commands module directory exists with order: 1
    - gsd-learn defaults to gsd-commands module
    - All concept-map tests pass with new signature
  </done>
</task>

</tasks>

<verification>
- `node --test learn/tests/progress.test.cjs` -- migration tests pass
- `node --test learn/tests/verifier.test.cjs` -- tilde expansion tests pass
- `node --test learn/tests/lessons.test.cjs` -- order sorting tests pass
- `node --test learn/tests/concept-map.test.cjs` -- module-owned map tests pass
- `node --test learn/tests/*.test.cjs` -- full suite green (no regressions)
</verification>

<success_criteria>
- v1 progress.json auto-migrates to v2 per-module schema on load
- Concept map renders from module-owned concept-map.txt + sectionMap in module.json
- Verifier resolves ~/paths to home directory cross-platform
- gsd-learn starts with gsd-commands module by default
- command-lifecycle has order: 2, gsd-commands has order: 1
- Full test suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/04-multi-module-infrastructure/04-01-SUMMARY.md`
</output>
