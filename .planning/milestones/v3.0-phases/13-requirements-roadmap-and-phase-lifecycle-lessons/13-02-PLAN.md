---
phase: 13-requirements-roadmap-and-phase-lifecycle-lessons
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - learn/content/modules/planning-state/lessons/04-phase-lifecycle.json
autonomous: true
requirements: [PHSE-01, PHSE-02, PHSE-03, PHSE-04]

must_haves:
  truths:
    - "Lesson 4 loads via loadModule('planning-state') and appears as lesson number 4"
    - "Lesson 4 teaches the five-step phase execution cycle: context -> research -> plan -> execute -> verify"
    - "Lesson 4 teaches PLAN.md anatomy including frontmatter (wave, depends_on, files_modified, must_haves), task XML format, and verification criteria"
    - "Lesson 4 teaches SUMMARY.md and VERIFICATION.md as execution records"
    - "Lesson 4 code blocks contain actual content parsed from GSD templates (phase-prompt.md, summary.md, verification-report.md)"
  artifacts:
    - path: "learn/content/modules/planning-state/lessons/04-phase-lifecycle.json"
      provides: "Lesson 4 content teaching phase execution cycle and plan anatomy"
      contains: "must_haves"
  key_links:
    - from: "04-phase-lifecycle.json"
      to: "module.json sectionMap"
      via: "conceptMap field matching sectionMap key"
      pattern: "phase-lifecycle"
---

<objective>
Create Lesson 4 for Module 3 (Planning & State) teaching the phase execution cycle, PLAN.md anatomy, and SUMMARY.md/VERIFICATION.md as execution records.

Purpose: Lesson 4 completes the "how" of GSD planning. After Lesson 3 teaches what gets built (requirements) and when (roadmap), Lesson 4 teaches the execution mechanics — how a phase goes from goal to shipped code with an audit trail.
Output: learn/content/modules/planning-state/lessons/04-phase-lifecycle.json
</objective>

<execution_context>
@C:/Users/18182/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/18182/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-module-3-infrastructure-and-first-lessons/12-02-SUMMARY.md

@learn/content/modules/planning-state/module.json
@learn/content/modules/planning-state/lessons/02-project-definition.json
@learn/content/modules/planning-state/concept-map.txt

<interfaces>
<!-- Lesson JSON format (from existing lessons): -->
<!--
{
  "id": "string",
  "title": "string",
  "lessonNumber": number,
  "objective": "string",
  "content": [
    { "type": "text", "value": "string", "focus": "string", "bridge": "string" },
    { "type": "code", "language": "markdown|text", "value": "string", "highlight": [line_numbers] }
  ],
  "conceptMap": "string",
  "successCriteria": "string"
}
-->

<!-- module.json sectionMap key for this lesson: "phase-lifecycle" -->

<!-- GSD template sources to parse content from: -->
<!-- C:/Users/18182/.claude/get-shit-done/templates/phase-prompt.md (PLAN.md format) -->
<!-- C:/Users/18182/.claude/get-shit-done/templates/summary.md (SUMMARY.md format) -->
<!-- C:/Users/18182/.claude/get-shit-done/templates/verification-report.md (VERIFICATION.md format) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Lesson 4 - Phase Lifecycle</name>
  <files>learn/content/modules/planning-state/lessons/04-phase-lifecycle.json</files>
  <action>
Create a lesson JSON file following the exact format established by lessons 01 and 02. The lesson must have:

- id: "phase-lifecycle"
- title: "Phase Lifecycle" (or similar like "Inside a Phase: Plan, Execute, Verify")
- lessonNumber: 4
- conceptMap: "phase-lifecycle" (matching module.json sectionMap key)
- successCriteria: describing what the learner can do after completing this lesson

Content blocks (target 9-11 blocks, mix of text and code):

**Block 1 (text):** Bridge from Lesson 3 - requirements become phases in the roadmap, but what happens when a phase starts? Each phase follows a five-step cycle: discuss (optional context gathering), research (optional technical investigation), plan (create PLAN.md files), execute (run tasks, produce SUMMARY.md), verify (check must_haves against reality). Not all steps are required — simple phases skip discuss and research.

**Block 2 (code, language: text):** Show the five-step cycle as a visual flow diagram (similar style to concept-map.txt). Something like:
```
/gsd:discuss-phase  (optional)
       |
       v
/gsd:research-phase (optional)
       |
       v
/gsd:plan-phase     -> PLAN.md files
       |
       v
/gsd:execute-phase  -> SUMMARY.md files
       |
       v
/gsd:verify-phase   -> VERIFICATION.md
```
Highlight the three mandatory steps (plan, execute, verify).

**Block 3 (text):** Explain PLAN.md as the execution blueprint. Each phase produces 1-3 PLAN.md files, each containing 2-3 tasks. Plans have YAML frontmatter for machine-readable metadata and XML task blocks for specific implementation instructions. The key insight: plans are prompts — they contain enough context and specificity that Claude can execute without asking clarifying questions.

**Block 4 (code, language: markdown):** Show the PLAN.md frontmatter structure. Parse from GSD's actual template at C:/Users/18182/.claude/get-shit-done/templates/phase-prompt.md. Show the frontmatter block with phase, plan, type, wave, depends_on, files_modified, autonomous, requirements, and must_haves sections. Highlight the wave, depends_on, and must_haves lines.

**Block 5 (text):** Explain the frontmatter fields: wave = execution order (wave 1 plans run in parallel, wave 2 waits for wave 1), depends_on = which plans must complete first, files_modified = exclusive file ownership preventing merge conflicts, autonomous = whether the plan needs human checkpoints, requirements = traceability back to REQUIREMENTS.md IDs, must_haves = goal-backward verification criteria (truths, artifacts, key_links).

**Block 6 (code, language: xml):** Show the task XML format from the PLAN.md template. Include a task element with name, files, action, verify, and done elements. Use a realistic example (not placeholder text). Highlight the verify and done elements.

**Block 7 (text):** Explain task structure: name is action-oriented, files lists exact paths, action has specific implementation instructions (what to do, how, what to avoid and WHY), verify has an automated command proving the task works, done is measurable acceptance criteria. The specificity test: could a different Claude instance execute this without asking clarifying questions? Task types: auto (fully autonomous), checkpoint:human-verify (pauses for human to confirm), checkpoint:decision (pauses for human choice).

**Block 8 (code, language: markdown):** Show the SUMMARY.md frontmatter structure. Parse from GSD's actual template at C:/Users/18182/.claude/get-shit-done/templates/summary.md. Show the frontmatter with phase, plan, subsystem, requires/provides/affects dependency graph, tech-stack, key-files, key-decisions, patterns-established, requirements-completed, and duration. Highlight the requires/provides/affects and requirements-completed lines.

**Block 9 (text):** Explain SUMMARY.md as the execution record: what was built, how long it took, what decisions were made, what deviated from the plan. The frontmatter dependency graph (requires/provides/affects) enables automatic context assembly — future plans can scan all summaries and select relevant ones based on dependencies. requirements-completed traces back to REQUIREMENTS.md IDs, closing the loop.

**Block 10 (text):** Explain VERIFICATION.md as the phase audit. After all plans execute, verify-phase checks must_haves against reality: Are observable truths actually true? Do required artifacts exist with real implementation (not stubs)? Are key links wired (not just files existing, but connected)? Status is passed, gaps_found, or human_needed. If gaps exist, gap closure plans are created to fix them. Bridge to next lesson: "Now you've seen how a phase executes. The next lesson covers STATE.md — how the project tracks where it is across all these phases and milestones."

**IMPORTANT:** Code blocks must contain actual content from GSD's templates, not invented examples. Follow the same pattern as Lesson 2 (02-project-definition.json) which used real template content in code blocks. Escape all quotes and special characters properly for JSON string values. Newlines in code block values use \n.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const lesson = JSON.parse(fs.readFileSync('learn/content/modules/planning-state/lessons/04-phase-lifecycle.json', 'utf8')); console.log('id:', lesson.id); console.log('lessonNumber:', lesson.lessonNumber); console.log('blocks:', lesson.content.length); console.log('conceptMap:', lesson.conceptMap); console.log('hasSuccessCriteria:', !!lesson.successCriteria); const codeBlocks = lesson.content.filter(b => b.type === 'code'); console.log('codeBlocks:', codeBlocks.length); const hasMustHaves = lesson.content.some(b => b.value && b.value.includes('must_haves')); console.log('hasMustHavesRef:', hasMustHaves); const hasPlanRef = lesson.content.some(b => b.value && b.value.includes('PLAN.md')); console.log('hasPlanRef:', hasPlanRef); if (lesson.lessonNumber !== 4) throw new Error('Wrong lesson number'); if (lesson.conceptMap !== 'phase-lifecycle') throw new Error('Wrong conceptMap'); if (lesson.content.length < 9) throw new Error('Too few blocks'); if (codeBlocks.length < 3) throw new Error('Too few code blocks');"</automated>
  </verify>
  <done>Lesson 4 JSON file exists with lessonNumber 4, conceptMap "phase-lifecycle", 9+ content blocks (3+ code blocks with real GSD template content), covers the five-step phase execution cycle, PLAN.md anatomy (frontmatter + task XML), SUMMARY.md as execution record, VERIFICATION.md as phase audit, and bridges to Lesson 5</done>
</task>

</tasks>

<verification>
- Lesson JSON is valid and parseable
- loadModule('planning-state') returns lesson with correct lessonNumber
- conceptMap matches module.json sectionMap key "phase-lifecycle"
- Content blocks use real GSD template content in code blocks
- Lesson bridges from Lesson 3 (requirements/roadmap) to Lesson 5 (state/milestones)
</verification>

<success_criteria>
- Lesson 4 teaches the five-step phase execution cycle (discuss, research, plan, execute, verify)
- Lesson 4 teaches PLAN.md anatomy with frontmatter and task XML format
- Lesson 4 teaches SUMMARY.md and VERIFICATION.md as execution records
- Code blocks contain actual GSD template content (not invented examples)
- Lesson loads correctly in the learning shell
</success_criteria>

<output>
After completion, create `.planning/phases/13-requirements-roadmap-and-phase-lifecycle-lessons/13-02-SUMMARY.md`
</output>
