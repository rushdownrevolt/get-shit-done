---
phase: 13-requirements-roadmap-and-phase-lifecycle-lessons
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - learn/content/modules/planning-state/lessons/03-requirements-and-roadmap.json
autonomous: true
requirements: [REQR-01, REQR-02, REQR-03, REQR-04]

must_haves:
  truths:
    - "Lesson 3 loads via loadModule('planning-state') and appears as lesson number 3"
    - "Lesson 3 teaches REQUIREMENTS.md structure including REQ-ID format, categories, v1/v2/out-of-scope sections, and traceability table"
    - "Lesson 3 teaches ROADMAP.md structure including phase numbering, milestone grouping, success criteria, and progress table"
    - "Lesson 3 shows how requirements map to phases through the traceability chain"
    - "Lesson 3 code blocks contain actual content parsed from GSD templates (requirements.md and roadmap.md)"
  artifacts:
    - path: "learn/content/modules/planning-state/lessons/03-requirements-and-roadmap.json"
      provides: "Lesson 3 content teaching requirements and roadmap artifacts"
      contains: "REQR"
  key_links:
    - from: "03-requirements-and-roadmap.json"
      to: "module.json sectionMap"
      via: "conceptMap field matching sectionMap key"
      pattern: "requirements-roadmap"
---

<objective>
Create Lesson 3 for Module 3 (Planning & State) teaching REQUIREMENTS.md structure, ROADMAP.md structure, and the traceability chain from requirements to phases.

Purpose: Lesson 3 is the bridge between PROJECT.md (taught in Lesson 2) and phase execution (taught in Lesson 4). The learner needs to understand how project goals become trackable requirements and how those requirements get sequenced into executable phases.
Output: learn/content/modules/planning-state/lessons/03-requirements-and-roadmap.json
</objective>

<execution_context>
@C:/Users/18182/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/18182/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-module-3-infrastructure-and-first-lessons/12-02-SUMMARY.md
@.planning/phases/12-module-3-infrastructure-and-first-lessons/12-03-SUMMARY.md

@learn/content/modules/planning-state/module.json
@learn/content/modules/planning-state/lessons/02-project-definition.json
@learn/content/modules/planning-state/concept-map.txt

<interfaces>
<!-- Lesson JSON format (from existing lessons): -->
<!--
{
  "id": "string",           // matches sectionMap key in module.json
  "title": "string",
  "lessonNumber": number,
  "objective": "string",
  "content": [
    { "type": "text", "value": "string", "focus": "string", "bridge": "string" },
    { "type": "code", "language": "markdown|text", "value": "string", "highlight": [line_numbers] }
  ],
  "conceptMap": "string",   // must match sectionMap key in module.json
  "successCriteria": "string"
}
-->

<!-- module.json sectionMap key for this lesson: "requirements-roadmap" -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Lesson 3 - Requirements and Roadmap</name>
  <files>learn/content/modules/planning-state/lessons/03-requirements-and-roadmap.json</files>
  <action>
Create a lesson JSON file following the exact format established by lessons 01 and 02. The lesson must have:

- id: "requirements-and-roadmap"
- title: "Requirements and Roadmap" (or similar descriptive title like "From Goals to Phases")
- lessonNumber: 3
- conceptMap: "requirements-roadmap" (matching module.json sectionMap key)
- successCriteria: describing what the learner can do after completing this lesson

Content blocks (target 8-10 blocks, mix of text and code):

**Block 1 (text):** Bridge from Lesson 2 - PROJECT.md defines what and why, but requirements make those goals trackable and testable. REQUIREMENTS.md breaks the project scope into individually checkable items. Reference that Lesson 2 ended with "In the next lesson, we'll follow this chain into REQUIREMENTS.md and ROADMAP.md."

**Block 2 (code, language: markdown):** Show the REQUIREMENTS.md template structure. Parse from GSD's actual template at C:/Users/18182/.claude/get-shit-done/templates/requirements.md. Include the header with Defined/Core Value, a category with REQ-ID format items (use the AUTH example from the template), and show the v1/v2/Out of Scope sections. Highlight the REQ-ID lines.

**Block 3 (text):** Explain the requirement format: [CATEGORY]-[NUMBER] (AUTH-01, CONT-02). Requirements are user-centric, testable, atomic. v1 = committed scope (in roadmap), v2 = acknowledged but deferred, Out of Scope = explicitly excluded with reasoning. The three-section structure prevents scope creep — you can't add something to v1 without removing something else or promoting from v2.

**Block 4 (code, language: markdown):** Show the Traceability table from the requirements template. Include the Coverage summary (total, mapped, unmapped). Highlight the table header. This is the bridge between requirements and roadmap — every requirement maps to exactly one phase.

**Block 5 (text):** Explain traceability: each requirement maps to a phase, unmapped = gap in roadmap. Status flows from Pending -> In Progress -> Complete as phases execute. This creates an audit trail — you can always answer "which requirement drove this work?" and "is this requirement satisfied?" The traceability table is updated during roadmap creation and after each phase completes.

**Block 6 (code, language: markdown):** Show the ROADMAP.md phase detail structure. Parse from GSD's actual template at C:/Users/18182/.claude/get-shit-done/templates/roadmap.md. Include one phase entry showing Goal, Depends on, Requirements (with REQ-IDs), Success Criteria (observable behaviors), and Plans list. Highlight the Requirements and Success Criteria lines.

**Block 7 (text):** Explain ROADMAP.md structure: phases are the execution unit, each with a goal (outcome-shaped, not task-shaped), dependencies, requirement IDs, success criteria (2-5 observable behaviors from user perspective), and plan list. Success criteria flow downstream to must_haves in plan files and are verified after execution. Milestone grouping organizes phases — completed milestones collapse in details tags, current milestone stays expanded.

**Block 8 (code, language: markdown):** Show the milestone-grouped roadmap structure from the template. Include the Milestones section with emoji status indicators and the collapsed details tag for completed milestones. Highlight the milestone lines.

**Block 9 (text):** Explain the complete chain: PROJECT.md Core Value -> REQUIREMENTS.md categories with IDs -> ROADMAP.md phases with success criteria -> PLAN.md tasks. Each link is traceable. When a task is verified, it traces back through plan -> phase -> requirement -> core value. This chain means nothing gets built that doesn't serve the project goal, and nothing gets forgotten because every requirement maps somewhere. Bridge to next lesson: "Now that you see how requirements become phases, the next lesson covers what happens INSIDE a phase — the execution cycle."

**IMPORTANT:** Code blocks must contain actual content from GSD's templates, not invented examples. Follow the same pattern as Lesson 2 (02-project-definition.json) which used real template content in code blocks. Escape all quotes and special characters properly for JSON string values. Newlines in code block values use \n.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const lesson = JSON.parse(fs.readFileSync('learn/content/modules/planning-state/lessons/03-requirements-and-roadmap.json', 'utf8')); console.log('id:', lesson.id); console.log('lessonNumber:', lesson.lessonNumber); console.log('blocks:', lesson.content.length); console.log('conceptMap:', lesson.conceptMap); console.log('hasSuccessCriteria:', !!lesson.successCriteria); const codeBlocks = lesson.content.filter(b => b.type === 'code'); console.log('codeBlocks:', codeBlocks.length); const hasReqId = lesson.content.some(b => b.value && b.value.includes('AUTH-')); console.log('hasReqIdExample:', hasReqId); if (lesson.lessonNumber !== 3) throw new Error('Wrong lesson number'); if (lesson.conceptMap !== 'requirements-roadmap') throw new Error('Wrong conceptMap'); if (lesson.content.length < 8) throw new Error('Too few blocks'); if (codeBlocks.length < 3) throw new Error('Too few code blocks');"</automated>
  </verify>
  <done>Lesson 3 JSON file exists with lessonNumber 3, conceptMap "requirements-roadmap", 8+ content blocks (3+ code blocks with real GSD template content), covers REQUIREMENTS.md structure (REQ-ID format, categories, v1/v2/out-of-scope, traceability) and ROADMAP.md structure (phases, milestones, success criteria), and bridges to Lesson 4</done>
</task>

</tasks>

<verification>
- Lesson JSON is valid and parseable
- loadModule('planning-state') returns lesson with correct lessonNumber
- conceptMap matches module.json sectionMap key "requirements-roadmap"
- Content blocks use real GSD template content in code blocks
- Lesson bridges from Lesson 2 (project definition) to Lesson 4 (phase lifecycle)
</verification>

<success_criteria>
- Lesson 3 teaches REQUIREMENTS.md structure with REQ-ID format, categories, traceability
- Lesson 3 teaches ROADMAP.md structure with phases, milestones, success criteria
- Lesson 3 shows the traceability chain from requirements to phases
- Code blocks contain actual GSD template content (not invented examples)
- Lesson loads correctly in the learning shell
</success_criteria>

<output>
After completion, create `.planning/phases/13-requirements-roadmap-and-phase-lifecycle-lessons/13-01-SUMMARY.md`
</output>
