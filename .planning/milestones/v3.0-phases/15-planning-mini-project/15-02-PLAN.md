---
phase: 15-planning-mini-project
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - learn/content/modules/planning-state/project/hints.json
autonomous: true
requirements: [MINI-05]

must_haves:
  truths:
    - "Learner can get 5 progressive hints from conceptual to specific"
    - "Hints guide from why-artifacts-persist through read/write patterns"
    - "Running --hint --module=planning-state returns hints in order"
  artifacts:
    - path: "learn/content/modules/planning-state/project/hints.json"
      provides: "5 progressive hints for mini-project"
      min_lines: 5
  key_links:
    - from: "learn/content/modules/planning-state/project/hints.json"
      to: "learn/lib/hints.cjs"
      via: "getNextHint reads hints array and returns next by index"
      pattern: "hints\\[hintsUsed\\]"
---

<objective>
Create 5 progressive hints for the Module 3 mini-project. Hints guide the learner from conceptual understanding (why artifacts persist state) through increasingly specific guidance about the read-previous and write-new patterns they need to add to their skeptic workflow.

Purpose: Progressive hints are the safety net that prevents learners from getting stuck while preserving the learning challenge.
Output: hints.json file with 5 ordered hint strings.
</objective>

<execution_context>
@C:/Users/18182/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/18182/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

<!-- Prior module hint patterns to follow -->
@learn/content/modules/gsd-commands/project/hints.json
@learn/content/modules/command-lifecycle/project/hints.json

<interfaces>
<!-- From learn/lib/hints.cjs — hints.json is a simple JSON array of strings -->
hints.json shape: string[]  (ordered from vague/conceptual to specific/actionable)

<!-- From gsd-learn.cjs — hints loaded by module ID -->
hintsPath = path.join(__dirname, '..', 'content', 'modules', activeModuleId, 'project', 'hints.json')
Accessed via: node learn/bin/gsd-learn.cjs --hint --module=planning-state
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 5 progressive hints for artifact-persistence mini-project</name>
  <files>learn/content/modules/planning-state/project/hints.json</files>
  <action>
Create hints.json at learn/content/modules/planning-state/project/hints.json as a JSON array of 5 strings, progressing from conceptual to specific. Follow the same progression style as the Module 1 and Module 2 hints.

The 5 hints should progress as follows:

**Hint 1 (conceptual — why):** Explain the core idea. The skeptic workflow currently analyzes and prints findings, then they vanish. The artifact persistence pattern solves this: read what was found before, write what was found now. Think about what your workflow needs to DO differently, not what it needs to SAY.

**Hint 2 (structural — what to add):** Point toward the two additions needed. Your workflow needs two new steps in its process section: one that reads .planning/SKEPTIC-REVIEW.md if it exists (before analysis), and one that writes findings to .planning/SKEPTIC-REVIEW.md (after analysis). The template from the lesson page shows the pattern — press C on the lesson to copy it.

**Hint 3 (read-previous specifics):** Guide the read-previous step. Add a step in your workflow's process section that reads the previous review artifact. It should check if .planning/SKEPTIC-REVIEW.md exists, read its contents for historical context, and use previous findings to inform the current analysis. If the file does not exist yet, the step should note that this is the first review.

**Hint 4 (write-new specifics):** Guide the write-new step. Add a step that writes findings to .planning/SKEPTIC-REVIEW.md. Each run should append a new dated section (like ## 2024-03-15) rather than overwriting. Include findings, observations, and recommendations. The file accumulates over time — that is what makes it persistent.

**Hint 5 (most specific — putting it together):** Give concrete guidance. In your workflow at ~/.claude/get-shit-done/workflows/skeptic.md, add two new steps inside the process section. The first step (name it something like "read_previous_reviews") reads .planning/SKEPTIC-REVIEW.md for historical context. The second step (name it something like "write_review_artifact") writes the current analysis as a new dated section to .planning/SKEPTIC-REVIEW.md. Then run /gsd:skeptic on any project to generate the artifact file. Verify with --verify --module=planning-state.

IMPORTANT: Write as a flat JSON array of 5 strings. No objects, no nesting. Study the existing hints.json files for the exact format.
  </action>
  <verify>
    <automated>node -e "const h=require('./learn/content/modules/planning-state/project/hints.json'); if(!Array.isArray(h)) { console.log('ERROR: not an array'); process.exit(1); } console.log('hints:', h.length); if(h.length!==5) { console.log('ERROR: expected 5 hints'); process.exit(1); } h.forEach((hint,i) => { if(typeof hint !== 'string') { console.log('ERROR: hint', i, 'is not a string'); process.exit(1); } console.log('Hint', i+1, ':', hint.substring(0, 60) + '...'); }); console.log('ALL OK')"</automated>
  </verify>
  <done>hints.json is a valid JSON array of exactly 5 strings, progressing from conceptual (why artifacts persist) to specific (exact workflow steps to add and verify command to run)</done>
</task>

</tasks>

<verification>
1. `node -e "require('./learn/content/modules/planning-state/project/hints.json')"` — valid JSON
2. `node learn/bin/gsd-learn.cjs --hint --module=planning-state` — returns first hint without error
3. Hint array has exactly 5 elements, all strings
4. Hints progress from conceptual to specific (verified by reading content)
</verification>

<success_criteria>
- hints.json contains exactly 5 string hints
- Hints progress from "why artifacts persist" (hint 1) to "exact workflow steps" (hint 5)
- Running --hint --module=planning-state returns the first hint correctly
- Hint format matches Module 1 and Module 2 patterns (flat JSON array of strings)
</success_criteria>

<output>
After completion, create `.planning/phases/15-planning-mini-project/15-02-SUMMARY.md`
</output>
