---
phase: 22-module-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - learn/content/modules/quality-feedback/module.json
  - learn/content/modules/quality-feedback/lessons/.gitkeep
  - learn/content/modules/quality-feedback/project/.gitkeep
  - learn/content/modules/quality-feedback/concept-map.txt
  - learn/lib/progress.cjs
  - learn/tests/progress.test.cjs
autonomous: true
requirements: [INFR-01, INFR-02, INFR-03]

must_haves:
  truths:
    - "Module picker shows 'Quality & Feedback Loops' as Module 5"
    - "Existing users' v5 progress files migrate to v6 with zero data loss"
    - "concept-map.txt shows lesson flow from overview through synthesis"
    - "Module directory has module.json, lessons/, project/ matching convention"
  artifacts:
    - path: "learn/content/modules/quality-feedback/module.json"
      provides: "Module 5 registration with id, title, description, order, sectionMap"
      contains: "quality-feedback"
    - path: "learn/content/modules/quality-feedback/concept-map.txt"
      provides: "Visual lesson flow diagram"
      contains: "Overview"
    - path: "learn/content/modules/quality-feedback/lessons/.gitkeep"
      provides: "Lessons directory placeholder"
    - path: "learn/content/modules/quality-feedback/project/.gitkeep"
      provides: "Project directory placeholder"
    - path: "learn/lib/progress.cjs"
      provides: "migrateV5toV6 function and v6 DEFAULT_PROGRESS"
      exports: ["migrateV5toV6"]
    - path: "learn/tests/progress.test.cjs"
      provides: "Tests for v5->v6 migration"
      contains: "migrateV5toV6"
  key_links:
    - from: "learn/lib/progress.cjs"
      to: "loadProgress chain"
      via: "migrateV5toV6 called when version < 6"
      pattern: "migrateV5toV6"
    - from: "learn/content/modules/quality-feedback/module.json"
      to: "module picker"
      via: "order: 5 registration"
      pattern: "\"order\": 5"
---

<objective>
Scaffold Module 5 "Quality & Feedback Loops" directory structure, add v5->v6 progress migration with TDD, and create concept map.

Purpose: Module 5 must exist in the system before lessons can be authored. Existing users need seamless progress migration.
Output: Module directory with module.json, progress migration chain extended to v6, concept map showing lesson flow.
</objective>

<execution_context>
@C:/Users/18182/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/18182/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Existing module.json pattern (from agent-orchestration): -->
```json
{
  "id": "agent-orchestration",
  "title": "Agent Orchestration",
  "description": "Learn how GSD orchestrates work through subagents...",
  "order": 4,
  "sectionMap": {
    "overview": "Overview",
    "subagent-types": "Subagent Types",
    ...
    "mini-project": "Mini-Project"
  }
}
```

<!-- Existing migration pattern (from progress.cjs): -->
```javascript
function migrateV4toV5(progress) {
  if (progress.version >= 5) { return progress; }
  return {
    version: 5,
    currentModule: progress.currentModule,
    currentLesson: progress.currentLesson,
    modules: { ...progress.modules },
  };
}
// DEFAULT_PROGRESS.version = 5
// loadProgress chains: v1->v2->v3->v4->v5
// module.exports includes all migrate functions
```

<!-- Existing test pattern (from progress.test.cjs): -->
```javascript
describe('migrateV4toV5()', () => {
  test('v4 data gets version bumped to 5, all entries preserved', ...);
  test('v5 data passes through unchanged (idempotent)', ...);
  test('v4 data with no modules migrates cleanly to v5', ...);
});
// loadProgress integration tests: auto-migrates vN to current, writes back
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Module 5 directory and module.json</name>
  <files>learn/content/modules/quality-feedback/module.json, learn/content/modules/quality-feedback/lessons/.gitkeep, learn/content/modules/quality-feedback/project/.gitkeep</files>
  <action>
Create the directory structure `learn/content/modules/quality-feedback/` with `lessons/` and `project/` subdirectories (each containing `.gitkeep`).

Create `module.json` with:
```json
{
  "id": "quality-feedback",
  "title": "Quality & Feedback Loops",
  "description": "Learn how GSD verifies work and closes the feedback loop -- UAT, skeptic reviews, debug workflows, and gap closure cycles.",
  "order": 5,
  "sectionMap": {
    "overview": "Overview",
    "verify-work": "Verify-Work & UAT",
    "skeptic": "Skeptic Reviews",
    "debug": "Debug Workflows",
    "gap-closure": "Gap Closure",
    "milestone-audit": "Milestone Audit",
    "synthesis": "Synthesis",
    "mini-project": "Mini-Project"
  }
}
```

sectionMap has 8 entries (7 lessons + mini-project) matching the concept map flow.
  </action>
  <verify>
    <automated>node -e "const m = require('./learn/content/modules/quality-feedback/module.json'); const assert = require('assert'); assert.strictEqual(m.id, 'quality-feedback'); assert.strictEqual(m.order, 5); assert.strictEqual(Object.keys(m.sectionMap).length, 8); console.log('OK');"</automated>
  </verify>
  <done>Module directory exists with module.json (id: quality-feedback, order: 5, 8 sectionMap entries), lessons/.gitkeep, project/.gitkeep</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TDD v5-to-v6 progress migration</name>
  <files>learn/lib/progress.cjs, learn/tests/progress.test.cjs</files>
  <behavior>
    - migrateV5toV6: v5 data with existing modules gets version bumped to 6, all module entries preserved (currentModule, currentLesson, modules with started/completed flags)
    - migrateV5toV6: v6 data passes through unchanged (idempotent)
    - migrateV5toV6: v5 data with no modules migrates cleanly to v6
    - loadProgress integration: auto-migrates v5 file to v6 and writes back to disk
    - loadProgress integration: does not rewrite v6 data
    - loadProgress: returns v6 default when file missing
    - Full chain: v1 data auto-migrates through v1->v2->v3->v4->v5->v6
  </behavior>
  <action>
RED phase: Add test describe block `migrateV5toV6()` with 3 tests following the exact migrateV4toV5 test pattern. Add loadProgress integration tests for v5->v6 migration. Update existing tests that reference v5 as the current version to reference v6 (DEFAULT_PROGRESS returns v6, loadProgress returns v6 defaults, full chain migrates to v6). Run tests -- they MUST fail.

GREEN phase: In progress.cjs:
1. Update `DEFAULT_PROGRESS.version` from 5 to 6
2. Add `migrateV5toV6(progress)` function following exact migrateV4toV5 pattern (structural version bump, no schema changes)
3. Add migration step in `loadProgress`: `if (progress.version < 6) { progress = migrateV5toV6(progress); }`
4. Add `migrateV5toV6` to `module.exports`
Run tests -- they MUST pass.
  </action>
  <verify>
    <automated>node --test learn/tests/progress.test.cjs</automated>
  </verify>
  <done>migrateV5toV6 function exists and is exported, DEFAULT_PROGRESS.version is 6, loadProgress chains v1->v2->v3->v4->v5->v6, all tests pass including new v5->v6 tests and updated existing tests</done>
</task>

<task type="auto">
  <name>Task 3: Create concept map</name>
  <files>learn/content/modules/quality-feedback/concept-map.txt</files>
  <action>
Create `concept-map.txt` following the exact same ASCII art format as `learn/content/modules/agent-orchestration/concept-map.txt`. Show the lesson flow:

```
  Quality & Feedback Loops
        |
        v
  Overview (quality lifecycle: build -> verify -> diagnose -> fix -> re-verify)
        |
        v
  Verify-Work & UAT (conversational testing, severity, persistent UAT.md)
        |
        v
  Skeptic Reviews (proactive critical assessment, skeptic.md)
        |
        v
  Debug Workflows (systematic debugging, hypothesis testing, persistent state)
        |
        v
  Gap Closure (diagnosis -> plan --gaps -> execute --gaps-only cycle)
        |
        v
  Milestone Audit (audit-milestone, plan-milestone-gaps, completion gates)
        |
        v
  Synthesis (quality loops in the full GSD lifecycle)
```

Use the box-drawing style from the agent-orchestration concept-map (boxes with +---+ borders and descriptive subtitles inside).
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const c = fs.readFileSync('learn/content/modules/quality-feedback/concept-map.txt', 'utf-8'); const assert = require('assert'); assert.ok(c.includes('Overview'), 'has Overview'); assert.ok(c.includes('Synthesis'), 'has Synthesis'); assert.ok(c.includes('Gap Closure'), 'has Gap Closure'); console.log('OK');"</automated>
  </verify>
  <done>concept-map.txt exists showing linear flow from Overview through Verify-Work, Skeptic, Debug, Gap Closure, Milestone Audit, to Synthesis</done>
</task>

</tasks>

<verification>
1. Module directory structure: `ls learn/content/modules/quality-feedback/` shows module.json, lessons/, project/, concept-map.txt
2. Module registration: `node -e "const m = require('./learn/content/modules/quality-feedback/module.json'); console.log(m.title, 'order:', m.order);"` prints "Quality & Feedback Loops order: 5"
3. Progress migration: `node --test learn/tests/progress.test.cjs` all tests pass
4. Concept map: `cat learn/content/modules/quality-feedback/concept-map.txt` shows 7-lesson flow
</verification>

<success_criteria>
- Module 5 "quality-feedback" registered with order 5 and 8 sectionMap entries
- v5->v6 progress migration works with zero data loss, full chain v1->v6 tested
- Concept map shows Overview -> Verify-Work -> Skeptic -> Debug -> Gap Closure -> Milestone Audit -> Synthesis
- All progress tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/22-module-infrastructure/22-01-SUMMARY.md`
</output>
