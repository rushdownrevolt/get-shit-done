---
phase: 02-prompt-driven-content-pipeline
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - learn/lib/parser.cjs
  - learn/lib/prompt-templates.cjs
  - learn/tests/parser.test.cjs
  - learn/tests/prompt-templates.test.cjs
  - learn/content/prompts/overview.prompt.md
  - learn/content/prompts/source-dive.prompt.md
autonomous: true
requirements: [CONT-03, CONT-04, MODL-02]

must_haves:
  truths:
    - "Parser extracts exports, functions, requires, JSDoc, and section separators from any GSD .cjs file"
    - "Prompt templates inject parsed source context into structured prompt text with lesson schema"
    - "Overview and source-dive templates produce structurally different prompts for different lesson types"
    - "Prompt templates instruct the LLM to explain WHY, not just WHAT"
  artifacts:
    - path: "learn/lib/parser.cjs"
      provides: "GSD source file parser"
      exports: ["parseSourceFile"]
    - path: "learn/lib/prompt-templates.cjs"
      provides: "Prompt template assembly"
      exports: ["assemblePrompt"]
    - path: "learn/content/prompts/overview.prompt.md"
      provides: "Overview lesson prompt template"
      contains: "conceptual overview"
    - path: "learn/content/prompts/source-dive.prompt.md"
      provides: "Source deep-dive lesson prompt template"
      contains: "FUNCTIONS"
    - path: "learn/tests/parser.test.cjs"
      provides: "Parser unit tests"
    - path: "learn/tests/prompt-templates.test.cjs"
      provides: "Prompt template unit tests"
  key_links:
    - from: "learn/lib/prompt-templates.cjs"
      to: "learn/lib/parser.cjs"
      via: "assemblePrompt consumes parseSourceFile output"
      pattern: "parseSourceFile"
    - from: "learn/content/prompts/source-dive.prompt.md"
      to: "learn/lib/prompt-templates.cjs"
      via: "template markers replaced by assemblePrompt"
      pattern: "\\{\\{FUNCTIONS\\}\\}"
---

<objective>
Build the GSD source parser and prompt template system that extracts structured data from GSD CommonJS files and assembles lesson-generation prompts.

Purpose: These are the foundation modules for the content pipeline. The parser provides structured source context; the templates shape that context into prompts that produce quality lesson JSON.
Output: Two tested library modules (parser.cjs, prompt-templates.cjs) and two prompt template files (overview, source-dive).
</objective>

<execution_context>
@C:/Users/18182/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/18182/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-prompt-driven-content-pipeline/02-RESEARCH.md

<interfaces>
<!-- Phase 1 lesson JSON schema (required fields validated by lessons.cjs) -->
<!-- Generated lessons MUST conform to this schema -->

From learn/lib/lessons.cjs:
```javascript
// Required fields for every lesson JSON file:
const required = ['id', 'title', 'lessonNumber', 'objective', 'content', 'conceptMap', 'successCriteria'];
```

Example lesson structure (from learn/content/modules/command-lifecycle/lessons/01-welcome.json):
```json
{
  "id": "welcome",
  "title": "Welcome to GSD",
  "lessonNumber": 1,
  "objective": "One sentence describing what learner will understand",
  "content": [
    { "type": "text", "value": "Explanation text..." },
    { "type": "code", "language": "javascript", "value": "actual code", "highlight": [2] },
    { "type": "text", "value": "Why explanation..." }
  ],
  "conceptMap": null,
  "successCriteria": "One sentence describing what learner can do after"
}
```

GSD source file patterns (what the parser must handle):
```javascript
// Pattern 1: Module doc (JSDoc at top)
/** Module Name — Description */

// Pattern 2: Requires
const fs = require('fs');
const { execSync } = require('child_process');

// Pattern 3: Section separators
// --- Section Name -----

// Pattern 4: Constants
const MODEL_PROFILES = { ... };

// Pattern 5: Function declarations with JSDoc
/** Description. */
function funcName(param) { ... }

// Pattern 6: module.exports at file end
module.exports = { func1, func2 };
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Source parser with TDD</name>
  <files>learn/lib/parser.cjs, learn/tests/parser.test.cjs</files>
  <behavior>
    - parseSourceFile returns object with filePath, fileName, lineCount, moduleDoc, requires, exports, functions, sections, constants
    - extractModuleDoc pulls top-level JSDoc comment (/** ... */ at file start)
    - extractRequires finds all require() calls with binding name, module path, isDestructured flag
    - extractExports parses module.exports = { name1, name2 } into string array
    - extractFunctions finds function declarations with name, startLine, endLine, code, jsdoc (preceding JSDoc if any)
    - extractSections finds // --- Section Name --- separator comments
    - extractConstants finds const UPPER_CASE = ... declarations
    - Tested against a real GSD source file (get-shit-done/bin/lib/core.cjs) to verify accuracy
  </behavior>
  <action>
    RED: Create learn/tests/parser.test.cjs with tests for each extraction function. Use a small inline fixture string for unit tests (10-15 lines of representative CommonJS code with module doc, requires, function, constant, section separator, exports). Add one integration test that runs parseSourceFile against the real get-shit-done/bin/lib/core.cjs file and asserts it finds known exports (like 'output', 'error', 'safeReadFile') and known functions.

    GREEN: Create learn/lib/parser.cjs implementing parseSourceFile and all extract* helpers using regex. Follow the patterns from 02-RESEARCH.md Pattern 1. Key implementation details:
    - extractModuleDoc: match /^\/\*\*\s*\n([\s\S]*?)\*\// at start of source
    - extractRequires: match const/let/var binding = require('module') per line
    - extractExports: match module.exports = { ... } and split on commas
    - extractFunctions: line-by-line scan for `function funcName(` with brace counting for body extraction; look backward for JSDoc
    - extractSections: match // --- ... --- or // ─── ... ─── separator patterns
    - extractConstants: match const UPPER_SNAKE_CASE = ... declarations
    - All functions are synchronous, using fs.readFileSync for file reading
    - Module follows GSD conventions: 'use strict', single module.exports at end

    Keep parser under 200 lines (per Pitfall 1 guidance). Build for GSD's actual patterns ONLY.
  </action>
  <verify>
    <automated>node --test learn/tests/parser.test.cjs</automated>
  </verify>
  <done>parseSourceFile correctly extracts moduleDoc, requires, exports, functions, sections, and constants from GSD source files. Integration test against core.cjs passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prompt templates with TDD</name>
  <files>learn/lib/prompt-templates.cjs, learn/tests/prompt-templates.test.cjs, learn/content/prompts/overview.prompt.md, learn/content/prompts/source-dive.prompt.md</files>
  <behavior>
    - assemblePrompt('overview', context) reads overview.prompt.md and replaces {{LESSON_NUMBER}}, {{LESSON_TITLE}} markers with context values
    - assemblePrompt('source-dive', context) reads source-dive.prompt.md and replaces {{FILE_NAME}}, {{MODULE_DOC}}, {{EXPORTS}}, {{FUNCTIONS}}, {{REQUIRES}}, {{LESSON_NUMBER}}, {{LESSON_TITLE}}, {{FOCUS}}, {{SOURCE_CODE}} markers
    - Overview template does NOT contain {{FUNCTIONS}} or {{SOURCE_CODE}} markers (different structure per MODL-02)
    - Source-dive template contains {{FUNCTIONS}} and {{SOURCE_CODE}} markers
    - Both templates contain the exact lesson JSON schema with all 7 required fields in their output instructions
    - Both templates explicitly instruct "explain WHY this design choice was made" (per CONT-04)
    - assemblePrompt returns the fully assembled prompt string
  </behavior>
  <action>
    RED: Create learn/tests/prompt-templates.test.cjs testing:
    1. assemblePrompt('overview', ...) returns string with lesson number and title substituted
    2. assemblePrompt('source-dive', ...) returns string with all markers substituted (fileName, moduleDoc, exports, functions, requires, focus)
    3. Overview template does not contain {{FUNCTIONS}} marker (structural difference per MODL-02)
    4. Source-dive template contains {{FUNCTIONS}} marker
    5. Both templates contain the JSON schema with all 7 required fields (id, title, lessonNumber, objective, content, conceptMap, successCriteria)
    6. Both templates contain "why" instruction text (CONT-04 requirement)

    GREEN: Create the two prompt template files and prompt-templates.cjs:

    learn/content/prompts/overview.prompt.md:
    - Title: "Conceptual Overview Lesson Generator"
    - Context section with {{LESSON_NUMBER}}, {{LESSON_TITLE}}, {{MODULE_OVERVIEW}} markers
    - Instructions emphasizing big-picture architecture understanding
    - Explicit "explain WHY" instruction: "For every design choice mentioned, explain WHY it was made. Reference GSD's constraints: zero dependencies, CommonJS, CLI-native, cross-platform."
    - Full lesson JSON schema in output requirements section with example
    - Content guidelines: alternate text and code, keep code snippets to 5-15 lines

    learn/content/prompts/source-dive.prompt.md:
    - Title: "Source Code Deep-Dive Lesson Generator"
    - Context section with {{FILE_NAME}}, {{MODULE_DOC}}, {{EXPORTS}}, {{FUNCTIONS}}, {{REQUIRES}}, {{LESSON_NUMBER}}, {{LESSON_TITLE}}, {{FOCUS}} markers
    - Instructions for code-focused teaching
    - Explicit "why" instruction: "For every code snippet, explain WHY this design choice was made, not just WHAT it does."
    - Full lesson JSON schema in output requirements section
    - Content guidelines: actual source code only (no invented code), highlight important lines

    learn/lib/prompt-templates.cjs:
    - assemblePrompt(templateName, context) function
    - Reads template from learn/content/prompts/{templateName}.prompt.md
    - Replaces all {{MARKER}} placeholders with context values
    - Helper functions: formatExports (bullet list), formatFunctions (code blocks with JSDoc), formatRequires (bullet list)
    - Falls back to sensible defaults for missing context values ('None', empty string)
    - Module follows GSD conventions: 'use strict', single module.exports at end
  </action>
  <verify>
    <automated>node --test learn/tests/prompt-templates.test.cjs</automated>
  </verify>
  <done>assemblePrompt produces complete prompts for both overview and source-dive types. Templates include JSON schema, "why" instructions, and appropriate markers for each lesson type.</done>
</task>

</tasks>

<verification>
All Phase 2 Plan 01 tests pass:
```bash
node --test learn/tests/parser.test.cjs learn/tests/prompt-templates.test.cjs
```

Parser correctly parses real GSD files. Prompt templates produce structurally different prompts for overview vs source-dive lesson types.
</verification>

<success_criteria>
- parseSourceFile extracts accurate data from GSD .cjs files (verified against core.cjs)
- assemblePrompt produces complete prompts with parsed source injected
- Overview and source-dive templates are structurally different (MODL-02)
- Both templates instruct the LLM to explain "why" not just "what" (CONT-04)
- Both templates include full lesson JSON schema in output requirements (CONT-03)
- All tests green
</success_criteria>

<output>
After completion, create `.planning/phases/02-prompt-driven-content-pipeline/02-01-SUMMARY.md`
</output>
