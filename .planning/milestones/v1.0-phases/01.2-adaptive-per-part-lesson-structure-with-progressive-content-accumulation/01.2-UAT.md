---
status: testing
phase: 01.2-adaptive-per-part-lesson-structure-with-progressive-content-accumulation
source: [01.2-01-SUMMARY.md, 01.2-02-SUMMARY.md]
started: 2026-03-12T18:30:00Z
updated: 2026-03-12T18:30:00Z
---

## Current Test

number: 1
name: Progressive Accumulation
expected: |
  Run `node learn/bin/gsd-learn.cjs`. Navigate to Lesson 1, Part 1 — only block 1 content visible. Press 'w' — screen clears and shows blocks 1 AND 2 together. Press 'w' again — blocks 1, 2, AND 3 all visible. Content grows with each press.
awaiting: user response

## Tests

### 1. Progressive Accumulation
expected: Run `node learn/bin/gsd-learn.cjs`. Navigate to Lesson 1, Part 1 — only block 1 content visible. Press 'w' — screen clears and shows blocks 1 AND 2 together. Press 'w' again — blocks 1, 2, AND 3 all visible. Content grows with each press.
result: [pending]

### 2. Focus Lines with Current Block Marker
expected: Each accumulated block has a focus line above its content (short phrase in dim text). Only the current (newest) block's focus line has a ▶ marker. Prior blocks' focus lines have no marker.
result: [pending]

### 3. Bridge Sections
expected: After each block's content, a bordered section appears containing bridge text (a sentence connecting to the next block). Borders use box-drawing characters. Bridge is visible on all accumulated blocks.
result: [pending]

### 4. Block Separation
expected: When multiple blocks are accumulated, dim horizontal rules separate them visually. Each block is clearly distinct from the previous one.
result: [pending]

### 5. Objective and Success Criteria Placement
expected: On Part 1 (first part of a lesson), the lesson objective ("What you'll learn") appears above the first block. On the last part, success criteria ("You'll know you've got it when") appears after all blocks. Objective does NOT appear on middle or last parts. Criteria does NOT appear on first or middle parts.
result: [pending]

### 6. Backward Navigation
expected: After accumulating 3+ blocks, press 'q'. Screen clears and shows fewer blocks (last block removed). Press 'q' again — one more block removed. Content shrinks on each back-press.
result: [pending]

### 7. Focus/Bridge Content Quality
expected: Focus phrases are descriptive and unique per block (3-8 words, not truncated first sentences). Bridge sentences create narrative flow between blocks. Last block of each lesson bridges to the next lesson. Last block of lesson 6 bridges to module completion.
result: [pending]

### 8. Load-Time Validation
expected: Run `node -e "const l = require('./learn/lib/lessons.cjs'); l.loadModule('command-lifecycle'); console.log('All lessons valid')"` — prints "All lessons valid" without errors. All 6 lessons have focus and bridge on every content item.
result: [pending]

## Summary

total: 8
passed: 0
issues: 0
pending: 8
skipped: 0

## Gaps

[none yet]
