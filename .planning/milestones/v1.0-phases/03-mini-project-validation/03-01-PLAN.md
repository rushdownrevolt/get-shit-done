---
phase: 03-mini-project-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - learn/lib/verifier.cjs
  - learn/lib/hints.cjs
  - learn/lib/feedback.cjs
  - learn/tests/verifier.test.cjs
  - learn/tests/hints.test.cjs
  - learn/tests/feedback.test.cjs
  - learn/content/modules/command-lifecycle/lessons/06-mini-project.json
  - learn/content/modules/command-lifecycle/project/spec.json
  - learn/content/modules/command-lifecycle/project/hints.json
autonomous: true
requirements: [VALD-01, VALD-02, VALD-03, VALD-04, MODL-03, MODL-04]

must_haves:
  truths:
    - "verifyArtifact checks file existence and regex patterns, returning structured pass/fail results"
    - "getNextHint serves hints progressively and reports remaining count"
    - "recordEvent appends timestamped events to feedback.json with project_started/completed/verify_attempt/hint_requested types"
    - "06-mini-project.json lesson loads correctly and contains project content type with deliverables"
    - "spec.json defines structural checks for the echo command mini-project"
    - "hints.json contains 5-6 progressive hints that escalate from vague to specific without giving code"
  artifacts:
    - path: "learn/lib/verifier.cjs"
      provides: "Structural artifact verification"
      exports: ["verifyArtifact", "runVerification"]
    - path: "learn/lib/hints.cjs"
      provides: "Progressive hint delivery"
      exports: ["getNextHint"]
    - path: "learn/lib/feedback.cjs"
      provides: "Feedback data collection"
      exports: ["loadFeedback", "saveFeedback", "recordEvent", "FEEDBACK_PATH"]
    - path: "learn/tests/verifier.test.cjs"
      provides: "Verifier tests"
      min_lines: 40
    - path: "learn/tests/hints.test.cjs"
      provides: "Hints tests"
      min_lines: 30
    - path: "learn/tests/feedback.test.cjs"
      provides: "Feedback tests"
      min_lines: 40
    - path: "learn/content/modules/command-lifecycle/lessons/06-mini-project.json"
      provides: "Mini-project lesson content"
      contains: "\"type\": \"project\""
    - path: "learn/content/modules/command-lifecycle/project/spec.json"
      provides: "Verification spec for echo command"
      contains: "command-lifecycle-project"
    - path: "learn/content/modules/command-lifecycle/project/hints.json"
      provides: "Progressive hint array"
  key_links:
    - from: "learn/lib/verifier.cjs"
      to: "learn/content/modules/command-lifecycle/project/spec.json"
      via: "runVerification loads spec and runs verifyArtifact per artifact"
      pattern: "verifyArtifact"
    - from: "learn/lib/feedback.cjs"
      to: ".planning/learn/feedback.json"
      via: "loadFeedback/saveFeedback reads/writes JSON"
      pattern: "feedback\\.json"
---

<objective>
Build the three core library modules (verifier, hints, feedback) with TDD, plus all content files (mini-project lesson, project spec, progressive hints).

Purpose: Establishes all building blocks needed for Plan 02 to wire into the CLI. Each module is independently testable with no external dependencies.
Output: 3 lib modules, 3 test files, 3 content files -- all passing tests.
</objective>

<execution_context>
@C:/Users/18182/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/18182/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-mini-project-validation/03-RESEARCH.md

<interfaces>
<!-- Existing modules the executor needs to understand -->

From learn/lib/progress.cjs:
```javascript
const PROGRESS_PATH = path.join('.planning', 'learn', 'progress.json');
function loadProgress(cwd) { /* returns { version, currentModule, currentLesson, modules } */ }
function saveProgress(cwd, progress) { /* writes JSON */ }
module.exports = { loadProgress, saveProgress };
```

From learn/lib/terminal.cjs:
```javascript
module.exports = { style, clearScreen, horizontalRule, highlightJS, stripAnsi, isForcedColor };
```

From learn/lib/renderer.cjs:
```javascript
function renderLesson(lesson, currentIndex, totalLessons) { /* returns string */ }
module.exports = { renderLesson };
```

Lesson JSON schema (from 01-welcome.json):
```json
{
  "id": "string",
  "title": "string",
  "lessonNumber": 1,
  "objective": "string",
  "content": [ { "type": "text|code", "value": "string" } ],
  "conceptMap": "string",
  "successCriteria": "string"
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD verifier, hints, and feedback lib modules</name>
  <files>learn/lib/verifier.cjs, learn/lib/hints.cjs, learn/lib/feedback.cjs, learn/tests/verifier.test.cjs, learn/tests/hints.test.cjs, learn/tests/feedback.test.cjs</files>
  <behavior>
    verifier.test.cjs:
    - verifyArtifact returns { passed: false } when file does not exist
    - verifyArtifact returns { passed: true, results: [...] } when file exists and all regex patterns match
    - verifyArtifact returns { passed: false } when file exists but a pattern does not match
    - verifyArtifact results array includes a 'File exists' check plus one entry per structural check
    - runVerification loads a spec.json, runs verifyArtifact for each artifact entry, returns aggregate { passed, artifacts: [...] }

    hints.test.cjs:
    - getNextHint returns first hint when hintsUsed is 0
    - getNextHint returns second hint when hintsUsed is 1
    - getNextHint returns { hint: null, remaining: 0 } when all hints exhausted
    - getNextHint.remaining decrements correctly with each call

    feedback.test.cjs:
    - loadFeedback returns default structure when file missing
    - recordEvent creates project entry and appends event with ISO timestamp
    - recordEvent sets startedAt on project_started event (only first time)
    - recordEvent sets completedAt on project_completed event
    - Multiple events accumulate in the events array
    - saveFeedback creates directory recursively if missing
  </behavior>
  <action>
    RED phase: Create all three test files with the behaviors above. Use temp directories (fs.mkdtempSync) for isolation -- same pattern as existing learn/tests/*.test.cjs. For verifier tests, create temp files with known content to test regex matching. Run tests -- all must FAIL.

    GREEN phase: Implement the three modules following the patterns from 03-RESEARCH.md:

    learn/lib/verifier.cjs:
    - verifyArtifact(filePath, checks) -- checks is Array<{pattern: RegExp, description: string}>. Returns { passed, results }.
    - runVerification(cwd, specPath) -- loads spec JSON, converts string patterns to RegExp, calls verifyArtifact per artifact. Resolves artifact paths relative to cwd. Returns { passed, artifacts: [{ description, ...verifyResult }] }.

    learn/lib/hints.cjs:
    - getNextHint(hints, hintsUsed) -- hints is string[], hintsUsed is number. Returns { hint: string|null, hintsUsed: number, remaining: number }.

    learn/lib/feedback.cjs:
    - FEEDBACK_PATH = path.join('.planning', 'learn', 'feedback.json')
    - loadFeedback(cwd) -- returns { version: 1, projects: {} } on missing/invalid file.
    - saveFeedback(cwd, feedback) -- mkdirSync recursive, writeFileSync.
    - recordEvent(cwd, projectId, eventType, data) -- appends to projects[projectId].events. Sets startedAt on first project_started. Sets completedAt on project_completed.

    All modules: 'use strict', CommonJS, zero external deps, follow existing conventions (2-space indent, single quotes, semicolons, JSDoc on exported functions).

    Run tests -- all must PASS.
  </action>
  <verify>
    <automated>node --test learn/tests/verifier.test.cjs learn/tests/hints.test.cjs learn/tests/feedback.test.cjs</automated>
  </verify>
  <done>All verifier, hints, and feedback tests pass. Modules export documented APIs.</done>
</task>

<task type="auto">
  <name>Task 2: Create mini-project content files</name>
  <files>learn/content/modules/command-lifecycle/lessons/06-mini-project.json, learn/content/modules/command-lifecycle/project/spec.json, learn/content/modules/command-lifecycle/project/hints.json</files>
  <action>
    Create three content files:

    1. learn/content/modules/command-lifecycle/lessons/06-mini-project.json:
    Follow the existing lesson JSON schema (id, title, lessonNumber, objective, content, conceptMap, successCriteria). Add a new content section type "project" with fields: task (string describing what to build), deliverables (array of strings), verifyCommand ("node learn/bin/gsd-learn.cjs --verify"), hintCommand ("node learn/bin/gsd-learn.cjs --hint"). Include 1-2 "text" sections before the project section that set context (you traced the lifecycle, now build something). Set lessonNumber to 6, conceptMap to "mini-project". The task: "Add a new 'echo' command to GSD that takes a message argument and outputs it using the standard GSD output pattern." Deliverables: a handler function in a lib/*.cjs file, a new case in gsd-tools.cjs switch, output following GSD JSON/raw dual-output pattern.

    2. learn/content/modules/command-lifecycle/project/spec.json:
    Define verification spec with id "command-lifecycle-project", moduleId "command-lifecycle", title, description. The artifacts array has 2 entries:
      - echo.cjs file: path "get-shit-done/bin/lib/echo.cjs", checks: module.exports pattern, function with cmd prefix pattern
      - gsd-tools.cjs modification: path "get-shit-done/bin/gsd-tools.cjs", checks: case 'echo' or case "echo" pattern
    Keep checks broad -- 2-3 per artifact max. Use string patterns (not RegExp literals) since this is JSON. The verifier will convert to RegExp.

    3. learn/content/modules/command-lifecycle/project/hints.json:
    Array of 5-6 strings, escalating from vague to specific. NO actual code in any hint:
      - Hint 1: Reframe -- "Think about the journey a command takes. You traced it in lessons 1-5. Your echo command follows the same path."
      - Hint 2: Point to lesson -- "Lesson 4 (Tool Modules) showed you exactly how existing commands are structured. Re-read it."
      - Hint 3: Name the file -- "Look at get-shit-done/bin/lib/ -- each command has its own .cjs file. Your echo command needs one too."
      - Hint 4: Name the pattern -- "Your module needs to export a function that follows the cmd* naming convention. Check how cmdConfigSet or cmdPhasesList are structured."
      - Hint 5: Describe the steps -- "Create lib/echo.cjs with a cmdEcho function that uses process.stdout.write for output. Then add case 'echo': in the switch statement in gsd-tools.cjs to call it."
      (Hint 5 is the most specific but still no copy-paste code)

    Verify the lesson JSON loads correctly by running the existing lessons.test.cjs (it tests that modules load all their lessons).
  </action>
  <verify>
    <automated>node -e "const l = require('./learn/lib/lessons.cjs'); const m = l.loadModule('command-lifecycle', './learn/content'); console.log('Lessons:', m.lessons.length); if (m.lessons.length !== 6) { process.exit(1); } const last = m.lessons[5]; if (!last.content.some(s => s.type === 'project')) { console.error('Missing project section'); process.exit(1); } console.log('OK: 6 lessons, project type present');"</automated>
  </verify>
  <done>06-mini-project.json loads as lesson 6 with project content type. spec.json defines 2 artifacts with structural checks. hints.json has 5-6 progressive hints with no code.</done>
</task>

</tasks>

<verification>
All new test files pass:
```
node --test learn/tests/verifier.test.cjs learn/tests/hints.test.cjs learn/tests/feedback.test.cjs
```

Full existing test suite still passes:
```
node --test learn/tests/*.test.cjs
```

Lesson 06 loads and has correct structure.
</verification>

<success_criteria>
- 3 lib modules (verifier.cjs, hints.cjs, feedback.cjs) with documented exports
- 3 test files with all behaviors verified
- 3 content files (lesson, spec, hints) with correct schemas
- All tests pass, no regressions in existing tests
</success_criteria>

<output>
After completion, create `.planning/phases/03-mini-project-validation/03-01-SUMMARY.md`
</output>
