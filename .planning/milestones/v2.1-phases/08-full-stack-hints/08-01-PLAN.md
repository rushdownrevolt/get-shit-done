---
phase: 08-full-stack-hints
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - learn/content/modules/command-lifecycle/project/hints.json
autonomous: true
requirements: [HNT-01]
must_haves:
  truths:
    - "Hints reference Module 1 markdown work as the starting point (learner knows they are extending, not starting fresh)"
    - "Hints provide progressive guidance for both Node.js artifacts (handler creation, then switch case wiring)"
    - "Hints follow progressive disclosure pattern (general direction first, specific code guidance later)"
  artifacts:
    - path: "learn/content/modules/command-lifecycle/project/hints.json"
      provides: "Progressive hint array for full-stack skeptic mini-project"
      contains: "skeptic"
  key_links:
    - from: "learn/content/modules/command-lifecycle/project/hints.json"
      to: "learn/content/modules/command-lifecycle/project/spec.json"
      via: "Hints guide learner toward the 4 artifacts verified by spec.json"
      pattern: "skeptic|handler|switch|command|workflow"
---

<objective>
Replace the echo-command hints in Module 2's hints.json with progressive guidance for the full-stack skeptic mini-project.

Purpose: Learner needs progressive hints that connect their existing Module 1 markdown-layer work (command.md + workflow.md) to the new Node.js-layer backend work (skeptic.cjs handler + gsd-tools.cjs switch case).
Output: Updated hints.json with 5 progressively specific hints following established pattern.
</objective>

<execution_context>
@C:/Users/18182/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/18182/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-full-stack-hints/08-CONTEXT.md

# Phase 7 output (what lesson 6 and spec.json now contain)
@.planning/phases/07-full-stack-lesson-and-verification/07-01-SUMMARY.md

# The file to modify
@learn/content/modules/command-lifecycle/project/hints.json

# Reference: Module 1 hints pattern to follow
@learn/content/modules/gsd-commands/project/hints.json

# Reference: spec.json artifacts the hints must guide toward
@learn/content/modules/command-lifecycle/project/spec.json

<interfaces>
<!-- Module 1 hints pattern (5-element flat JSON array, progressively more specific): -->
<!-- Hint 1: General conceptual framing ("you already know...") -->
<!-- Hint 2: Lesson references ("re-read Lesson X...") -->
<!-- Hint 3: Directory/path guidance ("files go at...") -->
<!-- Hint 4: Structural detail (fields, sections, patterns) -->
<!-- Hint 5: Near-complete guidance (specific steps, still not full code) -->

<!-- The 4 spec.json artifacts hints must guide toward: -->
<!-- 1. ~/.claude/commands/gsd/skeptic.md (command spec - Module 1, already done) -->
<!-- 2. ~/.claude/get-shit-done/workflows/skeptic.md (workflow - Module 1, already done) -->
<!-- 3. get-shit-done/bin/lib/skeptic.cjs (handler - NEW, Node.js layer) -->
<!-- 4. get-shit-done/bin/gsd-tools.cjs switch case (wiring - NEW, Node.js layer) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite hints.json with full-stack skeptic guidance</name>
  <files>learn/content/modules/command-lifecycle/project/hints.json</files>
  <action>
Replace the entire contents of hints.json with a new 5-element JSON array of progressive hints for the full-stack skeptic mini-project. Follow the established progressive disclosure pattern from both existing hints.json files.

Key constraints:
- Hint 1 (orientation): Frame this as an extension of Module 1 work. The learner already built the markdown layer (command.md + workflow.md) in Module 1. Now they need to add the Node.js backend. Reference their Module 1 output as the foundation.
- Hint 2 (lesson references): Point to specific lessons in Module 2 that cover the Node.js layer. Lessons 4 (Tool Modules) and 5 (State and Configuration) showed handler patterns and the switch dispatch.
- Hint 3 (directory/path guidance): Point to get-shit-done/bin/lib/ for handler modules and get-shit-done/bin/gsd-tools.cjs for the switch statement. The learner should look at existing .cjs files for patterns.
- Hint 4 (structural detail): Describe what the handler module needs: a cmd* function export following the naming convention, using process.stdout.write or the output() pattern. Then mention the switch case wiring in gsd-tools.cjs.
- Hint 5 (near-complete guidance): Give specific steps -- create lib/skeptic.cjs with a cmdSkeptic function that exports via module.exports, then add case 'skeptic' in gsd-tools.cjs switch to call it. Still stop short of giving complete code.

Important: The first two artifacts (command spec + workflow) are ALREADY DONE from Module 1. Hints should acknowledge this and focus guidance on the two NEW artifacts (handler + switch case). Do NOT re-guide the learner through markdown artifacts they already created.

Format: Flat JSON array of 5 strings, matching the exact structure of both existing hints.json files. Each hint is a single string, no nested objects.
  </action>
  <verify>
    <automated>node -e "const h=require('./learn/content/modules/command-lifecycle/project/hints.json'); if(!Array.isArray(h)||h.length!==5)throw 'Not 5-element array'; if(!h[0].toLowerCase().includes('module 1')&&!h[0].toLowerCase().includes('already'))throw 'Hint 1 missing Module 1 reference'; if(h.some(s=>s.includes('echo')))throw 'Still references echo'; if(!h.some(s=>s.includes('skeptic')))throw 'Missing skeptic reference'; console.log('All checks passed')"</automated>
  </verify>
  <done>hints.json contains 5 progressive hints that: (1) reference Module 1 as starting point, (2) guide toward handler creation and switch case wiring, (3) follow progressive disclosure pattern, (4) contain no echo references</done>
</task>

</tasks>

<verification>
1. hints.json is valid JSON (5-element array of strings)
2. No "echo" references remain
3. Hint 1 acknowledges Module 1 markdown work as already complete
4. Later hints guide toward skeptic.cjs handler and gsd-tools.cjs switch case
5. Progressive disclosure pattern maintained (general to specific)
</verification>

<success_criteria>
- hints.json updated with full-stack skeptic guidance
- Hints reference Module 1 output as starting point
- Hints provide progressive guidance for both Node.js artifacts
- HNT-01 requirement satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/08-full-stack-hints/08-01-SUMMARY.md`
</output>
