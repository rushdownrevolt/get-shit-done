---
phase: 07-full-stack-lesson-and-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - learn/content/modules/command-lifecycle/lessons/06-mini-project.json
  - learn/content/modules/command-lifecycle/project/spec.json
autonomous: true
requirements: [MP-01, MP-02, VER-01, VER-02]

must_haves:
  truths:
    - "Lesson 6 tells the learner they are extending their Module 1 skeptic command, not building a standalone echo command"
    - "Lesson 6 deliverables list all 4 layers: command.md, workflow.md, lib/skeptic.cjs, gsd-tools.cjs switch case"
    - "spec.json verifies both Node.js artifacts (skeptic.cjs handler + switch case) and both markdown artifacts (command.md + workflow.md)"
    - "Running the verifier against a complete skeptic implementation would pass all checks"
  artifacts:
    - path: "learn/content/modules/command-lifecycle/lessons/06-mini-project.json"
      provides: "Updated lesson content referencing skeptic instead of echo"
      contains: "skeptic"
    - path: "learn/content/modules/command-lifecycle/project/spec.json"
      provides: "Full-stack verification checks for all 4 skeptic layers"
      contains: "skeptic"
  key_links:
    - from: "learn/content/modules/command-lifecycle/project/spec.json"
      to: "learn/content/modules/gsd-commands/project/spec.json"
      via: "Shared markdown artifact paths (command.md and workflow.md checks carried forward)"
      pattern: "skeptic\\.md"
---

<objective>
Update Module 2 mini-project from a standalone "echo" command to a full-stack "/gsd:skeptic" command that extends Module 1's markdown-layer work with the Node.js backend layer.

Purpose: The learner should experience building ALL 4 layers of a GSD command in sequence across both modules, reinforcing the full command lifecycle.
Output: Updated lesson 6 JSON and spec.json with full-stack skeptic content and verification.
</objective>

<execution_context>
@C:/Users/18182/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/18182/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

Source files to modify:
@learn/content/modules/command-lifecycle/lessons/06-mini-project.json
@learn/content/modules/command-lifecycle/project/spec.json

Module 1 spec for reference (carry forward markdown checks):
@learn/content/modules/gsd-commands/project/spec.json

<interfaces>
<!-- Module 1 spec.json artifact paths the learner already created (these must appear in Module 2 spec too) -->
From learn/content/modules/gsd-commands/project/spec.json:
- path: "~/.claude/commands/gsd/skeptic.md" (command spec with frontmatter, objective, execution_context, context, process)
- path: "~/.claude/get-shit-done/workflows/skeptic.md" (workflow with purpose and process sections)

<!-- Existing spec.json format pattern -->
Each artifact: { "description": string, "path": string, "checks": [{ "pattern": regex_string, "description": string }] }

<!-- Existing lesson JSON content block types -->
- { "type": "text", "value": string, "focus": string, "bridge": string }
- { "type": "project", "task": string, "deliverables": string[], "verifyCommand": string, "hintCommand": string, "focus": string, "bridge": string }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update lesson 6 content from echo to full-stack skeptic</name>
  <files>learn/content/modules/command-lifecycle/lessons/06-mini-project.json</files>
  <action>
Read the full 06-mini-project.json file. Update ALL content to replace the echo command with the full-stack skeptic command:

1. Update top-level fields:
   - "title": "Mini-Project: Build a Full-Stack GSD Command"
   - "objective": "Extend your Module 1 skeptic command with the Node.js backend — completing all 4 layers of a GSD command"

2. Update content[0] (intro text block):
   - Replace the opening paragraph. New framing: "In Module 1, you built the markdown layer of /gsd:skeptic -- a command spec and workflow file. But those files are just the prompt side. To make a command that GSD can actually dispatch, you need the Node.js backend: a handler function and a switch case in the router. That is what you will build now."
   - Keep the hint/verify guidance paragraph but update framing to reference extending existing work.

3. Update content[1] (verify/hint instructions):
   - Keep as-is (verify and hint commands are the same). Only update if it references "echo".

4. Update content[2] (project block):
   - "task": "Extend your /gsd:skeptic command with a Node.js handler and switch case, completing all 4 layers of the GSD command lifecycle."
   - "deliverables" array with ALL 4 items:
     - "A command spec at ~/.claude/commands/gsd/skeptic.md (already built in Module 1 -- verify it is still there)"
     - "A workflow file at ~/.claude/get-shit-done/workflows/skeptic.md (already built in Module 1 -- verify it is still there)"
     - "A handler function in get-shit-done/bin/lib/skeptic.cjs following the cmd* naming convention"
     - "A new 'skeptic' case in the gsd-tools.cjs switch statement that routes to your handler"
   - Keep verifyCommand and hintCommand unchanged.

5. Update conceptMap if present — change echo references to skeptic.

6. Update successCriteria if present — reference all 4 layers passing verification.

Keep all JSON structure valid. Preserve lessonNumber: 6, id: "mini-project".
  </action>
  <verify>
    <automated>node -e "const j=require('./learn/content/modules/command-lifecycle/lessons/06-mini-project.json'); const s=JSON.stringify(j); if(s.includes('echo')) throw 'Still contains echo'; if(!s.includes('skeptic')) throw 'Missing skeptic'; if(j.content.find(c=>c.type==='project').deliverables.length!==4) throw 'Need 4 deliverables'; console.log('OK: lesson updated')"</automated>
  </verify>
  <done>Lesson 6 references skeptic (not echo), frames work as extending Module 1, lists all 4 deliverables</done>
</task>

<task type="auto">
  <name>Task 2: Update spec.json with full-stack skeptic verification checks</name>
  <files>learn/content/modules/command-lifecycle/project/spec.json</files>
  <action>
Rewrite spec.json to verify all 4 layers of the skeptic command. The new spec must have 4 artifacts:

1. Update top-level fields:
   - "title": "Build /gsd:skeptic (Full-Stack)"
   - "description": "Extend your skeptic command with a Node.js handler and switch case, completing all 4 GSD command layers"

2. Replace the artifacts array with these 4 entries:

   Artifact 1 — Skeptic command spec (from Module 1, carried forward):
   - "description": "Skeptic command spec (Module 1)"
   - "path": "~/.claude/commands/gsd/skeptic.md"
   - checks: same as Module 1 spec.json (frontmatter block, name field, description field, allowed-tools, objective, execution_context, @workflows/skeptic.md reference, context, process)

   Artifact 2 — Skeptic workflow (from Module 1, carried forward):
   - "description": "Skeptic workflow (Module 1)"
   - "path": "~/.claude/get-shit-done/workflows/skeptic.md"
   - checks: same as Module 1 spec.json (purpose section, process section)

   Artifact 3 — Skeptic handler module (NEW for Module 2):
   - "description": "Skeptic handler module"
   - "path": "get-shit-done/bin/lib/skeptic.cjs"
   - checks:
     - { "pattern": "module\\.exports", "description": "File exports a module" }
     - { "pattern": "function\\s+cmd", "description": "Follows the cmd* function naming convention" }
     - { "pattern": "process\\.stdout\\.write|output\\(", "description": "Uses GSD output pattern" }

   Artifact 4 — Switch case in router (NEW for Module 2):
   - "description": "Switch case for skeptic command"
   - "path": "get-shit-done/bin/gsd-tools.cjs"
   - checks:
     - { "pattern": "case\\s+['\"]skeptic['\"]", "description": "Skeptic case exists in switch statement" }

Keep moduleId: "command-lifecycle", id: "command-lifecycle-project".
  </action>
  <verify>
    <automated>node -e "const j=require('./learn/content/modules/command-lifecycle/project/spec.json'); if(j.artifacts.length!==4) throw 'Need 4 artifacts, got '+j.artifacts.length; if(j.artifacts.some(a=>JSON.stringify(a).includes('echo'))) throw 'Still contains echo'; const paths=j.artifacts.map(a=>a.path); if(!paths.some(p=>p.includes('skeptic.md')&&p.includes('commands'))) throw 'Missing command.md check'; if(!paths.some(p=>p.includes('skeptic.md')&&p.includes('workflows'))) throw 'Missing workflow.md check'; if(!paths.some(p=>p.includes('skeptic.cjs'))) throw 'Missing handler check'; if(!paths.some(p=>p.includes('gsd-tools'))) throw 'Missing switch case check'; console.log('OK: spec has all 4 artifacts')"</automated>
  </verify>
  <done>spec.json has 4 artifacts covering both markdown layers (command.md, workflow.md) and both Node.js layers (skeptic.cjs handler, gsd-tools.cjs switch case), with no echo references remaining</done>
</task>

</tasks>

<verification>
1. No "echo" references remain in either modified file
2. Lesson 6 has exactly 4 deliverables covering all layers
3. spec.json has exactly 4 artifacts with appropriate regex checks
4. Both files are valid JSON
</verification>

<success_criteria>
- Module 2 mini-project lesson frames work as extending Module 1's skeptic command
- All 4 GSD command layers represented in both deliverables and verification
- Verifier can structurally check all 4 artifacts of a complete skeptic implementation
</success_criteria>

<output>
After completion, create `.planning/phases/07-full-stack-lesson-and-verification/07-01-SUMMARY.md`
</output>
