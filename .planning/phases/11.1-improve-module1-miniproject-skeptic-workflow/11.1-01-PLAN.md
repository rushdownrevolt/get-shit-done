---
phase: 11.1-improve-module1-miniproject-skeptic-workflow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - learn/content/modules/gsd-commands/lessons/06-mini-project.json
  - learn/content/modules/gsd-commands/project/hints.json
autonomous: false
requirements: []

must_haves:
  truths:
    - "Lesson 6 shows the real GSD skeptic workflow as a starting template"
    - "Learner can press C to copy lesson content containing the workflow template"
    - "Lesson guides learner to customize the workflow and make it their own"
    - "Learner still writes command spec (skeptic.md) from scratch"
    - "Hints reflect template-based approach, not build-from-scratch"
    - "Verification checks remain minimal (purpose + process only)"
  artifacts:
    - path: "learn/content/modules/gsd-commands/lessons/06-mini-project.json"
      provides: "Rewritten mini-project lesson with workflow template"
      contains: "skeptic workflow"
    - path: "learn/content/modules/gsd-commands/project/hints.json"
      provides: "Updated hints for template-based approach"
  key_links:
    - from: "learn/content/modules/gsd-commands/lessons/06-mini-project.json"
      to: "learn/lib/clipboard-formatter.cjs"
      via: "C key copies lesson content including embedded workflow template"
      pattern: "formatLessonForClipboard"
    - from: "learn/content/modules/gsd-commands/project/spec.json"
      to: "learn/content/modules/gsd-commands/lessons/06-mini-project.json"
      via: "spec.json checks remain unchanged -- only purpose and process required"
      pattern: "<purpose>|<process>"
---

<objective>
Rewrite the Module 1 mini-project lesson to provide the real GSD skeptic workflow as a starting template, delivered via clipboard copy. Shift the pedagogical model from "figure out the structure from scratch" to "learn from a working example, then customize it creatively." Update hints to match the template-based approach.

Purpose: The skeptic review surfaced that the mini-project should deliver a working tool, not test recall. Giving learners the real workflow as a template makes the project about creative customization rather than structural guesswork.
Output: Updated 06-mini-project.json and hints.json
</objective>

<execution_context>
@C:/Users/18182/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/18182/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11.1-improve-module1-miniproject-skeptic-workflow/11.1-CONTEXT.md

@learn/content/modules/gsd-commands/lessons/06-mini-project.json
@learn/content/modules/gsd-commands/project/hints.json
@learn/content/modules/gsd-commands/project/spec.json
@learn/lib/clipboard-formatter.cjs
@learn/lib/navigator.cjs

<interfaces>
<!-- Clipboard copy: pressing C during lesson copies full lesson via formatLessonForClipboard() -->
<!-- Content types: "text" (plain prose), "code" (fenced block), "project" (task+deliverables) -->
<!-- Progressive display: content items are grouped and shown one at a time via w key -->
<!-- Hints: flat JSON array of 5 strings, progressively more specific -->

From learn/lib/clipboard-formatter.cjs:
- "text" blocks: rendered as plain paragraph
- "code" blocks: rendered as ```javascript fenced code (with optional source file annotation)
- "project" blocks: rendered as "## Your Mission" with numbered deliverables

From learn/lib/navigator.cjs:
- C key triggers 'copy' action -> formatLessonForClipboard(lesson, currentIndex, totalLessons) -> copyToClipboard(markdown)
- Copies FULL lesson content, not individual blocks
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite mini-project lesson with workflow template</name>
  <files>learn/content/modules/gsd-commands/lessons/06-mini-project.json</files>
  <action>
Rewrite 06-mini-project.json to restructure the lesson flow around the template-based approach. The lesson must have these content blocks in order:

1. **Opening text block** -- Set the scene: "You have traced the two-layer architecture... Now you build something real." Introduce the approach: instead of building from scratch, you get to start with the REAL skeptic workflow the course author actually uses. You will customize it to make it your own. The command spec is still yours to write from scratch.

2. **Text block with verify/hint instructions** -- Same as current: explain --verify and --hint commands, mention files can be deleted and restarted.

3. **Text block introducing the workflow template** -- Explain: "Here is the actual skeptic workflow from this GSD installation. Press C to copy this page, then paste the workflow content into your file at ~/.claude/get-shit-done/workflows/skeptic.md. This is your starting point -- read it, understand it, then make it your own."

4. **Code block containing the FULL real skeptic workflow** -- Use type "code" (NOT "text") so it renders as a fenced code block. The value should be the COMPLETE content of the real skeptic workflow file at ~/.claude/get-shit-done/workflows/skeptic.md (the file has ~90 lines with purpose, process with 6 steps, and success_criteria sections). Do NOT use source field since this is not a JavaScript file. Note: clipboard-formatter wraps code blocks in ```javascript -- this is acceptable since the content is markdown but will still be readable and usable when pasted.

5. **Text block with customization encouragement** -- "Make it your own. Rewrite the critique logic. Add steps, remove steps, reorder the process. Change the voice. Add your own success criteria. The only structural requirement is a purpose section and a process section -- everything else is your creative decision. Verification checks structure, not content."

6. **Project block** -- Update the task description to reflect template approach: "Build /gsd:skeptic -- a command that takes a phase number argument and critiques that phase. You already have the workflow template -- customize it and create the command spec from scratch. Create two files: command spec at ~/.claude/commands/gsd/skeptic.md and workflow at ~/.claude/get-shit-done/workflows/skeptic.md." Keep the same deliverables array (3 items: command spec structure, workflow with purpose+process, cross-file @file wiring). Keep same verifyCommand and hintCommand.

7. **Closing text block** -- Same as current: "Going Further" auto-detect enhancement, module completion message.

Preserve ALL existing JSON structure fields: id, title, lessonNumber, objective, conceptMap, successCriteria. Update objective to mention the template approach: "Apply everything from Lessons 1-5 to build a real GSD command, starting from a real workflow template".

IMPORTANT: The code block (#4) must contain the ACTUAL content of the skeptic workflow. Read it from ~/.claude/get-shit-done/workflows/skeptic.md. This is the real content the user wants learners to see -- not a synthetic example.

IMPORTANT: Do NOT modify spec.json -- verification checks stay at purpose + process only per user decision.
  </action>
  <verify>
    <automated>node -e "const l=require('./learn/content/modules/gsd-commands/lessons/06-mini-project.json'); const ok=l.content.some(c=>c.type==='code'&&c.value.includes('<purpose>')); const proj=l.content.some(c=>c.type==='project'); console.log('has_template:',ok,'has_project:',proj); if(!ok||!proj) process.exit(1)"</automated>
  </verify>
  <done>Lesson 6 contains the real skeptic workflow as a code block, has a project block with updated task description, and guides learner through template-customize-create flow</done>
</task>

<task type="auto">
  <name>Task 2: Update hints for template-based approach</name>
  <files>learn/content/modules/gsd-commands/project/hints.json</files>
  <action>
Rewrite hints.json to reflect the template-based approach. The current hints assume building from scratch. New hints should assume the learner has the workflow template and needs to:
- Customize the workflow (making it their own)
- Write the command spec from scratch
- Wire the two files together

5 progressive hints, from conceptual to specific:

1. Conceptual -- "You have the workflow template already. Your two tasks are: customize it to reflect your own critique approach, and create the command spec that points to it. The spec is the part you write from scratch -- revisit Lessons 2 and 3 for the anatomy of each."

2. Direction -- "Start with the command spec. It needs YAML frontmatter (name, description, allowed-tools) and XML sections. The execution_context section is where you wire to the workflow. The workflow file already exists from the template -- focus on making the critique logic yours."

3. Structural -- "The command spec goes at ~/.claude/commands/gsd/skeptic.md. It needs frontmatter with name: gsd:skeptic, a description, and allowed-tools. Then four XML sections: objective, execution_context (with the @file reference to your workflow), context, and process."

4. Nearly complete -- "For the command spec frontmatter: name: gsd:skeptic, description of what it does, and allowed-tools listing the tools Claude needs. In execution_context, add @~/.claude/get-shit-done/workflows/skeptic.md to wire the two layers. For the workflow, the template has purpose and process already -- your job is to make the critique steps genuinely yours."

5. Maximum specificity -- "Create ~/.claude/commands/gsd/skeptic.md with YAML frontmatter (name: gsd:skeptic, description, allowed-tools) and sections: objective (what skeptic does), execution_context (containing @~/.claude/get-shit-done/workflows/skeptic.md), context (what files to read), process (high-level steps). Your workflow at ~/.claude/get-shit-done/workflows/skeptic.md already has the template structure -- customize the steps, the voice, and the success criteria to reflect your personal approach to critique."
  </action>
  <verify>
    <automated>node -e "const h=require('./learn/content/modules/gsd-commands/project/hints.json'); console.log('count:',h.length,'has_template_ref:',h.some(s=>s.includes('template'))); if(h.length!==5||!h.some(s=>s.includes('template'))) process.exit(1)"</automated>
  </verify>
  <done>All 5 hints reference the template-based approach, progress from conceptual to specific, and guide the learner to customize the workflow while writing the command spec from scratch</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify mini-project lesson flow</name>
  <action>
Rewritten Module 1 mini-project lesson with real skeptic workflow template embedded as a copyable code block, plus updated hints for the template-based approach
  
    1. Run: node learn/bin/gsd-learn.cjs
    2. Select Module 1 (GSD Commands)
    3. Navigate to Lesson 6 (the mini-project)
    4. Verify the lesson introduces the template approach and shows the real skeptic workflow
    5. Press C to copy -- paste somewhere and confirm the workflow content is included
    6. Press W through all parts to confirm flow makes sense
    7. Run: node learn/bin/gsd-learn.cjs --hint (from mini-project context) -- confirm hints reference template approach
    8. Run: node learn/bin/gsd-learn.cjs --verify -- confirm existing checks still work (if you have the files) or fail gracefully (if you do not)
  </action>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Lesson 6 JSON is valid and parseable
- Lesson contains the real skeptic workflow content (not a placeholder)
- Lesson has a project block with deliverables and verify/hint commands
- Hints array has exactly 5 items referencing template approach
- spec.json is UNCHANGED (minimal checks preserved)
- Module 2 spec.json is UNCHANGED
- C key clipboard copy includes the workflow template content
</verification>

<success_criteria>
- Module 1 mini-project lesson shows the real skeptic workflow as a starting template
- Learner can copy the workflow via C key and paste into their file
- Lesson encourages open-ended creative customization
- Command spec remains a from-scratch challenge
- Hints reflect template-based approach
- Verification checks unchanged (purpose + process only)
</success_criteria>

<output>
After completion, create `.planning/phases/11.1-improve-module1-miniproject-skeptic-workflow/11.1-01-SUMMARY.md`
</output>
