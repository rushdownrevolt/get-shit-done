---
phase: 28-lesson-content-lessons-4-7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - learn/content/modules/gsd2-agent-application/lessons/04-auto-mode.json
autonomous: true
requirements: [LESS-04]

must_haves:
  truths:
    - "Lesson 4 teaches the auto loop cycle: derive state, resolve dispatch, execute unit, commit, repeat"
    - "Lesson 4 explains crash recovery via the lock file mechanism"
    - "Lesson 4 shows stuck detection with graduated recovery (artifact check at 3, hard stop at 5)"
    - "Lesson 4 has 8 blocks (5 text, 3 code) with consistent pacing"
  artifacts:
    - path: "learn/content/modules/gsd2-agent-application/lessons/04-auto-mode.json"
      provides: "Complete Lesson 4 content with real GSD-2 source snippets"
      contains: "autoLoop"
  key_links:
    - from: "learn/content/modules/gsd2-agent-application/lessons/04-auto-mode.json"
      to: "module.json sectionMap"
      via: "conceptMap: auto-mode"
      pattern: "\"conceptMap\": \"auto-mode\""
---

<objective>
Write the complete content for Module 6 Lesson 4: "Auto Mode" -- teaching the auto loop, crash recovery, stuck detection, and timeout supervision with real GSD-2 source code snippets.

Purpose: Auto mode is the heart of GSD-2's autonomous execution. Learners need to understand the loop structure, how it recovers from crashes, and how it detects when it's stuck.
Output: Complete lesson JSON replacing the skeleton placeholder.
</objective>

<execution_context>
@C:/Users/18182/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/18182/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Lesson 4 -- Auto Mode</name>
  <files>learn/content/modules/gsd2-agent-application/lessons/04-auto-mode.json</files>
  <read_first>
    - learn/content/modules/gsd2-agent-application/lessons/01-overview.json (reference lesson format and pacing)
    - learn/content/modules/gsd2-agent-application/module.json (sectionMap verification)
    - learn/content/modules/gsd2-agent-application/lessons/04-auto-mode.json (current skeleton)
  </read_first>
  <action>
Replace the skeleton content in 04-auto-mode.json with the following complete lesson JSON:

```json
{
  "id": "auto-mode",
  "title": "Auto Mode",
  "lessonNumber": 4,
  "objective": "Understand the auto loop, crash recovery, stuck detection, and timeout supervision that enable hands-free execution.",
  "content": [
    {
      "type": "text",
      "value": "Auto mode is the engine that makes GSD-2 autonomous. You run /gsd auto, and the system takes over -- deriving state, dispatching agents, committing work, and advancing through milestones without human intervention. The core is a while loop in auto-loop.ts. Each iteration follows the same five-phase sequence: pre-dispatch checks, guard evaluation, dispatch resolution, unit execution, and finalization. The loop exits when all milestones are complete, a blocker is hit, or a safety limit triggers. There is no scheduler, no job queue, no event bus. It is a linear loop that re-derives everything from disk on every iteration.",
      "focus": "Auto mode is a while loop that re-derives state from disk on every iteration.",
      "bridge": "Let's see the actual loop structure with its safety boundaries."
    },
    {
      "type": "code",
      "language": "typescript",
      "value": "const MAX_LOOP_ITERATIONS = 500;\n\nexport async function autoLoop(\n  ctx: ExtensionContext,\n  pi: ExtensionAPI,\n  s: AutoSession,\n  deps: LoopDeps,\n): Promise<void> {\n  let iteration = 0;\n  let lastDerivedUnit = \"\";\n  let sameUnitCount = 0;\n  let consecutiveErrors = 0;\n\n  while (s.active) {\n    iteration++;\n\n    if (iteration > MAX_LOOP_ITERATIONS) {\n      await deps.stopAuto(ctx, pi,\n        `Safety: loop exceeded ${MAX_LOOP_ITERATIONS} iterations`);\n      break;\n    }\n\n    try {\n      // Phase 1: Pre-dispatch (health gate, worktree sync)\n      // Phase 2: Guards (budget, context window, secrets)\n      // Phase 3: Dispatch resolution (resolveDispatch)\n      // Phase 4: Unit execution (runUnit)\n      // Phase 5: Finalize (closeout, verification, post-unit)\n      consecutiveErrors = 0;\n    } catch (loopErr) {\n      consecutiveErrors++;\n      if (consecutiveErrors >= 3) {\n        await deps.stopAuto(ctx, pi,\n          `${consecutiveErrors} consecutive iteration failures`);\n        break;\n      }\n      deps.invalidateAllCaches();\n    }\n  }\n}",
      "highlight": [1, 3, 14, 17, 31, 33],
      "explanation": "This is the autoLoop function from auto-loop.ts. The MAX_LOOP_ITERATIONS cap of 500 prevents runaway execution -- a milestone with 20 slices x 5 tasks x 3 phases is roughly 300 units, so 500 gives generous headroom. The blanket try/catch ensures one bad iteration cannot kill the entire session. After 3 consecutive errors, it hard-stops. After 1-2 errors, it invalidates caches and retries."
    },
    {
      "type": "text",
      "value": "The loop's error handling is graduated: one error gets a retry, two errors trigger cache invalidation, three consecutive errors cause a hard stop. This matters because transient failures happen -- a network blip, a file lock, a timeout. You do not want a single transient failure to kill an 8-hour autonomous session. But you also do not want to retry forever when something is fundamentally broken. The graduated approach balances resilience with fail-fast behavior.",
      "focus": "Graduated error handling: retry once, invalidate caches on second, hard stop on third.",
      "bridge": "What happens when the process itself crashes -- not an error, but a complete process death?"
    },
    {
      "type": "code",
      "language": "typescript",
      "value": "export interface LockData {\n  pid: number;\n  startedAt: string;\n  unitType: string;\n  unitId: string;\n  unitStartedAt: string;\n  completedUnits: number;\n  sessionFile?: string;\n}\n\nexport function writeLock(\n  basePath: string, unitType: string, unitId: string,\n  completedUnits: number, sessionFile?: string,\n): void {\n  const data: LockData = {\n    pid: process.pid,\n    startedAt: new Date().toISOString(),\n    unitType, unitId,\n    unitStartedAt: new Date().toISOString(),\n    completedUnits, sessionFile,\n  };\n  atomicWriteSync(lockPath(basePath), JSON.stringify(data, null, 2));\n}\n\nexport function clearLock(basePath: string): void {\n  const p = lockPath(basePath);\n  if (existsSync(p)) unlinkSync(p);\n}",
      "highlight": [1, 11, 25],
      "explanation": "This is the crash recovery system from crash-recovery.ts. The lock file (.gsd/auto.lock) is written on auto-start and updated on each unit dispatch. On clean stop, clearLock deletes it. If the lock file exists on next startup, the previous session crashed mid-unit. The lock records the PID, the unit that was in progress, and the session file path -- so crash recovery can read the surviving JSONL transcript and build a recovery briefing for the next agent."
    },
    {
      "type": "text",
      "value": "Crash recovery is elegant because the session file survives the crash. The pi coding agent writes tool calls and responses to a JSONL file incrementally using appendFileSync. When the process dies, the file on disk contains every tool call up to the crash point. On restart, GSD-2 checks if the lock file exists, reads the session JSONL, and builds a recovery briefing that tells the next agent what the crashed session was doing, what files it had read, and what changes it had made. The fresh agent picks up where the crashed one left off -- not from the beginning of the task, but from the last known good state.",
      "focus": "The session JSONL survives crashes -- recovery reads it to build a briefing for the next agent.",
      "bridge": "Crashes are rare. A much more common problem is getting stuck on the same unit."
    },
    {
      "type": "code",
      "language": "typescript",
      "value": "// Same-unit stuck counter with graduated recovery\nconst derivedKey = `${unitType}/${unitId}`;\nif (derivedKey === lastDerivedUnit && !s.pendingVerificationRetry) {\n  sameUnitCount++;\n\n  if (sameUnitCount === 3) {\n    // Level 1: try verifying the artifact\n    const artifactExists = deps.verifyExpectedArtifact(\n      unitType, unitId, s.basePath);\n    if (artifactExists) {\n      deps.invalidateAllCaches();\n      continue;\n    }\n    ctx.ui.notify(\n      `Stuck on ${unitType} ${unitId} (attempt ${sameUnitCount}).`,\n      \"warning\");\n    deps.invalidateAllCaches();\n  } else if (sameUnitCount === 5) {\n    // Level 2: hard stop -- genuinely stuck\n    await deps.stopAuto(ctx, pi,\n      `Stuck: ${unitType} ${unitId} derived ${sameUnitCount} times`);\n    break;\n  }\n} else {\n  lastDerivedUnit = derivedKey;\n  sameUnitCount = 0;\n}",
      "highlight": [3, 6, 18],
      "explanation": "This is the stuck detection from auto-loop.ts. When deriveState returns the same unit type and ID on consecutive iterations, the counter increments. At 3 repetitions (Level 1), it checks whether the artifact actually exists on disk -- maybe the agent wrote it but the cache is stale. If found, it invalidates caches and continues. At 5 repetitions (Level 2), it hard-stops. This graduated approach avoids false positives from cache staleness while catching genuine stuck loops."
    },
    {
      "type": "text",
      "value": "Stuck detection solves a real problem: the agent writes a file, but deriveState does not see it because of caching. The result is an infinite loop where the system keeps dispatching the same unit. Level 1 catches this common case by checking the disk directly, bypassing the cache. Level 2 catches the harder case where the agent genuinely fails to produce the required artifact after multiple attempts. The combination of stuck detection, graduated errors, and crash recovery creates a system that can run for hours without human intervention -- surviving transient failures, process crashes, and agent misbehavior.",
      "focus": "Stuck detection catches both cache staleness and genuine agent failures.",
      "bridge": "The supervisor watches over all of this from the outside."
    },
    {
      "type": "text",
      "value": "The auto-supervisor module handles the outermost layer of protection: SIGTERM handling and working-tree activity detection. When the operating system sends SIGTERM (container shutdown, user kill), the handler clears the lock file so the next startup does not incorrectly trigger crash recovery. Working-tree activity detection uses git to check for uncommitted changes -- if the agent has not signaled progress through runtime records but has modified files on disk, it is still working. This prevents the timeout supervisor from killing an agent that is actively writing code but has not yet reported back. Together, the auto loop, crash recovery, stuck detection, and supervisor form a layered defense: the loop handles iteration errors, crash recovery handles process death, stuck detection handles agent failures, and the supervisor handles external signals and silent progress.",
      "focus": "Four layers of defense: loop errors, crash recovery, stuck detection, and supervision.",
      "bridge": "Next lesson: how GSD-2 uses git branches and worktrees to isolate work safely."
    }
  ],
  "conceptMap": "auto-mode",
  "successCriteria": "You can describe the auto loop's five-phase iteration cycle, explain crash recovery via the lock file and session JSONL, trace the graduated stuck detection (Level 1 artifact check at 3, Level 2 hard stop at 5), and identify the four layers of defense that enable autonomous execution."
}
```

Write this JSON to the file, replacing the existing skeleton content entirely.
  </action>
  <verify>
    <automated>cd C:/Users/18182/documents/get-shit-done && node -e "const j=require('./learn/content/modules/gsd2-agent-application/lessons/04-auto-mode.json'); const t=j.content.filter(b=>b.type==='text').length; const c=j.content.filter(b=>b.type==='code').length; console.log('blocks:', j.content.length, 'text:', t, 'code:', c); if(j.content.length!==8||t!==5||c!==3) process.exit(1); if(!j.content[1].value.includes('autoLoop')) process.exit(1); console.log('PASS')"</automated>
  </verify>
  <acceptance_criteria>
    - File has exactly 8 content blocks (5 text, 3 code)
    - grep "autoLoop" finds the loop function from auto-loop.ts
    - grep "writeLock" finds the crash recovery from crash-recovery.ts
    - grep "sameUnitCount" finds the stuck detection from auto-loop.ts
    - grep "conceptMap.*auto-mode" confirms sectionMap linkage
    - JSON parses without errors
  </acceptance_criteria>
  <done>Lesson 4 contains 8 blocks with real GSD-2 source snippets covering the auto loop, crash recovery, stuck detection, and supervision layers</done>
</task>

</tasks>

<verification>
- 04-auto-mode.json has exactly 8 blocks (5 text, 3 code)
- Code blocks contain real snippets from auto-loop.ts, crash-recovery.ts
- JSON is valid and parseable
- conceptMap matches sectionMap key "auto-mode"
</verification>

<success_criteria>
Lesson 4 renders in the learning shell with 8 blocks teaching the auto loop, crash recovery, stuck detection, and supervision using real GSD-2 source code.
</success_criteria>

<output>
After completion, create `.planning/phases/28-lesson-content-lessons-4-7/28-01-SUMMARY.md`
</output>
