---
phase: 28-lesson-content-lessons-4-7
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - learn/content/modules/gsd2-agent-application/lessons/05-git-worktrees.json
autonomous: true
requirements: [LESS-05]

must_haves:
  truths:
    - "Lesson 5 teaches branch-per-milestone isolation strategy"
    - "Lesson 5 explains worktree lifecycle: create, enter, work, merge-and-exit"
    - "Lesson 5 shows squash merge and integration branch resolution"
    - "Lesson 5 has 8 blocks (5 text, 3 code) with consistent pacing"
  artifacts:
    - path: "learn/content/modules/gsd2-agent-application/lessons/05-git-worktrees.json"
      provides: "Complete Lesson 5 content with real GSD-2 source snippets"
      contains: "worktree"
  key_links:
    - from: "learn/content/modules/gsd2-agent-application/lessons/05-git-worktrees.json"
      to: "module.json sectionMap"
      via: "conceptMap: git-worktrees"
      pattern: "\"conceptMap\": \"git-worktrees\""
---

<objective>
Write the complete content for Module 6 Lesson 5: "Git & Worktrees" -- teaching branch-per-milestone, squash merge, and worktree isolation patterns with real GSD-2 source code snippets.

Purpose: Git isolation is how GSD-2 keeps autonomous work safe. Learners need to understand why worktrees exist and how the merge lifecycle works.
Output: Complete lesson JSON replacing the skeleton placeholder.
</objective>

<execution_context>
@C:/Users/18182/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/18182/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Lesson 5 -- Git & Worktrees</name>
  <files>learn/content/modules/gsd2-agent-application/lessons/05-git-worktrees.json</files>
  <read_first>
    - learn/content/modules/gsd2-agent-application/lessons/01-overview.json (reference lesson format and pacing)
    - learn/content/modules/gsd2-agent-application/module.json (sectionMap verification)
    - learn/content/modules/gsd2-agent-application/lessons/05-git-worktrees.json (current skeleton)
  </read_first>
  <action>
Replace the skeleton content in 05-git-worktrees.json with the following complete lesson JSON:

```json
{
  "id": "git-worktrees",
  "title": "Git & Worktrees",
  "lessonNumber": 5,
  "objective": "Learn branch-per-milestone, squash merge, and worktree isolation patterns that GSD-2 uses for safe parallel work.",
  "content": [
    {
      "type": "text",
      "value": "An autonomous agent committing code for hours without supervision needs guardrails. If it writes directly to your main branch and introduces a bug in iteration 47, you have 46 good commits tangled with the bad one. GSD-2 solves this with git isolation: each milestone gets its own worktree -- a separate working directory with its own branch, created inside .gsd/worktrees/. The agent works there, commits freely, and when the milestone is complete, the entire history is squash-merged back to your integration branch as a single clean commit. If the milestone fails, you delete the worktree and lose nothing on main.",
      "focus": "Each milestone gets its own worktree -- isolated commits, squash-merged on completion.",
      "bridge": "The isolation strategy is configurable. Let's see the three modes."
    },
    {
      "type": "code",
      "language": "typescript",
      "value": "export interface GitPreferences {\n  auto_push?: boolean;\n  push_branches?: boolean;\n  remote?: string;\n  snapshots?: boolean;\n  commit_docs?: boolean;\n  pre_merge_check?: boolean | string;\n  merge_strategy?: \"squash\" | \"merge\";\n  main_branch?: string;\n  /** Controls auto-mode git isolation strategy.\n   *  - \"worktree\": (default) creates a milestone worktree\n   *  - \"branch\": works in the project root (for submodule-heavy repos)\n   *  - \"none\": no isolation -- commits land on your current branch\n   */\n  isolation?: \"worktree\" | \"branch\" | \"none\";\n  worktree_post_create?: string;\n  auto_pr?: boolean;\n  pr_target_branch?: string;\n}",
      "highlight": [10, 11, 12, 13, 15],
      "explanation": "This is GitPreferences from git-service.ts. The isolation field controls how GSD-2 handles git during auto mode. The default 'worktree' mode creates a full git worktree in .gsd/worktrees/<milestone-id>/. The 'branch' mode works directly in the project root but on a dedicated branch -- useful for repos with submodules that break in worktrees. The 'none' mode disables isolation entirely, committing directly to your current branch."
    },
    {
      "type": "text",
      "value": "The worktree mode is the default because it provides the strongest isolation. A git worktree is a real, separate working directory backed by the same repository. It has its own checked-out branch, its own index, and its own HEAD. You can run your application from the project root (on main) while the agent works in the worktree (on the milestone branch) -- they do not interfere with each other. This is critical for long-running autonomous sessions: the user can continue to use their project while GSD-2 works in the background.",
      "focus": "Worktrees give full isolation -- the user keeps their working directory while the agent works separately.",
      "bridge": "The WorktreeResolver manages the lifecycle: entering, working, and exiting milestones."
    },
    {
      "type": "code",
      "language": "typescript",
      "value": "export class WorktreeResolver {\n  private readonly s: AutoSession;\n  private readonly deps: WorktreeResolverDeps;\n\n  /** Current working path -- may be worktree or project root. */\n  get workPath(): string {\n    return this.s.basePath;\n  }\n\n  /** Original project root -- always the non-worktree path. */\n  get projectRoot(): string {\n    return this.s.originalBasePath || this.s.basePath;\n  }\n\n  enterMilestone(milestoneId: string, ctx: NotifyCtx): void {\n    this.validateMilestoneId(milestoneId);\n    if (!this.deps.shouldUseWorktreeIsolation()) return;\n    // Creates or enters the worktree, calls process.chdir() internally\n    // Updates s.basePath and rebuilds GitService on success\n  }\n\n  mergeAndExit(milestoneId: string, ctx: NotifyCtx): void {\n    // Squash-merge the milestone branch back to integration branch\n    // Teardown the worktree directory\n    // Restore s.basePath to originalBasePath\n  }\n}",
      "highlight": [1, 15, 22],
      "explanation": "The WorktreeResolver from worktree-resolver.ts encapsulates all worktree path state. It tracks both the current working path (which may be inside a worktree) and the original project root. enterMilestone creates or enters a worktree when isolation is enabled. mergeAndExit squash-merges the milestone branch and tears down the worktree. The key invariant: process.chdir() only happens inside the create/enter functions -- the resolver never double-chdirs."
    },
    {
      "type": "text",
      "value": "When the auto loop detects a milestone transition -- the current milestone completes and a new one begins -- it calls mergeAndExit on the old milestone and enterMilestone on the new one. The merge step squash-merges all commits from the milestone branch into the integration branch. This means a milestone that took 50 agent sessions and produced 200 commits appears as a single, clean commit on main. The squash merge is not just cosmetic: it makes reverting an entire milestone trivial (one git revert) and keeps the main branch history readable even after hundreds of autonomous units.",
      "focus": "Squash merge on completion: 200 agent commits become one clean commit on main.",
      "bridge": "The integration branch is not always main -- GSD-2 records where it should merge back to."
    },
    {
      "type": "code",
      "language": "typescript",
      "value": "export function getMainBranch(): string {\n  // 1. Explicit main_branch preference (user override)\n  if (this.prefs.main_branch &&\n      VALID_BRANCH_NAME.test(this.prefs.main_branch)) {\n    return this.prefs.main_branch;\n  }\n\n  // 2. Milestone integration branch from metadata file\n  if (this._milestoneId) {\n    const integrationBranch =\n      readIntegrationBranch(this.basePath, this._milestoneId);\n    if (integrationBranch &&\n        nativeBranchExists(this.basePath, integrationBranch)) {\n      return integrationBranch;\n    }\n  }\n\n  // 3. Worktree base branch (worktree/<name>)\n  const wtName = detectWorktreeName(this.basePath);\n  if (wtName) {\n    const wtBranch = `worktree/${wtName}`;\n    if (nativeBranchExists(this.basePath, wtBranch)) return wtBranch;\n  }\n\n  // 4. origin/HEAD -> main -> master -> current branch\n  return nativeDetectMainBranch(this.basePath);\n}",
      "highlight": [1, 8, 18, 25],
      "explanation": "This is getMainBranch from git-service.ts -- the integration branch resolver. It follows a four-level resolution order. First, an explicit user preference. Second, the branch recorded in the milestone's META.json file when auto-mode started. Third, the worktree's own base branch. Fourth, the standard repo default detection. This matters because users often start GSD on feature branches, not main. The system records which branch they were on and merges back there."
    },
    {
      "type": "text",
      "value": "The integration branch recording is a subtle but important design choice. When you run /gsd auto on a feature branch called f-123-redesign, GSD-2 writes that branch name into the milestone's metadata file. All slice branches merge back into f-123-redesign, not main. This means GSD-2 works with your existing branching strategy instead of fighting it. The captureIntegrationBranch function is called once when auto-mode starts and is idempotent -- if you restart auto-mode on the same branch, it does not overwrite. If you restart on a different branch, it updates the record.",
      "focus": "GSD-2 records your starting branch and merges back there -- it adapts to your branching strategy.",
      "bridge": "The git layer also handles something surprisingly tricky: leftover merge state from crashes."
    },
    {
      "type": "text",
      "value": "You now understand GSD-2's git isolation strategy. Worktrees provide full working-directory isolation so the user and the agent do not interfere with each other. Each milestone gets a dedicated branch where commits land freely. On milestone completion, all commits are squash-merged into the integration branch -- producing a clean, revertable history on main. The WorktreeResolver manages the lifecycle: enter milestone (create or resume worktree), work (commit freely on the milestone branch), and merge-and-exit (squash merge, teardown, restore to project root). The integration branch resolver ensures merges land on the correct branch regardless of where the user started. In the next lesson, you will see how GSD-2 discovers and loads skills to extend its capabilities.",
      "focus": "Worktree isolation, squash merge, and integration branch recording make autonomous git safe.",
      "bridge": "Next lesson: how GSD-2 discovers skills and extensions to expand what it can do."
    }
  ],
  "conceptMap": "git-worktrees",
  "successCriteria": "You can explain the three isolation modes (worktree, branch, none), describe the worktree lifecycle (enter, work, merge-and-exit), explain why squash merge produces clean history, and trace the four-level integration branch resolution order."
}
```

Write this JSON to the file, replacing the existing skeleton content entirely.
  </action>
  <verify>
    <automated>cd C:/Users/18182/documents/get-shit-done && node -e "const j=require('./learn/content/modules/gsd2-agent-application/lessons/05-git-worktrees.json'); const t=j.content.filter(b=>b.type==='text').length; const c=j.content.filter(b=>b.type==='code').length; console.log('blocks:', j.content.length, 'text:', t, 'code:', c); if(j.content.length!==8||t!==5||c!==3) process.exit(1); if(!j.content[1].value.includes('isolation')) process.exit(1); console.log('PASS')"</automated>
  </verify>
  <acceptance_criteria>
    - File has exactly 8 content blocks (5 text, 3 code)
    - grep "WorktreeResolver" finds the resolver from worktree-resolver.ts
    - grep "getMainBranch" finds the integration branch resolution from git-service.ts
    - grep "GitPreferences" finds the isolation config from git-service.ts
    - grep "conceptMap.*git-worktrees" confirms sectionMap linkage
    - JSON parses without errors
  </acceptance_criteria>
  <done>Lesson 5 contains 8 blocks with real GSD-2 source snippets covering worktree isolation, squash merge, and integration branch resolution</done>
</task>

</tasks>

<verification>
- 05-git-worktrees.json has exactly 8 blocks (5 text, 3 code)
- Code blocks contain real snippets from git-service.ts, worktree-resolver.ts, worktree.ts
- JSON is valid and parseable
- conceptMap matches sectionMap key "git-worktrees"
</verification>

<success_criteria>
Lesson 5 renders in the learning shell with 8 blocks teaching branch-per-milestone, worktree isolation, squash merge, and integration branch resolution using real GSD-2 source code.
</success_criteria>

<output>
After completion, create `.planning/phases/28-lesson-content-lessons-4-7/28-02-SUMMARY.md`
</output>
