---
phase: 29-mini-project
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - learn/content/modules/gsd2-agent-application/project/spec.json
  - learn/content/modules/gsd2-agent-application/project/hints.json
  - learn/content/modules/gsd2-agent-application/lessons/08-mini-project.json
autonomous: true
requirements:
  - MINI-01
  - MINI-02
  - MINI-03

must_haves:
  truths:
    - "Mini-project spec defines a dispatch-loop task exercising GSD-2 concepts from Lessons 1-7"
    - "Structural checks validate the learner added state tracking, dispatch logic, context injection, and review coverage"
    - "5 progressive hints guide learner from conceptual orientation through concrete implementation"
    - "Mini-project lesson introduces the task with template-first pedagogy"
  artifacts:
    - path: "learn/content/modules/gsd2-agent-application/project/spec.json"
      provides: "Mini-project spec with structural verification checks"
      contains: "gsd2-agent-application-project"
    - path: "learn/content/modules/gsd2-agent-application/project/hints.json"
      provides: "5 progressive hints"
    - path: "learn/content/modules/gsd2-agent-application/lessons/08-mini-project.json"
      provides: "Mini-project lesson content"
      contains: "mini-project"
  key_links:
    - from: "learn/content/modules/gsd2-agent-application/lessons/08-mini-project.json"
      to: "spec.json checks"
      via: "verifyCommand references module=gsd2-agent-application"
      pattern: "verify.*module=gsd2-agent-application"
---

<objective>
Create the Module 6 mini-project: spec.json with 6+ structural verification checks, hints.json with 5 progressive hints, and 08-mini-project.json lesson content. The mini-project has the learner add a "dispatch loop" to their skeptic command -- inspired by GSD-2's auto mode pattern -- exercising state machine, context engineering, auto loop, and isolation concepts from Lessons 1-7.

Purpose: Learners demonstrate GSD-2 understanding through hands-on building, completing Module 6.
Output: Three JSON files replacing skeleton placeholders.
</objective>

<execution_context>
@C:/Users/18182/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/18182/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Reference files for format (read these first):
@learn/content/modules/quality-feedback/project/spec.json
@learn/content/modules/quality-feedback/project/hints.json
@learn/content/modules/quality-feedback/lessons/08-mini-project.json
@learn/content/modules/agent-orchestration/project/spec.json
@learn/content/modules/agent-orchestration/project/hints.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create spec.json and hints.json for dispatch-loop mini-project</name>
  <files>learn/content/modules/gsd2-agent-application/project/spec.json, learn/content/modules/gsd2-agent-application/project/hints.json</files>
  <read_first>
    - learn/content/modules/quality-feedback/project/spec.json (format reference)
    - learn/content/modules/quality-feedback/project/hints.json (format reference)
    - learn/content/modules/agent-orchestration/project/spec.json (format reference)
    - learn/content/modules/agent-orchestration/project/hints.json (format reference)
  </read_first>
  <action>
Write spec.json with this exact content:

```json
{
  "id": "gsd2-agent-application-project",
  "moduleId": "gsd2-agent-application",
  "title": "Add a Dispatch Loop to Skeptic Reviews",
  "description": "Extend your skeptic workflow with a dispatch loop -- inspired by GSD-2's auto mode, it reads project state to decide what to review next, dispatches focused review subagents with scoped context, tracks what has been reviewed, and loops until coverage is complete",
  "artifacts": [
    {
      "description": "Skeptic workflow with dispatch loop",
      "path": "~/.claude/get-shit-done/workflows/skeptic.md",
      "checks": [
        { "pattern": "state|status|position|progress|current.*(phase|step|stage)", "description": "Reads or tracks project state to decide what to review next" },
        { "pattern": "dispatch|route|send|delegate|assign.*task|hand.*off|invoke.*(agent|review)", "description": "Has dispatch logic that routes work to focused review subagents" },
        { "pattern": "context|scope|focus|relevant.*file|narrow|filter|inject.*prompt", "description": "Injects scoped context into each dispatched review (not full project)" },
        { "pattern": "track|coverage|reviewed|visited|completed|checked.*(area|file|module)|progress.*map", "description": "Tracks what has been reviewed to avoid duplication" },
        { "pattern": "loop|repeat|iterate|cycle|until.*done|until.*complete|while.*remain|next.*round", "description": "Loops or iterates until review coverage is complete" },
        { "pattern": "done|complete|finish|exit|stop|termin|all.*reviewed|coverage.*100|no.*remaining", "description": "Has a termination condition that ends the dispatch loop" }
      ]
    }
  ]
}
```

Write hints.json with this exact content:

```json
[
  "Your skeptic workflow reviews code but always runs the same steps in the same order. GSD-2's auto mode is different -- it reads state, decides what to do, dispatches a focused agent, and repeats. Think about what your skeptic would need to behave like a dispatch loop instead of a static checklist.",
  "A dispatch loop has four components: (1) read current state to know what has and hasn't been done, (2) decide what to dispatch next based on that state, (3) dispatch with focused context -- not the whole project, just what's relevant to this review pass, (4) update tracking so the next iteration knows what's been covered. All four need to appear in your workflow.",
  "Add a step (or restructure existing steps) so the skeptic tracks which areas have been reviewed and which remain. Before each review dispatch, it should read this tracking state and pick the next unreviewed area. Each dispatched review gets scoped context -- only the files and patterns relevant to that area, not everything. The loop continues until all areas are marked reviewed.",
  "Create a dispatch_loop step (or similar) that: (a) maintains a list of review areas with their completion status (state tracking), (b) selects the next unreviewed area and routes to a focused review for it (dispatch logic), (c) provides each review with only the relevant files and context for that area (context scoping), (d) marks the area as reviewed after completion and checks if more areas remain (coverage tracking with loop continuation).",
  "In your skeptic workflow, add a dispatch loop mechanism: (a) define a coverage map listing review areas and their status (e.g., architecture: pending, conventions: pending), (b) add dispatch logic that reads the coverage map, picks the next pending area, and invokes a focused review subagent for it -- provide that subagent with scoped context (only files relevant to that area, not the full project), (c) after each dispatch completes, update the coverage map to mark that area reviewed, (d) loop back to the dispatch step and repeat until all areas show complete, (e) include a termination condition (all areas reviewed or coverage threshold met) that exits the loop. The structural checks verify these patterns exist -- the specific review areas, context scoping strategy, and loop mechanism are your creative decisions."
]
```
  </action>
  <verify>
    <automated>node -e "const s=require('./learn/content/modules/gsd2-agent-application/project/spec.json'); const h=require('./learn/content/modules/gsd2-agent-application/project/hints.json'); console.log('spec checks:', s.artifacts[0].checks.length, '>=6:', s.artifacts[0].checks.length >= 6); console.log('hints count:', h.length, '==5:', h.length === 5); console.log('spec id:', s.id); console.log('spec moduleId:', s.moduleId); if(s.artifacts[0].checks.length < 6 || h.length !== 5) process.exit(1);"</automated>
  </verify>
  <acceptance_criteria>
    - spec.json has id "gsd2-agent-application-project", moduleId "gsd2-agent-application"
    - spec.json has exactly 6 structural checks covering: state reading, dispatch logic, context injection, coverage tracking, looping, termination
    - spec.json artifact path targets ~/.claude/get-shit-done/workflows/skeptic.md
    - hints.json is an array of exactly 5 strings, progressing from conceptual to concrete
  </acceptance_criteria>
  <done>spec.json has 6 structural checks validating dispatch-loop patterns; hints.json has 5 progressive hints from orientation to implementation</done>
</task>

<task type="auto">
  <name>Task 2: Create 08-mini-project.json lesson content</name>
  <files>learn/content/modules/gsd2-agent-application/lessons/08-mini-project.json</files>
  <read_first>
    - learn/content/modules/quality-feedback/lessons/08-mini-project.json (format reference)
    - learn/content/modules/gsd2-agent-application/lessons/01-overview.json (Module 6 lesson format)
  </read_first>
  <action>
Write 08-mini-project.json with this exact content:

```json
{
  "id": "gsd2-mini-project",
  "title": "Mini-Project: Dispatch Loop for Skeptic Reviews",
  "lessonNumber": 8,
  "objective": "Apply GSD-2 architecture concepts (state machine, dispatch pipeline, context engineering, auto mode) to extend the skeptic workflow with a dispatch loop that reads state, routes focused reviews, and tracks coverage until complete",
  "content": [
    {
      "type": "text",
      "value": "Look at the skeptic command you have built across five modules. In Module 1 you wrote the command spec. In Module 2 you added the handler. In Module 3 you made findings persist. In Module 4 you added orchestration with parallel review subagents and aggregation. In Module 5 you added quality verification that checks the review's own output.\n\nBut every time you run skeptic, it executes the same steps in the same order. It does not look at what it already reviewed. It does not decide what to focus on next based on project state. It does not scope its context to the area under review. Compare that to GSD-2's auto mode from Lesson 4: auto mode reads state from disk, decides what task to dispatch, gives that task only the context it needs, marks it complete, and loops until the milestone is done.\n\nModule 6 closes this gap. You will add a dispatch loop to your skeptic workflow -- a mechanism that reads review state, decides what area to review next, dispatches a focused review with scoped context, tracks coverage, and repeats until all areas are reviewed. This is the auto mode pattern applied to code review.",
      "focus": "The skeptic journey across 6 modules -- now adding GSD-2's dispatch loop pattern",
      "bridge": "Here is how to check your progress as you build."
    },
    {
      "type": "text",
      "value": "Run the verify command any time to check your structural progress. Run the hint command when you need a nudge. You can use both as many times as you like.\n\nThis project modifies your existing skeptic workflow file, so the verify command checks it for dispatch loop patterns. If something goes wrong with the workflow, you can git checkout the original from your previous work:\n\n  git checkout -- ~/.claude/get-shit-done/workflows/skeptic.md",
      "focus": "Verify and hint workflow, with git safety net",
      "bridge": "Here is the template you will integrate into your existing workflow."
    },
    {
      "type": "text",
      "value": "Below is a workflow template that adds a dispatch loop to your existing skeptic workflow. Press C to copy, then integrate it into your workflow at ~/.claude/get-shit-done/workflows/skeptic.md. The key addition is a dispatch loop mechanism that replaces static review steps with a dynamic cycle: read state, pick next area, dispatch focused review, update tracking, repeat.\n\nPlace this mechanism so it controls how reviews are dispatched -- it can replace or wrap your existing wave-based review steps from Module 4. The specific review areas, context scoping strategy, and loop structure are yours to customize.",
      "focus": "Introducing the dispatch loop template",
      "bridge": "The template follows below."
    },
    {
      "type": "code",
      "language": "xml",
      "value": "<!-- Dispatch loop for skeptic reviews (inspired by GSD-2 auto mode) -->\n\n<step name=\"dispatch_loop\">\n**Dispatch focused reviews in a loop until coverage is complete.**\n\n## State Tracking\nMaintain a coverage map of review areas and their status:\n- architecture: pending\n- conventions: pending\n- dependencies: pending\n- error-handling: pending\n(Add or remove areas based on the project under review)\n\n## Dispatch Logic\nFor each iteration of the loop:\n1. Read the coverage map to find the next pending area\n2. Route to a focused review for that specific area\n3. Provide scoped context -- only files and patterns relevant to this area\n\n## Context Scoping\nEach dispatched review receives narrowed context:\n- Filter files to only those relevant to the review area\n- Inject area-specific review criteria\n- Do NOT pass the entire project -- scope to what matters\n\n## Coverage Tracking\nAfter each dispatched review completes:\n- Mark the area as reviewed in the coverage map\n- Record findings from this review pass\n- Check if unreviewed areas remain\n\n## Loop Continuation\nRepeat the dispatch cycle:\n- If pending areas remain -> dispatch next review\n- If all areas are reviewed -> exit loop, proceed to aggregation\n- Termination: all areas marked complete or coverage threshold met\n</step>",
      "focus": "Dispatch loop step with state tracking, dispatch logic, context scoping, coverage tracking, and termination"
    },
    {
      "type": "text",
      "value": "Make it yours. The six structural requirements that verification checks for are:\n\n1. State tracking -- reads or tracks project state to decide what to review next\n2. Dispatch logic -- routes work to focused review subagents\n3. Context scoping -- injects scoped context into each dispatched review (not full project)\n4. Coverage tracking -- tracks what has been reviewed to avoid duplication\n5. Loop iteration -- loops or iterates until review coverage is complete\n6. Termination condition -- has a condition that ends the dispatch loop\n\nEverything else -- the specific review areas, how context is scoped, the loop mechanism, whether you use a counter or a map or a checklist, how many iterations, what triggers termination -- is your creative decision. Verification checks structure, not content.",
      "focus": "Six structural requirements with creative freedom for everything else",
      "bridge": "Here is exactly what you need to deliver."
    },
    {
      "type": "project",
      "task": "Extend your skeptic workflow with a dispatch loop -- a mechanism that reads review state, decides what area to review next, dispatches focused reviews with scoped context, tracks coverage, and loops until all areas are reviewed.",
      "deliverables": [
        "Updated workflow at ~/.claude/get-shit-done/workflows/skeptic.md with a dispatch loop mechanism",
        "Running /gsd:skeptic dispatches focused reviews iteratively based on state, with scoped context and coverage tracking"
      ],
      "verifyCommand": "node learn/bin/gsd-learn.cjs --verify --module=gsd2-agent-application",
      "hintCommand": "node learn/bin/gsd-learn.cjs --hint --module=gsd2-agent-application",
      "focus": "Project deliverables and verification commands",
      "bridge": "Complete both deliverables and run verify to confirm your module mastery."
    },
    {
      "type": "text",
      "value": "With this mini-project complete, you have finished Module 6: GSD-2 -- The Agent Application. Step back and see the full arc: from understanding why GSD evolved into an autonomous agent, through the dispatch pipeline, context engineering, auto mode, git workflows, skills and extensions, and the synthesis of how all pieces connect -- you have learned how a methodology document became a self-driving application.\n\nAnd now you have applied the core pattern. Your skeptic command no longer runs the same steps in the same order -- it reads state, decides what to do next, dispatches focused work with scoped context, tracks progress, and loops until done. That is the dispatch loop. That is what makes GSD-2 autonomous. And now your skeptic embodies the same pattern.\n\nSix modules complete. Your skeptic started as a simple command spec in Module 1. Now it has a handler, persistent findings, orchestrated parallel reviews, quality verification, and an autonomous dispatch loop. Each module added a layer of capability -- just like GSD evolved from v1 to v2. Module 6 complete.",
      "focus": "Module 6 completion -- the dispatch loop pattern applied from GSD-2 to the skeptic workflow",
      "bridge": "Module 6 complete. Your skeptic workflow now dispatches focused reviews autonomously using the same pattern as GSD-2's auto mode."
    }
  ],
  "conceptMap": "mini-project",
  "successCriteria": "Running --verify --module=gsd2-agent-application shows all checks passing for the skeptic workflow (state tracking, dispatch logic, context scoping, coverage tracking, loop iteration, termination condition). Your skeptic command now dispatches focused reviews autonomously, completing the GSD-2 architecture module."
}
```
  </action>
  <verify>
    <automated>node -e "const l=require('./learn/content/modules/gsd2-agent-application/lessons/08-mini-project.json'); console.log('blocks:', l.content.length); console.log('has project block:', l.content.some(b => b.type === 'project')); console.log('has code block:', l.content.some(b => b.type === 'code')); console.log('verify cmd:', l.content.find(b => b.type === 'project')?.verifyCommand); if(l.content.length < 5 || !l.content.some(b => b.type === 'project')) process.exit(1);"</automated>
  </verify>
  <acceptance_criteria>
    - 08-mini-project.json has 7 content blocks (4 text, 1 code, 1 project, 1 closing text)
    - Has a code block with the dispatch loop template in XML format
    - Has a project block with verifyCommand referencing module=gsd2-agent-application
    - Has a project block with hintCommand referencing module=gsd2-agent-application
    - Narrative connects Modules 1-6 and frames the dispatch loop as GSD-2's auto mode applied to reviews
    - Lists 6 structural requirements matching spec.json checks
  </acceptance_criteria>
  <done>08-mini-project.json renders as a complete lesson with template, structural requirements, project deliverables, and module completion narrative</done>
</task>

</tasks>

<verification>
All three JSON files parse without errors. spec.json has 6+ structural checks. hints.json has exactly 5 hints. 08-mini-project.json has 7 content blocks with project and code types. Verify command references correct module ID.
</verification>

<success_criteria>
- spec.json defines dispatch-loop mini-project with 6 structural pattern checks
- hints.json provides 5 progressive hints from conceptual to concrete
- 08-mini-project.json introduces the task with template-first pedagogy and module completion narrative
- All three files are valid JSON matching established Module 1-5 formats
</success_criteria>

<output>
After completion, create `.planning/phases/29-mini-project/29-01-SUMMARY.md`
</output>
