---
phase: 34-module-7-content-lessons-4-7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - learn/content/modules/workspaces-collaboration/lessons/05-workspace-isolation.json
  - learn/content/modules/workspaces-collaboration/lessons/06-workspace-lifecycle.json
  - learn/content/modules/workspaces-collaboration/lessons/07-collaboration-patterns.json
autonomous: true
requirements: [WRK-04, WRK-05, WRK-06, WRK-07]

must_haves:
  truths:
    - "Learner can navigate Lesson 5 and understand workspace isolation (file ownership, .gsd/ separation, branch scoping)"
    - "Learner can navigate Lesson 6 and understand the full workspace lifecycle (create, switch, complete/merge, resume)"
    - "Learner can navigate Lesson 7 and understand collaboration patterns (review feedback, multi-runtime coordination) and when to use workstreams vs sequential milestones"
  artifacts:
    - path: "learn/content/modules/workspaces-collaboration/lessons/05-workspace-isolation.json"
      provides: "Workspace isolation lesson with 8 blocks"
      contains: "workspace-isolation"
    - path: "learn/content/modules/workspaces-collaboration/lessons/06-workspace-lifecycle.json"
      provides: "Workspace lifecycle lesson with 8 blocks"
      contains: "workspace-lifecycle"
    - path: "learn/content/modules/workspaces-collaboration/lessons/07-collaboration-patterns.json"
      provides: "Collaboration patterns + decision guidance lesson with 8 blocks"
      contains: "collaboration-patterns"
  key_links:
    - from: "05-workspace-isolation.json"
      to: "worktree-manager.ts, auto-worktree.ts"
      via: "real source snippets showing isolation mechanisms"
      pattern: "worktreePath|syncGsdState|isolation"
    - from: "06-workspace-lifecycle.json"
      to: "worktree-command.ts, auto-worktree.ts"
      via: "real source snippets showing create/switch/merge/remove handlers"
      pattern: "handleCreate|handleSwitch|handleMerge|handleReturn"
    - from: "07-collaboration-patterns.json"
      to: "review/SKILL.md, worktree-resolver.ts"
      via: "real source snippets showing review and coordination patterns"
      pattern: "WorktreeResolverDeps|review"
---

<objective>
Replace skeleton lessons 5-7 with full 8-block content covering workspace isolation, workspace lifecycle, and collaboration patterns (including workstreams vs sequential milestones decision guidance).

Purpose: Complete Module 7 lesson content so learners can understand the full workspace and collaboration feature set.
Output: Three JSON lesson files with 8 blocks each (5 text, 3 code), real GSD source snippets, proper conceptMap and successCriteria.
</objective>

<execution_context>
@C:/Users/18182/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/18182/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

# Reference for 8-block lesson pattern (5 text, 3 code):
@learn/content/modules/workspaces-collaboration/lessons/01-overview.json
@learn/content/modules/workspaces-collaboration/lessons/02-workstream-namespacing.json

# GSD source files for real snippets:
# Isolation: worktree-manager.ts (worktreePath, worktreesDir, createWorktree validation), auto-worktree.ts (syncGsdStateToWorktree), auto-worktree-sync.ts (syncProjectRootToWorktree, syncStateToProjectRoot)
# Lifecycle: worktree-command.ts (handleCreate, handleSwitch, handleReturn, handleMerge, handleRemove, registerWorktreeCommand)
# Collaboration: review/SKILL.md, worktree-resolver.ts (WorktreeResolverDeps interface)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace skeleton lessons 5-7 with full 8-block content</name>
  <files>
    learn/content/modules/workspaces-collaboration/lessons/05-workspace-isolation.json
    learn/content/modules/workspaces-collaboration/lessons/06-workspace-lifecycle.json
    learn/content/modules/workspaces-collaboration/lessons/07-collaboration-patterns.json
  </files>
  <read_first>
    learn/content/modules/workspaces-collaboration/lessons/01-overview.json
    learn/content/modules/workspaces-collaboration/lessons/02-workstream-namespacing.json
    learn/content/modules/workspaces-collaboration/lessons/05-workspace-isolation.json
    learn/content/modules/workspaces-collaboration/lessons/06-workspace-lifecycle.json
    learn/content/modules/workspaces-collaboration/lessons/07-collaboration-patterns.json
    C:/Users/18182/AppData/Roaming/npm/node_modules/gsd-pi/src/resources/extensions/gsd/worktree-manager.ts
    C:/Users/18182/AppData/Roaming/npm/node_modules/gsd-pi/src/resources/extensions/gsd/worktree-command.ts
    C:/Users/18182/AppData/Roaming/npm/node_modules/gsd-pi/src/resources/extensions/gsd/auto-worktree.ts
    C:/Users/18182/AppData/Roaming/npm/node_modules/gsd-pi/src/resources/extensions/gsd/auto-worktree-sync.ts
    C:/Users/18182/AppData/Roaming/npm/node_modules/gsd-pi/src/resources/extensions/gsd/worktree-resolver.ts
    C:/Users/18182/AppData/Roaming/npm/node_modules/gsd-pi/src/resources/extensions/gsd/workspace-index.ts
    C:/Users/18182/AppData/Roaming/npm/node_modules/gsd-pi/src/resources/skills/review/SKILL.md
  </read_first>
  <action>
    Replace each skeleton lesson JSON with full 8-block content (5 text, 3 code blocks). Follow the exact pattern from lessons 01 and 02: each text block has value/focus/bridge, each code block has language/value/highlight, conceptMap is a string matching the sectionMap ID, successCriteria is a single string.

    **IMPORTANT JSON rules:**
    - Use straight double-dashes (--) not em-dashes in text values
    - Escape all quotes inside JSON strings
    - conceptMap must be a STRING (e.g., "workspace-isolation"), NOT an object
    - successCriteria must be a STRING, NOT an array
    - Validate JSON before writing each file

    **Lesson 5: Workspace Isolation (05-workspace-isolation.json)** — per WRK-04
    - id: "workspace-isolation", title: "Workspace Isolation", lessonNumber: 5
    - Objective: Understand how workspaces isolate workstreams to avoid conflicts -- file ownership, .gsd/ separation, and branch scoping
    - Content flow (8 blocks):
      1. TEXT: Introduce the problem -- parallel workstreams risk file conflicts, branch pollution, and .gsd/ state corruption. Isolation is the answer. Bridge to how worktrees get their own directory.
      2. CODE: Extract the path helpers from worktree-manager.ts showing worktreesDir() and worktreePath() -- these show the physical isolation (each worktree lives under .gsd/worktrees/<name>/). Language: typescript.
      3. TEXT: Explain directory isolation -- each worktree is a full working copy at its own path. The .gsd/worktrees/ directory acts as a container. No two worktrees share a working directory. Bridge to branch isolation.
      4. CODE: Extract worktreeBranchName() from worktree-manager.ts showing how each worktree gets its own branch `worktree/<name>`. Also show the branch validation from createWorktree (checking branchInUse to prevent conflicts). Language: typescript.
      5. TEXT: Explain branch isolation -- each worktree gets a dedicated branch, and GSD validates that no two worktrees share a branch. This prevents git conflicts at the branch level. Bridge to .gsd/ state isolation.
      6. CODE: Extract the syncGsdStateToWorktree function signature and the key comment from auto-worktree.ts (lines 82-94) showing the one-way sync pattern: "Only adds missing content -- never overwrites existing files in the worktree (the worktree's execution state is authoritative for in-progress work)". Language: typescript.
      7. TEXT: Explain .gsd/ state isolation -- each worktree has its own .gsd/ directory. State syncs one-way from main to worktree (additive only, never overwrites). The worktree's state is authoritative for in-progress work. This prevents state corruption between parallel workstreams.
      8. TEXT: Summary -- three layers of isolation: directory (separate working copies), branch (dedicated branches), and .gsd/ state (one-way sync, worktree authoritative). Bridge to lifecycle lesson.
    - conceptMap: "workspace-isolation"
    - successCriteria: single string covering ability to explain directory isolation, branch isolation, state isolation, and why one-way sync prevents conflicts

    **Lesson 6: Workspace Lifecycle (06-workspace-lifecycle.json)** — per WRK-05
    - id: "workspace-lifecycle", title: "Workspace Lifecycle", lessonNumber: 6
    - Objective: Understand the full workspace lifecycle: create, switch, complete (merge), and resume
    - Content flow (8 blocks):
      1. TEXT: Introduce the lifecycle concept -- a worktree has four phases: create (set up isolated environment), switch (move between worktrees), complete (merge back to main), and resume (re-enter existing worktree). Bridge to create.
      2. CODE: Extract the handleCreate function body from worktree-command.ts (lines 312-350 approximately) showing the create flow: auto-commit dirty files, createWorktree(), run post-create hook, chdir into worktree. Show enough to see the flow without all error handling. Language: typescript.
      3. TEXT: Explain create -- GSD auto-commits dirty files before creating, runs a post-create hook for environment setup (.env copy, asset symlinks), and changes directory into the new worktree. Bridge to switch/return.
      4. CODE: Extract the /worktree command usage block from worktree-command.ts showing the full command set: `/<alias> <name>` (create/switch), `/<alias> switch <name>`, `/<alias> return`, `/<alias> list`, `/<alias> merge [name] [target]`, `/<alias> remove <name|all>`. Language: text.
      5. TEXT: Explain switch and return -- switch moves you into an existing worktree, return takes you back to the main project tree. GSD tracks the original cwd so return always works. The create-or-switch behavior is smart: if a name matches an existing worktree, it switches; if not, it creates. Bridge to merge (complete).
      6. CODE: Extract the WorktreeResolverDeps interface from worktree-resolver.ts showing the lifecycle operations: isInAutoWorktree, mergeMilestoneToMain, syncWorktreeStateBack, teardownAutoWorktree, createAutoWorktree, enterAutoWorktree. Language: typescript.
      7. TEXT: Explain complete (merge) and teardown -- merge brings work back to main via squash merge, sync copies state back to project root, teardown removes the worktree directory and optionally the branch. For auto-mode, the resolver orchestrates this automatically at milestone boundaries.
      8. TEXT: Summary -- lifecycle is create -> switch/return -> merge -> remove. Auto-mode manages this automatically via WorktreeResolver. Manual /worktree gives you full control over each phase. Bridge to collaboration patterns.
    - conceptMap: "workspace-lifecycle"
    - successCriteria: single string covering ability to explain the four lifecycle phases, what auto-commit does before create, how switch/return tracks original cwd, and how WorktreeResolver automates lifecycle for auto-mode

    **Lesson 7: Collaboration Patterns (07-collaboration-patterns.json)** — per WRK-06 and WRK-07
    - id: "collaboration-patterns", title: "Collaboration Patterns", lessonNumber: 7
    - Objective: Understand collaboration patterns (review feedback loops, multi-runtime coordination) and when to choose workstreams vs sequential milestones
    - Content flow (8 blocks):
      1. TEXT: Introduce collaboration in GSD -- collaboration is not just about parallel workstreams, it is about structured feedback loops and coordinated work across different AI runtimes. Three patterns: review feedback, multi-runtime coordination, and deciding when parallelism adds value. Bridge to review.
      2. CODE: Extract the review skill's core_principle and analysis_only_rule from review/SKILL.md showing the read-only review contract and the focus on real issues. Use language: text, formatted as the markdown content.
      3. TEXT: Explain review feedback pattern -- /review is read-only by design (reviewer never modifies code). It analyzes diffs across security, performance, bugs, and quality. The structured output (severity ratings, categorized findings) feeds back into the development loop. This works across runtimes -- one AI writes code, another reviews it. Bridge to multi-runtime.
      4. CODE: Extract WorktreeResolverDeps interface fields showing the coordination operations: getIsolationMode returning "worktree" | "branch" | "none", mergeMilestoneToMain, syncWorktreeStateBack. These show how different runtimes coordinate through shared file-system state. Language: typescript.
      5. TEXT: Explain multi-runtime coordination -- different AI runtimes (Claude Code, Cursor CLI, Gemini CLI) coordinate through the file system, not through APIs. .gsd/ state files are the shared protocol. WorktreeResolver ensures each runtime sees consistent state by syncing worktree state back to the project root. This means you can start work in one runtime and continue in another. Bridge to decision guidance (per WRK-07).
      6. CODE: Show a decision table as a code block comparing workstreams vs sequential milestones. Language: text. Content should be a markdown table:
         ```
         ### When to Use Workstreams vs Sequential Milestones

         | Factor             | Sequential Milestones     | Parallel Workstreams       |
         |--------------------|---------------------------|----------------------------|
         | Work independence   | Tasks depend on each other | Tasks are independent      |
         | Risk tolerance      | Low -- linear progress    | Higher -- merge complexity |
         | Project size        | Small-medium              | Medium-large               |
         | Exploration needed  | No -- path is clear       | Yes -- prototyping/spikes  |
         | Team/runtime count  | Single runtime            | Multiple runtimes          |
         | Merge overhead      | None                      | Squash merge per worktree  |

         Rule of thumb: if you would create a feature branch
         in plain git, use a workstream. If you would just
         keep committing to main, use sequential milestones.
         ```
      7. TEXT: Explain the decision guidance (per WRK-07) -- sequential milestones are the default and work well for most projects. Workstreams add value when you need to explore alternatives without disrupting main, when tasks are genuinely independent, or when multiple runtimes need isolated workspaces. The merge overhead is real -- each worktree requires a squash merge. Use workstreams when the isolation benefit outweighs the merge cost.
      8. TEXT: Summary -- three collaboration patterns: review feedback (read-only structured analysis), multi-runtime coordination (file-system protocol via .gsd/ state), and the workstreams vs milestones decision. The rule of thumb: if tasks are independent and you would branch in git, use workstreams. Bridge to mini-project.
    - conceptMap: "collaboration-patterns"
    - successCriteria: single string covering ability to explain review feedback pattern, multi-runtime coordination via file system state, and when to use workstreams vs sequential milestones
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const lessons=['05-workspace-isolation','06-workspace-lifecycle','07-collaboration-patterns'];let ok=true;for(const l of lessons){const f=`learn/content/modules/workspaces-collaboration/lessons/${l}.json`;const j=JSON.parse(fs.readFileSync(f,'utf8'));if(j.content.length!==8){console.error(l+': expected 8 blocks, got '+j.content.length);ok=false;}const textCount=j.content.filter(b=>b.type==='text').length;const codeCount=j.content.filter(b=>b.type==='code').length;if(textCount!==5||codeCount!==3){console.error(l+': expected 5 text + 3 code, got '+textCount+' text + '+codeCount+' code');ok=false;}if(typeof j.conceptMap!=='string'||j.conceptMap===''){console.error(l+': conceptMap must be non-empty string');ok=false;}if(typeof j.successCriteria!=='string'||j.successCriteria===''){console.error(l+': successCriteria must be non-empty string');ok=false;}if(j.objective.startsWith('Placeholder')){console.error(l+': still has placeholder objective');ok=false;}console.log(l+': '+textCount+' text, '+codeCount+' code, conceptMap='+j.conceptMap);}if(!ok)process.exit(1);console.log('All 3 lessons valid.')"</automated>
  </verify>
  <acceptance_criteria>
    - grep -l "workspace-isolation" learn/content/modules/workspaces-collaboration/lessons/05-workspace-isolation.json returns a match
    - grep -l "workspace-lifecycle" learn/content/modules/workspaces-collaboration/lessons/06-workspace-lifecycle.json returns a match
    - grep -l "collaboration-patterns" learn/content/modules/workspaces-collaboration/lessons/07-collaboration-patterns.json returns a match
    - Each lesson has exactly 8 content blocks (5 text, 3 code)
    - No lesson has "Placeholder" in the objective
    - conceptMap is a string (not an object) matching the sectionMap ID
    - successCriteria is a string (not an array)
    - Code blocks contain real snippets from GSD source files (worktree-manager.ts, worktree-command.ts, auto-worktree.ts, worktree-resolver.ts, review/SKILL.md)
    - Lesson 7 includes workstreams vs sequential milestones decision guidance (WRK-07)
  </acceptance_criteria>
  <done>All three skeleton lessons replaced with full 8-block content. Lesson 5 teaches workspace isolation (directory, branch, state). Lesson 6 teaches workspace lifecycle (create, switch, complete, resume). Lesson 7 teaches collaboration patterns (review, multi-runtime, decision guidance). All use real GSD source snippets.</done>
</task>

</tasks>

<verification>
1. All three lesson JSON files parse without errors
2. Each lesson has exactly 8 content blocks (5 text, 3 code)
3. conceptMap is a string matching sectionMap IDs: workspace-isolation, workspace-lifecycle, collaboration-patterns
4. successCriteria is a non-empty string on each lesson
5. No placeholder content remains
6. Code blocks contain recognizable GSD source snippets
</verification>

<success_criteria>
- Three lesson files with full content replacing skeletons
- 8 blocks per lesson (5 text, 3 code) with real source snippets
- WRK-04 (isolation), WRK-05 (lifecycle), WRK-06 (collaboration), WRK-07 (decision guidance) all addressed
- Verification script passes confirming block counts and field types
</success_criteria>

<output>
After completion, create `.planning/phases/34-module-7-content-lessons-4-7/34-01-SUMMARY.md`
</output>
