---
phase: 05-module-1-lessons
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - learn/content/modules/gsd-commands/lessons/02-command-anatomy.json
  - learn/content/modules/gsd-commands/lessons/03-workflow-anatomy.json
  - learn/content/modules/gsd-commands/lessons/04-dispatch-chain.json
autonomous: true
requirements: [MOD1-02, MOD1-03, MOD1-04]
must_haves:
  truths:
    - "Learner can navigate through an anatomy lesson showing real command.md source with highlighted sections"
    - "Learner can navigate through an anatomy lesson showing real workflow.md source with highlighted sections"
    - "Learner can see how a /gsd:X command dispatches through the full chain from command spec to workflow execution"
    - "All 5 Module 1 lessons load and validate successfully via loadModule"
  artifacts:
    - path: "learn/content/modules/gsd-commands/lessons/02-command-anatomy.json"
      provides: "Command spec anatomy lesson using quick.md"
      contains: "frontmatter"
    - path: "learn/content/modules/gsd-commands/lessons/03-workflow-anatomy.json"
      provides: "Workflow anatomy lesson using quick.md workflow"
      contains: "process"
    - path: "learn/content/modules/gsd-commands/lessons/04-dispatch-chain.json"
      provides: "Dispatch chain lesson connecting command spec to workflow"
      contains: "execution_context"
  key_links:
    - from: "learn/content/modules/gsd-commands/lessons/02-command-anatomy.json"
      to: "learn/content/modules/gsd-commands/module.json"
      via: "conceptMap field"
      pattern: "command-spec"
    - from: "learn/content/modules/gsd-commands/lessons/03-workflow-anatomy.json"
      to: "learn/content/modules/gsd-commands/module.json"
      via: "conceptMap field"
      pattern: "workflow"
    - from: "learn/content/modules/gsd-commands/lessons/04-dispatch-chain.json"
      to: "learn/content/modules/gsd-commands/module.json"
      via: "conceptMap field"
      pattern: "dispatch"
---

<objective>
Create the three anatomy lessons (Lessons 2-4) that teach command spec structure, workflow structure, and the dispatch chain connecting them -- using real quick.md source files as content.

Purpose: Delivers the core teaching content of Module 1 where learners read and understand actual GSD markdown source files.
Output: Three lesson JSON files completing the 5-lesson module, all passing loadModule validation.
</objective>

<execution_context>
@C:/Users/18182/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/18182/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-module-1-lessons/05-RESEARCH.md
@.planning/phases/05-module-1-lessons/05-01-SUMMARY.md

<interfaces>
<!-- Lesson JSON schema -- every content item requires focus + bridge -->
```json
{
  "id": "kebab-case-id",
  "title": "Human Title",
  "lessonNumber": 2,
  "objective": "One sentence",
  "content": [
    { "type": "text", "value": "...", "focus": "3-8 words", "bridge": "One sentence to next block" },
    { "type": "code", "language": "markdown", "value": "actual source from file", "highlight": [1, 3], "focus": "...", "bridge": "..." }
  ],
  "conceptMap": "sectionMap-key-from-module.json",
  "successCriteria": "One sentence"
}
```

sectionMap keys (from module.json, created in Plan 01):
- "overview" -> Lesson 1
- "command-spec" -> Lesson 2
- "workflow" -> Lesson 3
- "dispatch" -> Lesson 4
- "bridge" -> Lesson 5

Source files to use:
- Command spec: ~/.claude/commands/gsd/quick.md (45 lines, has frontmatter + XML sections + @file refs)
- Workflow: ~/.claude/get-shit-done/workflows/quick.md (585 lines, has purpose/process/steps/code blocks)

From learn/lib/markdown-parser.cjs:
```javascript
function parseMarkdownFile(filePath)
// Returns: { filePath, fileName, fileType, frontmatter, sections, fileReferences, codeBlocks, body }
```

Content guidelines from RESEARCH.md:
- Code blocks must use ACTUAL content from source files (not invented markup)
- Alternate text and code blocks (never two code back-to-back)
- Keep code snippets 5-15 lines with highlights on important lines
- focus: 3-8 words; bridge: one sentence connecting to next block
- Last item bridge references next lesson title
- loadModule validates: required fields + non-empty focus/bridge on every content item
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create command spec anatomy lesson (Lesson 2) and workflow anatomy lesson (Lesson 3)</name>
  <files>
    learn/content/modules/gsd-commands/lessons/02-command-anatomy.json,
    learn/content/modules/gsd-commands/lessons/03-workflow-anatomy.json
  </files>
  <action>
First, read the actual source files to extract real content for the lessons:
- `parseMarkdownFile(path.join(os.homedir(), '.claude', 'commands', 'gsd', 'quick.md'))` for command spec
- `parseMarkdownFile(path.join(os.homedir(), '.claude', 'get-shit-done', 'workflows', 'quick.md'))` for workflow

Or directly read the files with `cat` to understand their structure, then hand-author JSON using actual snippets.

**Lesson 2 (02-command-anatomy.json) -- MOD1-02: Command Spec Anatomy**
Teach learner how a command spec (.md file with frontmatter) works. Content must use ACTUAL content from quick.md command spec:
- Start with what a command spec IS: a markdown file that defines a slash command's metadata and behavior
- Show the YAML frontmatter block from quick.md (name, description, argument-hint, allowed-tools fields). Explain each field's purpose. Use `"language": "yaml"` for frontmatter code blocks.
- Show the `<objective>` XML section. Explain it tells the AI what the command should accomplish.
- Show the `<execution_context>` section with its `@file:` reference. Explain this is the critical wiring that connects the command spec to its workflow.
- Show the `<context>` section. Explain it provides the AI with read-only reference material.
- Show or mention the `<process>` section. Explain it provides step-by-step instructions.
- Explain @file references: how `@C:/path/to/workflow.md` tells Claude Code to load that file as context.
- Every content item needs `focus` (3-8 words) and `bridge` (one sentence)
- Last content item's bridge references Lesson 3: "Workflow File Anatomy"
- `"conceptMap": "command-spec"`
- Target: 8-10 content items (alternating text/code)
- Schema: `{ id: "command-anatomy", title: "Command Spec Anatomy", lessonNumber: 2, ... }`

**Lesson 3 (03-workflow-anatomy.json) -- MOD1-03: Workflow File Anatomy**
Teach learner how a workflow file works. Content must use ACTUAL content from quick.md workflow:
- Start with what a workflow IS: a markdown file WITHOUT frontmatter that provides step-by-step execution instructions
- Show the `<purpose>` section from the workflow. Explain it describes the workflow's goal.
- Show one or two `<step>` tags from the `<process>` section. Explain step naming and sequential execution.
- Show a bash code block from the workflow. Explain how workflows embed CLI commands that the AI runs.
- Explain the difference from command specs: workflows have no YAML, focus on process execution, may reference other tools
- Point out nested XML patterns: `<step>` tags inside `<process>` tags
- Keep workflow code snippets SHORT (5-15 lines) -- the full workflow is 585 lines; extract the most instructive parts
- Every content item needs `focus` and `bridge`
- Last content item's bridge references Lesson 4: "Command to Workflow Wiring"
- `"conceptMap": "workflow"`
- Target: 7-9 content items
- Schema: `{ id: "workflow-anatomy", title: "Workflow File Anatomy", lessonNumber: 3, ... }`

CRITICAL: All code block `value` fields must contain ACTUAL content from the real quick.md files. Do NOT invent fake markup. Read the source files first, extract relevant snippets, and embed them in the lesson JSON.
  </action>
  <verify>
    <automated>node -e "const { loadModule } = require('./learn/lib/lessons.cjs'); const p = require('path'); const m = loadModule('gsd-commands', p.join(__dirname, 'learn', 'content')); const l2 = m.lessons.find(l => l.lessonNumber === 2); const l3 = m.lessons.find(l => l.lessonNumber === 3); if (!l2) throw new Error('Lesson 2 missing'); if (!l3) throw new Error('Lesson 3 missing'); console.log('L2:', l2.title, l2.content.length, 'blocks, conceptMap:', l2.conceptMap); console.log('L3:', l3.title, l3.content.length, 'blocks, conceptMap:', l3.conceptMap);"</automated>
  </verify>
  <done>Lesson 2 teaches command spec anatomy using actual quick.md frontmatter/XML/file-refs; Lesson 3 teaches workflow anatomy using actual quick.md workflow purpose/steps/code; both pass loadModule validation with valid focus/bridge on all content items</done>
</task>

<task type="auto">
  <name>Task 2: Create dispatch chain lesson (Lesson 4) and validate full module</name>
  <files>
    learn/content/modules/gsd-commands/lessons/04-dispatch-chain.json
  </files>
  <action>
**Lesson 4 (04-dispatch-chain.json) -- MOD1-04: Command-to-Workflow Wiring**
Teach the full dispatch chain from `/gsd:quick` to workflow execution. This lesson CONNECTS Lessons 2 and 3, showing how command specs wire to workflows. Content must use ACTUAL content from both quick.md files:
- Start by recapping: learner now knows command spec structure (Lesson 2) and workflow structure (Lesson 3) separately
- Show the `<execution_context>` section from quick.md command spec, highlighting the `@file:` reference to the workflow path. This is THE critical wiring.
- Explain the dispatch chain step by step:
  1. User types `/gsd:quick` in Claude Code
  2. Claude reads `commands/gsd/quick.md` (the command spec)
  3. The `<execution_context>` section says `@file:~/.claude/get-shit-done/workflows/quick.md`
  4. Claude loads the workflow file as additional context
  5. The workflow's `<process>` section provides step-by-step execution instructions
  6. Each step may run bash commands like `node gsd-tools.cjs ...`
- Show a code example of the execution_context @file reference from quick.md
- Show how the command spec's frontmatter (allowed-tools) restricts what the workflow can do
- Show one process step from the workflow that runs a CLI command, connecting it back to the command spec
- Explain that this two-file pattern (command spec + workflow) is used for ALL GSD commands -- not just quick
- Every content item needs `focus` and `bridge`
- Last content item's bridge references Lesson 5: "Bridge to Node.js"
- `"conceptMap": "dispatch"`
- Target: 7-9 content items (alternating text/code)
- Schema: `{ id: "dispatch-chain", title: "Command to Workflow Wiring", lessonNumber: 4, ... }`

After creating the lesson, run full module validation to confirm all 5 lessons load correctly:
- `loadModule('gsd-commands', contentDir)` succeeds
- All 5 lessons have valid conceptMap values matching sectionMap keys
- All content items have non-empty focus and bridge strings
- Run the full test suite: `node --test learn/tests/lessons.test.cjs`
  </action>
  <verify>
    <automated>node -e "const { loadModule } = require('./learn/lib/lessons.cjs'); const p = require('path'); const m = loadModule('gsd-commands', p.join(__dirname, 'learn', 'content')); if (m.lessons.length !== 5) throw new Error('Expected 5 lessons, got ' + m.lessons.length); const sectionMap = m.sectionMap; m.lessons.forEach(l => { if (!sectionMap[l.conceptMap] && l.conceptMap !== null) throw new Error('L' + l.lessonNumber + ' conceptMap ' + l.conceptMap + ' not in sectionMap'); l.content.forEach((c, i) => { if (!c.focus || typeof c.focus !== 'string') throw new Error('L' + l.lessonNumber + ' content[' + i + '] missing focus'); if (!c.bridge || typeof c.bridge !== 'string') throw new Error('L' + l.lessonNumber + ' content[' + i + '] missing bridge'); }); }); console.log('All 5 lessons validated:', m.lessons.map(l => 'L' + l.lessonNumber + ' ' + l.title).join(', '));" && node --test learn/tests/lessons.test.cjs</automated>
  </verify>
  <done>All 5 Module 1 lessons exist and pass loadModule validation; Lesson 4 teaches the full dispatch chain using actual content from both quick.md files; conceptMap values all match sectionMap keys; all lesson test cases pass including the gsd-commands describe block from Plan 01</done>
</task>

</tasks>

<verification>
- `node -e "const m = require('./learn/lib/lessons.cjs').loadModule('gsd-commands', require('path').join(__dirname,'learn','content')); console.log(m.id, m.lessons.length + ' lessons'); m.lessons.forEach(l => console.log(' L'+l.lessonNumber, l.title))"` prints all 5 lessons
- `node --test learn/tests/lessons.test.cjs` all tests pass (including gsd-commands module tests)
- `node --test learn/tests/*.test.cjs` full suite green
- Each lesson's code blocks contain actual content from real GSD source files (not invented)
</verification>

<success_criteria>
- Lesson 2 (MOD1-02) teaches command spec anatomy with real quick.md command spec content
- Lesson 3 (MOD1-03) teaches workflow anatomy with real quick.md workflow content
- Lesson 4 (MOD1-04) teaches the dispatch chain connecting both files
- All 5 lessons pass loadModule('gsd-commands') validation
- All content items have valid focus (3-8 words) and bridge (one sentence) fields
- All conceptMap values match sectionMap keys in module.json
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/05-module-1-lessons/05-02-SUMMARY.md`
</output>
