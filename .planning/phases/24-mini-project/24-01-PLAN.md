---
phase: 24-mini-project
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - learn/content/modules/quality-feedback/project/spec.json
  - learn/content/modules/quality-feedback/project/hints.json
  - learn/content/modules/quality-feedback/lessons/08-mini-project.json
autonomous: true
requirements:
  - MINI-01
  - MINI-02
  - MINI-03
must_haves:
  truths:
    - "Mini-project lesson appears as lesson 08 in Module 5 with spec-based verification"
    - "Verification checks test quality/feedback concepts (severity, UAT checklist, gap tracking) not just file existence"
    - "5 progressive hints guide learner from conceptual nudge to near-solution without giving away the answer"
    - "Pressing H on mini-project step reveals hints inline, persisted across sessions"
  artifacts:
    - path: "learn/content/modules/quality-feedback/project/spec.json"
      provides: "Artifact verification spec with regex checks for quality patterns"
      contains: "pattern"
    - path: "learn/content/modules/quality-feedback/project/hints.json"
      provides: "5 progressive hints array"
      min_lines: 5
    - path: "learn/content/modules/quality-feedback/lessons/08-mini-project.json"
      provides: "Mini-project lesson with 7 blocks"
      contains: "quality-feedback-mini-project"
  key_links:
    - from: "learn/content/modules/quality-feedback/lessons/08-mini-project.json"
      to: "learn/content/modules/quality-feedback/project/spec.json"
      via: "verifyCommand references --module=quality-feedback"
      pattern: "--module=quality-feedback"
    - from: "learn/content/modules/quality-feedback/module.json"
      to: "learn/content/modules/quality-feedback/lessons/08-mini-project.json"
      via: "sectionMap mini-project entry"
      pattern: "mini-project"
---

<objective>
Create the Module 5 mini-project: spec, hints, and lesson JSON for a quality verification exercise that extends the learner's skeptic workflow with UAT-style checking.

Purpose: The learner applies quality/feedback knowledge by adding a verify-findings section to their skeptic workflow -- after the skeptic review completes, it runs a UAT-style verification checklist against its own findings, with severity inference and gap tracking. This continues the cross-module skeptic extension story (Module 1: spec, Module 2: handler, Module 3: persistence, Module 4: orchestration, Module 5: quality verification).

Output: spec.json, hints.json, 08-mini-project.json
</objective>

<execution_context>
@C:/Users/18182/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/18182/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Module 4 mini-project files serve as the EXACT template. Replicate structure. -->

From learn/content/modules/agent-orchestration/project/spec.json:
```json
{
  "id": "agent-orchestration-project",
  "moduleId": "agent-orchestration",
  "title": "Build Orchestrated Skeptic Reviews",
  "description": "...",
  "artifacts": [
    {
      "description": "Skeptic workflow with orchestration layer",
      "path": "~/.claude/get-shit-done/workflows/skeptic.md",
      "checks": [
        { "pattern": "<regex>", "description": "Has X" }
      ]
    }
  ]
}
```

From learn/content/modules/agent-orchestration/project/hints.json:
```json
["hint1", "hint2", "hint3", "hint4", "hint5"]
```

From learn/content/modules/agent-orchestration/lessons/08-mini-project.json:
```json
{
  "id": "agent-orchestration-mini-project",
  "title": "Mini-Project: Orchestrated Skeptic Reviews",
  "lessonNumber": 8,
  "objective": "...",
  "content": [
    { "type": "text", "value": "...", "focus": "...", "bridge": "..." },
    { "type": "text", "value": "...", "focus": "...", "bridge": "..." },
    { "type": "text", "value": "...", "focus": "...", "bridge": "..." },
    { "type": "code", "language": "xml", "value": "...", "focus": "..." },
    { "type": "text", "value": "...", "focus": "...", "bridge": "..." },
    { "type": "project", "task": "...", "deliverables": [...], "verifyCommand": "...", "hintCommand": "...", "focus": "...", "bridge": "..." },
    { "type": "text", "value": "...", "focus": "...", "bridge": "..." }
  ],
  "conceptMap": "mini-project",
  "successCriteria": "..."
}
```

From learn/content/modules/quality-feedback/module.json:
```json
{
  "id": "quality-feedback",
  "title": "Quality & Feedback Loops",
  "order": 5,
  "sectionMap": { ..., "mini-project": "Mini-Project" }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create spec.json and hints.json for quality verification mini-project</name>
  <files>learn/content/modules/quality-feedback/project/spec.json, learn/content/modules/quality-feedback/project/hints.json</files>
  <action>
Create spec.json following the EXACT format of agent-orchestration/project/spec.json.

The mini-project concept: learner extends their skeptic workflow (already has orchestrated review subagents from Module 4) with a QUALITY VERIFICATION section. After the skeptic review completes, the workflow runs a UAT-style verification checklist against its own findings -- checking severity levels, tracking gaps, and producing a verification summary.

spec.json fields:
- id: "quality-feedback-project"
- moduleId: "quality-feedback"
- title: "Add Quality Verification to Skeptic Reviews"
- description: "Extend your skeptic workflow with a verify-findings section -- after the review completes, run a UAT-style verification checklist against the findings, infer severity levels, and track gaps between what was reviewed and what was missed"
- artifacts: single artifact targeting ~/.claude/get-shit-done/workflows/skeptic.md
- checks (6 regex checks that test QUALITY concepts, not just file existence):
  1. Has a verify/validation/check section after the review steps: pattern matches verify_findings or verify_review or validate_findings or quality_check or similar step name
  2. Defines severity levels (critical/major/minor or high/medium/low): pattern matches severity classification language
  3. Has a UAT-style checklist (checklist items, acceptance criteria, or pass/fail checks against findings): pattern matches checklist or acceptance or pass.*fail or criteria
  4. Tracks coverage gaps (what was NOT reviewed or areas missed): pattern matches gap or coverage or miss or blind.spot or uncovered
  5. Produces a verification summary separate from the review itself: pattern matches verification.*summary or quality.*report or verify.*output or summary.*finding
  6. The verification step depends on review completion (ordering/dependency): pattern matches after.*review or depends.*on.*review or post.*review or once.*complete or when.*done

Create hints.json as a JSON array of exactly 5 strings, progressive from conceptual to specific:
1. Conceptual nudge: Your skeptic workflow reviews code but never checks its own work. Quality systems verify their own output. Think about what a UAT tester would check after reading the skeptic's findings.
2. Structural hint: You need a new step (or steps) AFTER the aggregation step. This verify-findings section examines the review output, not the original code. It asks: did we cover everything? How severe are these findings? What did we miss?
3. Pattern hint: Add a step like verify_findings or quality_check after aggregate_findings. In it, define severity levels (critical/major/minor), create a checklist of verification criteria, and identify gaps in coverage -- areas the review subagents did not examine.
4. Detailed guidance: After your aggregate_findings step, add a verify_findings step that: (a) classifies each finding by severity (critical/major/minor), (b) runs through a checklist of quality criteria (are findings actionable? do they cite evidence? are severity levels justified?), (c) identifies coverage gaps (what areas of the project were NOT reviewed), and (d) produces a verification summary section.
5. Near-solution: In your skeptic workflow after the aggregation step, add a step named verify_findings (or similar). Inside it: (a) define severity categories and classify aggregated findings into them, (b) list acceptance criteria as a checklist (findings are specific, evidence-based, actionable, severity-justified), (c) track gaps by listing project areas or aspects not covered by any review subagent, (d) write a verification summary. The verification must depend on review completion (place it after aggregation or note the dependency).
  </action>
  <verify>
    <automated>node -e "const s=require('./learn/content/modules/quality-feedback/project/spec.json'); const h=require('./learn/content/modules/quality-feedback/project/hints.json'); console.assert(s.id==='quality-feedback-project'); console.assert(s.moduleId==='quality-feedback'); console.assert(s.artifacts[0].checks.length>=6); console.assert(Array.isArray(h)&&h.length===5); console.log('PASS: spec has',s.artifacts[0].checks.length,'checks, hints has',h.length,'entries')"</automated>
  </verify>
  <done>spec.json has 6 regex checks testing quality/feedback concepts (severity, UAT checklist, gap tracking, verification summary, dependency ordering). hints.json has exactly 5 progressive strings from conceptual to near-solution.</done>
</task>

<task type="auto">
  <name>Task 2: Create 08-mini-project.json lesson</name>
  <files>learn/content/modules/quality-feedback/lessons/08-mini-project.json</files>
  <action>
Create 08-mini-project.json following the EXACT structure of agent-orchestration/lessons/08-mini-project.json. The lesson must have exactly 7 content blocks (matching the Module 4 pattern: 3 text, 1 code, 1 text, 1 project, 1 text).

Lesson fields:
- id: "quality-feedback-mini-project"
- title: "Mini-Project: Quality Verification for Skeptic Reviews"
- lessonNumber: 8
- objective: "Apply quality and feedback loop concepts (UAT-style verification, severity inference, gap tracking) to extend the skeptic workflow with a self-verification section that checks the quality of its own review findings"
- conceptMap: "mini-project"
- successCriteria: "Running --verify --module=quality-feedback shows all checks passing for the skeptic workflow (verify-findings section, severity levels, UAT checklist, gap tracking, verification summary, review dependency). Your skeptic command now verifies its own output quality, completing the quality feedback loop."

Content blocks (7 total, template-first pedagogy, 8 blocks per lesson convention -- the project block counts as the 6th with closing text as 7th):

Block 1 (text): Narrative arc -- recap the skeptic journey across all 5 modules. Module 1 built the command spec. Module 2 added the handler. Module 3 made findings persist. Module 4 added orchestrated parallel reviews. Now Module 5 closes the loop: the skeptic will verify its OWN work. This mirrors real quality systems -- they do not just test code, they test whether their tests were thorough. Introduce the concept: after the review, run a UAT-style verification pass on the findings themselves. Focus on the "quality of quality" meta-concept. Bridge to verification instructions.

Block 2 (text): Explain verify/hint commands and git safety net (same pattern as Module 4). Mention that this modifies the same skeptic.md file. Include the git checkout command. Bridge to template.

Block 3 (text): Introduce the template -- a verify-findings section that goes AFTER the aggregation step from Module 4. Explain the three components: severity classification, acceptance checklist, and coverage gap tracking. Tell learner to press C to copy, then integrate into their workflow. Bridge to code block.

Block 4 (code, language: xml): Template XML showing a verify_findings step. The step should:
- Classify findings by severity (critical/major/minor) with criteria for each level
- Run through an acceptance checklist (are findings specific? evidence-based? actionable?)
- Identify coverage gaps (project areas not examined by any review subagent)
- Produce a verification summary section
- Be clearly marked as depending on review/aggregation completion (post-review step)
Keep it a starting template -- enough structure to pass checks but room for learner customization.

Block 5 (text): Customization encouragement. List the 6 structural requirements that verification checks for (matching spec.json checks). Emphasize that specific checklist items, severity thresholds, gap categories, and summary format are the learner's creative choices. Verification checks structure, not content.

Block 6 (project): Task description, deliverables (updated workflow with verify-findings section, running skeptic produces verified findings with severity and gap tracking), verifyCommand: "node learn/bin/gsd-learn.cjs --verify --module=quality-feedback", hintCommand: "node learn/bin/gsd-learn.cjs --hint --module=quality-feedback".

Block 7 (text): Module completion celebration. Recap the full journey: from quality lifecycle overview through verify-work, UAT, skeptic reviews, debug workflows, gap closure, milestone audits, synthesis -- and now applying it. The learner has built a skeptic that not only reviews code across multiple dimensions in parallel but verifies the quality of its own output. This is the complete feedback loop: build, review, verify the review, close gaps. Bridge to: Module 5 complete.

IMPORTANT: Each text block must have "value", "focus", and "bridge" fields. Code block has "value", "language", "focus". Project block has "task", "deliverables", "verifyCommand", "hintCommand", "focus", "bridge".
  </action>
  <verify>
    <automated>node -e "const l=require('./learn/content/modules/quality-feedback/lessons/08-mini-project.json'); console.assert(l.id==='quality-feedback-mini-project'); console.assert(l.lessonNumber===8); console.assert(l.content.length===7); const types=l.content.map(b=>b.type); console.assert(types.filter(t=>t==='text').length===4); console.assert(types.filter(t=>t==='code').length===1); console.assert(types.filter(t=>t==='project').length===1); console.assert(l.content.find(b=>b.type==='project').verifyCommand.includes('quality-feedback')); console.log('PASS: lesson has',l.content.length,'blocks, types:',types.join(','))"</automated>
  </verify>
  <done>08-mini-project.json loads correctly with 7 content blocks (4 text, 1 code, 1 project), references quality-feedback module in verify/hint commands, conceptMap is "mini-project", and lesson tells the complete 5-module skeptic story.</done>
</task>

</tasks>

<verification>
1. All three files exist and parse as valid JSON:
   node -e "require('./learn/content/modules/quality-feedback/project/spec.json'); require('./learn/content/modules/quality-feedback/project/hints.json'); require('./learn/content/modules/quality-feedback/lessons/08-mini-project.json'); console.log('All files parse OK')"

2. Spec checks test quality concepts (not just file existence):
   node -e "const s=require('./learn/content/modules/quality-feedback/project/spec.json'); s.artifacts[0].checks.forEach(c=>console.log(c.description))"

3. Lesson references correct module in commands:
   node -e "const l=require('./learn/content/modules/quality-feedback/lessons/08-mini-project.json'); const p=l.content.find(b=>b.type==='project'); console.log(p.verifyCommand); console.log(p.hintCommand)"
</verification>

<success_criteria>
- spec.json has 6+ regex checks exercising quality/feedback concepts (severity, UAT, gaps)
- hints.json has exactly 5 progressive hints from conceptual to near-solution
- 08-mini-project.json has 7 content blocks with template-first pedagogy
- Lesson tells the 5-module skeptic extension story
- verify/hint commands reference --module=quality-feedback
- All files parse as valid JSON
</success_criteria>

<output>
After completion, create `.planning/phases/24-mini-project/24-01-SUMMARY.md`
</output>
