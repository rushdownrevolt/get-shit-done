---
phase: 23-lesson-content
plan: 02
type: execute
wave: 2
depends_on: [23-01]
files_modified:
  - learn/content/modules/quality-feedback/lessons/04-debug.json
  - learn/content/modules/quality-feedback/lessons/05-gap-closure.json
  - learn/content/modules/quality-feedback/lessons/06-milestone-audit.json
  - learn/content/modules/quality-feedback/lessons/07-synthesis.json
autonomous: true
requirements: [LESS-04, LESS-05, LESS-06, LESS-07]

must_haves:
  truths:
    - "Lesson 4 teaches debug workflows: systematic debugging with hypothesis testing, persistent DEBUG.md state, parallel debug agents"
    - "Lesson 5 teaches gap closure: diagnosis -> plan --gaps -> execute --gaps-only cycle"
    - "Lesson 6 teaches milestone audit: audit-milestone aggregation, plan-milestone-gaps phase creation, completion gates"
    - "Lesson 7 synthesizes the full quality lifecycle: connecting all quality loops into the complete GSD feedback system"
    - "All four lessons load with correct lessonNumber values 4-7 and conceptMap values matching module.json"
    - "Code blocks contain real snippets from GSD workflow files"
  artifacts:
    - path: "learn/content/modules/quality-feedback/lessons/04-debug.json"
      provides: "Lesson 4 - debug workflows"
      contains: "hypothesis"
    - path: "learn/content/modules/quality-feedback/lessons/05-gap-closure.json"
      provides: "Lesson 5 - gap closure cycle"
      contains: "gaps"
    - path: "learn/content/modules/quality-feedback/lessons/06-milestone-audit.json"
      provides: "Lesson 6 - milestone audit"
      contains: "milestone"
    - path: "learn/content/modules/quality-feedback/lessons/07-synthesis.json"
      provides: "Lesson 7 - quality lifecycle synthesis"
      contains: "lifecycle"
  key_links:
    - from: "04-debug.json"
      to: "module.json sectionMap"
      via: "conceptMap field"
      pattern: "debug"
    - from: "05-gap-closure.json"
      to: "module.json sectionMap"
      via: "conceptMap field"
      pattern: "gap-closure"
    - from: "06-milestone-audit.json"
      to: "module.json sectionMap"
      via: "conceptMap field"
      pattern: "milestone-audit"
    - from: "07-synthesis.json"
      to: "module.json sectionMap"
      via: "conceptMap field"
      pattern: "synthesis"
---

<objective>
Create lessons 4-7 for Module 5 (Quality & Feedback Loops) covering debug workflows, gap closure, milestone audit, and full lifecycle synthesis.

Purpose: These four lessons complete the Module 5 curriculum -- from systematic debugging through the gap closure cycle, milestone-level auditing, and finally synthesizing everything into the complete GSD quality feedback system. Each lesson uses real code from GSD's workflow files.
Output: Four lesson JSON files in learn/content/modules/quality-feedback/lessons/
</objective>

<execution_context>
@C:/Users/18182/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/18182/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-lesson-content/23-01-SUMMARY.md

@learn/content/modules/quality-feedback/module.json
@learn/content/modules/quality-feedback/concept-map.txt
@learn/content/modules/quality-feedback/lessons/01-overview.json
@learn/content/modules/quality-feedback/lessons/03-skeptic.json

<interfaces>
<!-- Lesson JSON format (established by Modules 1-4): -->
<!--
{
  "id": "string",           // matches sectionMap key in module.json
  "title": "string",
  "lessonNumber": number,
  "objective": "string",
  "content": [
    { "type": "text", "value": "string", "focus": "string", "bridge": "string" },
    { "type": "code", "language": "markdown|text|yaml", "value": "string", "highlight": [line_numbers] }
  ],
  "conceptMap": "string",   // must match sectionMap key in module.json
  "successCriteria": "string"
}
-->

<!-- module.json sectionMap keys for lessons 4-7: "debug", "gap-closure", "milestone-audit", "synthesis" -->

<!-- GSD source files to extract real code snippets from: -->
<!-- C:/Users/18182/.claude/get-shit-done/workflows/diagnose-issues.md — parallel debug agent orchestration, gap parsing, root cause diagnosis -->
<!-- C:/Users/18182/.claude/get-shit-done/templates/DEBUG.md — persistent debug session format: hypothesis, symptoms, eliminated, evidence, resolution -->
<!-- C:/Users/18182/.claude/get-shit-done/templates/debug-subagent-prompt.md — individual debug agent behavior -->
<!-- C:/Users/18182/.claude/get-shit-done/workflows/plan-phase.md — gap_closure_mode section showing plan --gaps flow -->
<!-- C:/Users/18182/.claude/get-shit-done/workflows/audit-milestone.md — milestone verification aggregation, integration checking -->
<!-- C:/Users/18182/.claude/get-shit-done/workflows/plan-milestone-gaps.md — creating phases from audit gaps -->
<!-- C:/Users/18182/.claude/get-shit-done/workflows/verify-phase.md — goal-backward verification (for synthesis context) -->
<!-- C:/Users/18182/.claude/get-shit-done/workflows/verify-work.md — UAT flow (for synthesis context) -->
<!-- C:/Users/18182/.claude/get-shit-done/references/checkpoints.md — checkpoint reference (for debug context) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Lesson 4 - Debug Workflows</name>
  <files>learn/content/modules/quality-feedback/lessons/04-debug.json</files>
  <action>
Create lesson JSON file with:
- id: "debug"
- title: "Debug Workflows"
- lessonNumber: 4
- conceptMap: "debug"
- successCriteria: describing what the learner understands about systematic debugging with hypothesis testing and persistent state

Content blocks (target 8 blocks: 5 text, 3 code):

**Block 1 (text):** Bridge from Lesson 3 -- skeptic reviews catch structural problems, but sometimes bugs get through to testing. When verify-work finds issues, GSD does not just guess at fixes. It uses a systematic debug workflow: diagnose-issues spawns parallel debug agents, each investigating one gap with hypothesis testing and persistent state. Focus: "Systematic debugging, not guessing." Bridge to seeing the debug orchestrator.

**Block 2 (code, language: text):** Show the diagnose-issues.md purpose and core_principle tags. Read C:/Users/18182/.claude/get-shit-done/workflows/diagnose-issues.md and extract the `<purpose>` section (orchestrate parallel debug agents to investigate UAT gaps) and the `<core_principle>` section (diagnose before planning fixes, with the symptom vs root cause comparison). Highlight the "Without diagnosis" vs "With diagnosis" comparison lines.

**Block 3 (text):** Explain the debug orchestration flow: diagnose-issues reads UAT.md gaps, spawns one debug agent per gap (in parallel), each agent investigates autonomously with symptoms pre-filled from UAT. The orchestrator stays lean -- it parses gaps, spawns agents, collects results, and updates UAT.md with diagnoses. Each debug agent has its own persistent state file in .planning/debug/. Focus: "One agent per gap, parallel investigation, orchestrator stays lean."

**Block 4 (code, language: text):** Show the DEBUG.md template format. Read C:/Users/18182/.claude/get-shit-done/templates/DEBUG.md and extract the file template showing: frontmatter (status states: gathering | investigating | fixing | verifying | resolved), Current Focus section (hypothesis, test, expecting, next_action), Symptoms section (expected, actual, errors, reproduction), Eliminated section (append-only hypotheses that were wrong), and Evidence section (append-only facts discovered). Highlight the status field options and the hypothesis/test/expecting fields.

**Block 5 (text):** Explain the hypothesis-testing methodology: the debug agent states a hypothesis ("useEffect missing dependency"), designs a test to confirm/deny it, records what result would mean, then executes. If the hypothesis is wrong, it goes into the Eliminated section (append-only -- prevents re-investigating the same dead end after /clear). If right, it goes into Resolution with root_cause and fix. This structured approach prevents the "change random things and hope" anti-pattern. Focus: "State hypothesis, test, record -- never re-investigate eliminated theories."

**Block 6 (code, language: text):** Show the gap parsing step from diagnose-issues.md. Read C:/Users/18182/.claude/get-shit-done/workflows/diagnose-issues.md and extract the parse_gaps step showing how gaps are extracted from UAT.md (the YAML format with truth, status, reason, severity, test number, artifacts, missing). Then show the report_plan step showing the diagnosis plan table format. Highlight the truth and severity fields.

**Block 7 (text):** Explain persistence across sessions: DEBUG.md files survive /clear. If a debug session is interrupted, the agent picks up from the Current Focus section (which is always overwritten to reflect NOW), reads Eliminated to avoid repeating work, and reads Evidence for accumulated facts. The Symptoms section is written once during gathering and becomes immutable -- the original bug description never changes even as understanding evolves. Focus: "Debug state persists -- investigation survives session boundaries."

**Block 8 (text):** Bridge to next lesson: debug agents find root causes for individual gaps. But how does the system turn those diagnoses into organized fix plans and close the loop? Next, we will see the gap closure cycle that takes diagnosed gaps all the way to targeted fixes.

**IMPORTANT:** Read each source file and extract real snippets. Escape properly for JSON.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const lesson = JSON.parse(fs.readFileSync('learn/content/modules/quality-feedback/lessons/04-debug.json', 'utf8')); console.log('id:', lesson.id); console.log('lessonNumber:', lesson.lessonNumber); console.log('blocks:', lesson.content.length); console.log('conceptMap:', lesson.conceptMap); const codeBlocks = lesson.content.filter(b => b.type === 'code'); console.log('codeBlocks:', codeBlocks.length); const hasHypothesis = lesson.content.some(b => b.value && b.value.includes('hypothesis')); console.log('hasHypothesisRef:', hasHypothesis); if (lesson.lessonNumber !== 4) throw new Error('Wrong lesson number'); if (lesson.conceptMap !== 'debug') throw new Error('Wrong conceptMap'); if (lesson.content.length < 7) throw new Error('Too few blocks'); if (codeBlocks.length < 3) throw new Error('Too few code blocks');"</automated>
  </verify>
  <done>Lesson 4 JSON exists with lessonNumber 4, conceptMap "debug", 8 blocks covering diagnose-issues orchestration, DEBUG.md persistent state, hypothesis testing, and gap parsing with real workflow snippets</done>
</task>

<task type="auto">
  <name>Task 2: Create Lesson 5 - Gap Closure</name>
  <files>learn/content/modules/quality-feedback/lessons/05-gap-closure.json</files>
  <action>
Create lesson JSON file with:
- id: "gap-closure"
- title: "Gap Closure"
- lessonNumber: 5
- conceptMap: "gap-closure"
- successCriteria: describing what the learner understands about the diagnosis -> plan --gaps -> execute --gaps-only cycle

Content blocks (target 8 blocks: 5 text, 3 code):

**Block 1 (text):** Bridge from Lesson 4 -- debug agents found root causes and updated UAT.md with diagnoses. Now what? The gap closure cycle turns those diagnoses into targeted fix plans. The flow is: diagnose-issues updates UAT.md status to "diagnosed" -> plan-phase --gaps reads diagnosed gaps -> creates fix plans with gap_closure: true -> execute-phase --gaps-only runs just those plans. Focus: "From diagnosis to targeted fix, automatically." Bridge to seeing the gap closure mechanism.

**Block 2 (code, language: text):** Show the gap_closure_mode from plan-phase.md. Read C:/Users/18182/.claude/get-shit-done/workflows/plan-phase.md and extract the `<gap_closure_mode>` section content. Show the steps: find gap sources (VERIFICATION.md or UAT.md with diagnosed status), parse gaps, load existing SUMMARYs, find next plan number, group gaps into plans, create gap closure tasks, assign waves. Highlight the step descriptions showing the flow from gap sources to plan creation.

**Block 3 (text):** Explain what makes gap closure targeted: each gap has a truth (what should be TRUE), a reason (what went wrong), artifacts (files with issues), and missing (things to add/fix). The planner does not guess -- it reads the diagnosed root cause from UAT.md and creates tasks that address the specific missing items. The gap_closure: true frontmatter flag tracks that these plans exist to close gaps, not add new features. Focus: "Gaps carry their diagnosis -- planners do not re-investigate."

**Block 4 (code, language: text):** Show the gap closure task template from plan-phase.md. Read the gap_closure_mode section of C:/Users/18182/.claude/get-shit-done/workflows/plan-phase.md and extract the XML template for gap closure tasks showing the task structure with fix_description, files from artifact.path, action with gap.missing items, reference to existing code from SUMMARYs, gap reason, verify, and done fields. Also show the PLAN.md frontmatter for gap closure plans with gap_closure: true flag. Highlight the gap_closure: true flag and the gap.missing reference.

**Block 5 (text):** Explain the execute --gaps-only flow: when running execute-phase with --gaps-only, only plans with gap_closure: true are executed. This prevents re-running already-complete plans. The gap closure plans get sequential plan numbers after existing plans (if plans 01-03 exist, gap plans start at 04), their own wave assignments based on dependencies, and their own verification. After execution, verify-work can re-test to confirm gaps are closed. Focus: "Gaps-only execution -- surgical fixes without re-running everything."

**Block 6 (code, language: text):** Show the gap source detection from plan-phase.md gap_closure_mode. Read C:/Users/18182/.claude/get-shit-done/workflows/plan-phase.md and extract the "find gap sources" code block showing how it checks for VERIFICATION.md (code verification gaps) and UAT.md with "status: diagnosed" (user testing gaps). Then show the "group gaps into plans" guidance (same artifact, same concern, dependency order). Highlight the ls and grep commands that find gap sources.

**Block 7 (text):** Explain the re-verification loop: after gap closure plans execute, the system can run verify-work again. If new issues are found, the cycle repeats: diagnose -> plan --gaps -> execute --gaps-only -> verify again. Each iteration is targeted and smaller than the last. In practice, most gaps close in one cycle because root cause diagnosis prevents symptom-chasing. The cycle converges because each iteration addresses specific, diagnosed causes. Focus: "The cycle converges -- each pass is smaller and more targeted."

**Block 8 (text):** Bridge to next lesson: gap closure fixes problems within a single phase. But what about gaps that span multiple phases? What about ensuring an entire milestone hangs together? Next, we will see how audit-milestone checks the big picture and plan-milestone-gaps creates cross-phase fixes.

**IMPORTANT:** Read plan-phase.md and extract real snippets from the gap_closure_mode section. Escape properly for JSON.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const lesson = JSON.parse(fs.readFileSync('learn/content/modules/quality-feedback/lessons/05-gap-closure.json', 'utf8')); console.log('id:', lesson.id); console.log('lessonNumber:', lesson.lessonNumber); console.log('blocks:', lesson.content.length); console.log('conceptMap:', lesson.conceptMap); const codeBlocks = lesson.content.filter(b => b.type === 'code'); console.log('codeBlocks:', codeBlocks.length); const hasGaps = lesson.content.some(b => b.value && b.value.includes('gap')); console.log('hasGapsRef:', hasGaps); if (lesson.lessonNumber !== 5) throw new Error('Wrong lesson number'); if (lesson.conceptMap !== 'gap-closure') throw new Error('Wrong conceptMap'); if (lesson.content.length < 7) throw new Error('Too few blocks'); if (codeBlocks.length < 3) throw new Error('Too few code blocks');"</automated>
  </verify>
  <done>Lesson 5 JSON exists with lessonNumber 5, conceptMap "gap-closure", 8 blocks covering plan-phase --gaps flow, gap closure task creation, gaps-only execution, and re-verification convergence with real plan-phase.md snippets</done>
</task>

<task type="auto">
  <name>Task 3: Create Lessons 6-7 - Milestone Audit and Synthesis</name>
  <files>
    learn/content/modules/quality-feedback/lessons/06-milestone-audit.json
    learn/content/modules/quality-feedback/lessons/07-synthesis.json
  </files>
  <action>
Create TWO lesson JSON files:

### Lesson 6: Milestone Audit

- id: "milestone-audit"
- title: "Milestone Audit"
- lessonNumber: 6
- conceptMap: "milestone-audit"
- successCriteria: describing what the learner understands about milestone-level verification, cross-phase integration checking, and completion gates

Content blocks (target 8 blocks: 5 text, 3 code):

**Block 1 (text):** Bridge from Lesson 5 -- gap closure fixes problems within a single phase. But a milestone spans multiple phases. Phase 1 might create a database schema, Phase 2 an API, Phase 3 a UI -- each phase can pass verification individually while the cross-phase wiring is broken. GSD checks the big picture with /gsd:audit-milestone, which aggregates phase verifications and checks cross-phase integration. Focus: "Individual phase passes do not guarantee milestone success." Bridge to seeing the audit process.

**Block 2 (code, language: text):** Show the audit-milestone.md purpose tag and the first steps. Read C:/Users/18182/.claude/get-shit-done/workflows/audit-milestone.md and extract the `<purpose>` section (verify milestone definition of done by aggregating phase verifications, checking cross-phase integration, assessing requirements coverage) and Step 1 (Determine Milestone Scope) showing how it identifies phases, extracts definition of done, and extracts requirements. Highlight the phrase about "aggregating phase verifications" and "cross-phase integration."

**Block 3 (text):** Explain the three-layer audit: (1) Phase verification aggregation -- read every VERIFICATION.md, aggregate status/gaps/anti-patterns/requirements coverage. If any phase is unverified, that is a blocker. (2) Integration checking -- spawn a gsd-integration-checker subagent that examines cross-phase wiring (does the API use the schema types? does the UI call the right API endpoints?). (3) Requirements coverage -- compare actual verified requirements against REQUIREMENTS.md to find anything unsatisfied. Focus: "Three layers: phase-level, integration-level, requirement-level."

**Block 4 (code, language: text):** Show the "Read All Phase Verifications" step from audit-milestone.md. Read C:/Users/18182/.claude/get-shit-done/workflows/audit-milestone.md and extract Step 2 showing what is extracted from each VERIFICATION.md (status, critical gaps, non-critical gaps, anti-patterns, requirements coverage) and the note about missing VERIFICATION.md being a blocker. Then show the "Spawn Integration Checker" step header and its purpose. Highlight the extraction list and the "unverified phase" blocker note.

**Block 5 (text):** Explain completion gates: the audit produces a structured result. If critical gaps exist (requirements unsatisfied, integration broken, unverified phases), the milestone CANNOT be completed -- these are gates. Non-critical items (tech debt, minor deferred gaps) are recorded but do not block completion. This prevents shipping a milestone that looks done but has fundamental gaps. The gate is binary: all critical items resolved or milestone stays open. Focus: "Gates enforce completion quality -- no shipping with critical gaps."

**Block 6 (code, language: text):** Show the plan-milestone-gaps.md purpose and gap loading. Read C:/Users/18182/.claude/get-shit-done/workflows/plan-milestone-gaps.md and extract the `<purpose>` section (create all phases necessary to close audit gaps, reads MILESTONE-AUDIT.md, groups gaps into logical phases) and the Load Audit Results step showing the three gap categories (requirements, integration, flows). Also show the Prioritize Gaps step with the priority table (must = blocks milestone, should = recommended, nice = ask user). Highlight the priority table.

**Block 7 (text):** Explain how plan-milestone-gaps closes the milestone loop: it reads audit gaps, groups them by priority and logical concern, creates new phase entries in ROADMAP.md, and offers to plan each phase. Must-priority gaps get phases automatically. Nice-priority gaps ask the user. This creates a clean path from "audit found problems" to "here are the specific phases that fix them." The same execute -> verify loop runs on these new phases. Focus: "Audit gaps become phases -- the same lifecycle that built the milestone now fixes it."

**Block 8 (text):** Bridge to final lesson: milestone audit completes the quality picture at the highest level. In the next lesson, we will step back and see how ALL these quality mechanisms -- verify-work, skeptic reviews, debug workflows, gap closure, and milestone audit -- compose into a single coherent feedback system.

### Lesson 7: Synthesis

- id: "synthesis"
- title: "The Quality Feedback System"
- lessonNumber: 7
- conceptMap: "synthesis"
- successCriteria: describing what the learner can explain about the complete quality feedback system and how all quality loops connect

Content blocks (target 8 blocks: 5 text, 3 code):

**Block 1 (text):** This final lesson connects all the quality pieces. You have learned: the quality lifecycle (Lesson 1), verify-work and UAT (Lesson 2), skeptic reviews via goal-backward analysis (Lesson 3), debug workflows (Lesson 4), gap closure (Lesson 5), and milestone audit (Lesson 6). Now we will see how these compose into a unified quality feedback system that operates at every level of GSD. Focus: "From parts to whole -- the quality system."

**Block 2 (code, language: text):** Show the complete quality feedback system as an ASCII diagram. Create a clean diagram showing all quality touchpoints in a typical GSD lifecycle:
```
plan-phase                execute-phase              verify-work
  |                          |                          |
  +-- plan-checker           +-- task commits           +-- conversational tests
  |   (pre-execution         |   (atomic, verifiable)   |   (UAT.md persistent)
  |    skeptic review)       |                          |
  v                          v                          v
verify-phase ---------> diagnose-issues ---------> plan-phase --gaps
  |                          |                          |
  +-- goal-backward          +-- parallel debug         +-- gap closure plans
  |   (truths, artifacts,    |   agents                 |   (targeted fixes)
  |    key_links)            |   (DEBUG.md persistent)  |
  v                          v                          v
audit-milestone ------> plan-milestone-gaps         execute --gaps-only
  |                          |                          |
  +-- phase aggregation      +-- priority grouping      +-- surgical execution
  +-- integration check      +-- new phases created     +-- re-verify
  +-- requirements gate
```
Highlight the arrows between major stages.

**Block 3 (text):** Walk through a concrete example: building a commenting feature across 3 phases. Phase 1 creates the schema and API. Phase 2 creates the UI. Phase 3 adds real-time updates. After Phase 2 executes, verify-work finds: "comments appear but don't refresh until page reload." UAT.md records this as a major gap. diagnose-issues spawns a debug agent that finds "useEffect missing comment subscription dependency." plan-phase --gaps creates a fix plan. execute --gaps-only applies the fix. verify-work re-tests: comments now refresh. Focus: "Concrete example: from gap to fix in one automated cycle."

**Block 4 (code, language: text):** Show what the gap looks like in UAT.md and then in the gap closure plan. Create a realistic two-part snippet: first the UAT.md gap entry (truth, status: failed, reason with user quote, severity: major, test number), then the corresponding gap closure PLAN.md task (referencing the diagnosed root cause, specific file to fix, action describing the useEffect fix, verify command). Highlight the truth -> action connection across the two artifacts.

**Block 5 (text):** Explain the three feedback loops operating at different scales. Loop 1 (task level): each task has a verify block that runs after implementation -- instant feedback. Loop 2 (phase level): verify-work -> diagnose -> plan --gaps -> execute --gaps-only -- catches integration issues within a phase. Loop 3 (milestone level): audit-milestone -> plan-milestone-gaps -> execute new phases -- catches cross-phase gaps. Each loop is wider but less frequent. Together they catch problems at every granularity. Focus: "Three loops at three scales -- nothing falls through."

**Block 6 (code, language: text):** Show the verify-phase must_haves verification in action. Read C:/Users/18182/.claude/get-shit-done/workflows/verify-phase.md and extract the section showing how it checks each level: truths (observable behavior confirmed), artifacts (file exists, has expected exports/content), key_links (grep for connection patterns in actual source). Show a concrete example of checking one key_link with a grep pattern. Highlight the grep-based evidence checking.

**Block 7 (text):** Explain why this system works: it is not a single monolithic quality check -- it is layered, persistent, and evidence-based. Persistent because UAT.md, DEBUG.md, and VERIFICATION.md survive /clear. Evidence-based because key_links use grep patterns on actual source, not trust. Layered because plan-checker catches plan issues, verify-phase catches implementation issues, verify-work catches user-visible issues, and audit-milestone catches integration issues. Each layer catches different failure modes. Focus: "Persistent, evidence-based, layered -- not a single quality gate."

**Block 8 (text):** Final synthesis: GSD's quality system embodies one principle -- close the loop. Every verification feeds back into planning. Every gap drives targeted fixes. Every fix gets re-verified. This is not a waterfall quality gate at the end; it is continuous feedback woven into every workflow. In the next step (the mini-project), you will apply these quality concepts by building your own verification workflow.

**IMPORTANT:** Read workflow source files for real snippets where indicated. The ASCII diagram is a teaching illustration showing connections between real GSD workflows. Escape properly for JSON.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); ['04-debug', '05-gap-closure', '06-milestone-audit', '07-synthesis'].forEach((name, i) => { const lesson = JSON.parse(fs.readFileSync('learn/content/modules/quality-feedback/lessons/' + name + '.json', 'utf8')); console.log(name, '- id:', lesson.id, 'num:', lesson.lessonNumber, 'blocks:', lesson.content.length, 'map:', lesson.conceptMap); const expected = [['debug', 4, 'debug'], ['gap-closure', 5, 'gap-closure'], ['milestone-audit', 6, 'milestone-audit'], ['synthesis', 7, 'synthesis']][i]; if (lesson.id !== expected[0]) throw new Error('Wrong id for ' + name); if (lesson.lessonNumber !== expected[1]) throw new Error('Wrong number for ' + name); if (lesson.conceptMap !== expected[2]) throw new Error('Wrong conceptMap for ' + name); if (lesson.content.length < 7) throw new Error('Too few blocks for ' + name); });"</automated>
  </verify>
  <done>Lessons 4-7 JSON files exist with correct lessonNumbers (4, 5, 6, 7), conceptMaps ("debug", "gap-closure", "milestone-audit", "synthesis"), 8 blocks each covering debug workflows, gap closure, milestone audit, and quality lifecycle synthesis with real workflow snippets</done>
</task>

</tasks>

<verification>
- All four lesson JSON files are valid and parseable
- lessonNumbers are 4, 5, 6, 7 respectively
- conceptMap values match module.json sectionMap keys: "debug", "gap-closure", "milestone-audit", "synthesis"
- Code blocks contain real GSD workflow content (not invented examples)
- Lessons flow: debug workflows -> gap closure -> milestone audit -> synthesis
- Each lesson bridges to the next; Lesson 7 bridges to mini-project
- All 7 lessons together cover the complete quality feedback lifecycle
- 8 blocks per lesson (5 text, 3 code) following established pacing pattern
</verification>

<success_criteria>
- Lesson 4 teaches debug workflows with real diagnose-issues.md and DEBUG.md snippets
- Lesson 5 teaches gap closure with real plan-phase.md gap_closure_mode snippets
- Lesson 6 teaches milestone audit with real audit-milestone.md and plan-milestone-gaps.md snippets
- Lesson 7 synthesizes the full quality lifecycle with a concrete example and system diagram
- All four lessons load correctly in the learning shell
- Content blocks contain actual GSD source code, not invented examples
</success_criteria>

<output>
After completion, create `.planning/phases/23-lesson-content/23-02-SUMMARY.md`
</output>
