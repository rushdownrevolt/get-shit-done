---
phase: 09-navigation-architecture-progress-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - learn/lib/progress.cjs
  - learn/tests/progress.test.cjs
autonomous: true
requirements:
  - WELC-02
  - NAV-01

must_haves:
  truths:
    - "isFirstRun returns true when no module has started (empty modules map)"
    - "isFirstRun returns false when any module has started:true"
    - "v2 progress data migrates to v3 with completed:false added to each module entry"
    - "v3 data passes through migration unchanged (idempotent)"
    - "Chained migration v1->v2->v3 works in a single loadProgress call"
    - "Migration persists to disk immediately"
  artifacts:
    - path: "learn/lib/progress.cjs"
      provides: "isFirstRun, migrateV2toV3, updated loadProgress with chained migration, DEFAULT_PROGRESS v3"
      exports: ["loadProgress", "saveProgress", "migrateV1toV2", "migrateV2toV3", "isFirstRun"]
    - path: "learn/tests/progress.test.cjs"
      provides: "Test cases for migrateV2toV3, isFirstRun, chained migration, round-trip with v3"
  key_links:
    - from: "learn/lib/progress.cjs"
      to: "loadProgress"
      via: "chained migration in loadProgress"
      pattern: "migrateV2toV3"
---

<objective>
Upgrade progress schema from v2 to v3 and add first-run detection.

Purpose: The v3 schema adds a `completed` boolean to each module entry, enabling the module picker (Phase 10) to show completion state. The `isFirstRun()` helper enables distinguishing first-time users from returning users, which Phase 10 uses to decide between welcome screen and resume flow.

Output: Updated `progress.cjs` with v3 migration, `isFirstRun()`, and comprehensive test coverage.
</objective>

<execution_context>
@C:/Users/18182/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/18182/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-navigation-architecture-progress-foundation/09-CONTEXT.md
@.planning/phases/09-navigation-architecture-progress-foundation/09-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From learn/lib/progress.cjs (current exports):
```javascript
module.exports = { loadProgress, saveProgress, migrateV1toV2 };

const DEFAULT_PROGRESS = {
  version: 2,
  currentModule: null,
  currentLesson: 0,
  modules: {},
};

function migrateV1toV2(progress) { /* v1->v2 migration */ }
function loadProgress(cwd) { /* reads file, auto-migrates v1->v2, returns progress */ }
function saveProgress(cwd, progress) { /* writes JSON to .planning/learn/progress.json */ }
```

Current v2 schema on disk:
```json
{
  "version": 2,
  "currentModule": "gsd-commands",
  "currentLesson": 0,
  "modules": {}
}
```

Target v3 schema:
```json
{
  "version": 3,
  "currentModule": "gsd-commands",
  "currentLesson": 3,
  "modules": {
    "gsd-commands": { "currentLesson": 3, "started": true, "completed": false }
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add v2-to-v3 migration and isFirstRun with tests</name>
  <files>learn/lib/progress.cjs, learn/tests/progress.test.cjs</files>
  <behavior>
    - migrateV2toV3: v2 data with module entries gets `completed: false` added to each entry, version becomes 3
    - migrateV2toV3: v2 data with empty modules map migrates cleanly (version 3, modules still empty)
    - migrateV2toV3: v3 data passes through unchanged (idempotent)
    - migrateV2toV3: preserves all existing fields (currentLesson, started) in module entries
    - isFirstRun: returns true for empty modules map `{}`
    - isFirstRun: returns true when all module entries have `started: false`
    - isFirstRun: returns false when any module entry has `started: true`
    - loadProgress with v2 file on disk: auto-migrates to v3, persists to disk, returns v3 object
    - loadProgress with v1 file on disk: chains v1->v2->v3 migration, persists v3 to disk
    - loadProgress with no file: returns v3 DEFAULT_PROGRESS (version: 3)
    - round-trip: saveProgress(v3) then loadProgress returns same v3 data
  </behavior>
  <action>
    Write tests FIRST in learn/tests/progress.test.cjs (new describe blocks for migrateV2toV3, isFirstRun, chained migration). Run tests -- they must FAIL (RED).

    Then implement in learn/lib/progress.cjs:

    1. Update DEFAULT_PROGRESS to version: 3.

    2. Add `migrateV2toV3(progress)` function:
       - If `progress.version >= 3`, return unchanged
       - Create new object with version: 3, copy currentModule/currentLesson
       - For each entry in progress.modules, spread existing fields and add `completed: modData.completed || false`
       - Return migrated object

    3. Add `isFirstRun(progress)` function:
       - Return true if `Object.values(progress.modules || {})` is empty OR none have `started === true`
       - This is a pure function, no side effects

    4. Update `loadProgress()`:
       - Chain migrations: after v1->v2 check, add v2->v3 check (if version < 3, migrate and saveProgress)
       - The catch block returns `{ ...DEFAULT_PROGRESS }` which now has version: 3

    5. Update `module.exports` to include `migrateV2toV3` and `isFirstRun`.

    Run tests -- they must PASS (GREEN).

    IMPORTANT: The existing v1->v2 migration tests and loadProgress tests must still pass. Do not break them. The existing tests check for version: 2 in some places -- those tests are checking the migrateV1toV2 function directly, which is correct (it produces v2). But the loadProgress tests that check `result.version` when file is missing or invalid will need to expect version: 3 now since DEFAULT_PROGRESS changes.

    Update these existing tests to expect version: 3:
    - "returns v2 default object when file missing" -> expect version 3
    - "returns default object when file contains invalid JSON" -> expect version 3
    - "returns default object when progress file has missing fields" -> expect version 3
    - "auto-migrates v1 file to v2 and writes back" -> now chains to v3, expect version 3 and completed: false in module entry
  </action>
  <verify>
    <automated>node --test learn/tests/progress.test.cjs</automated>
  </verify>
  <done>
    - migrateV2toV3 exported and tested (migration, idempotency, field preservation)
    - isFirstRun exported and tested (empty map, all unstarted, some started)
    - loadProgress returns version 3 in all cases (missing file, invalid file, v1 file, v2 file, v3 file)
    - Chained v1->v2->v3 migration works and persists to disk
    - All existing tests updated and passing
  </done>
</task>

</tasks>

<verification>
```bash
node --test learn/tests/progress.test.cjs
```
All tests pass, including new v3 migration, isFirstRun, and chained migration tests.
</verification>

<success_criteria>
- `isFirstRun({modules: {}})` returns true (WELC-02)
- `isFirstRun({modules: {'gsd-commands': {started: true}}})` returns false (WELC-02)
- v2 data on disk auto-migrates to v3 on loadProgress (NAV-01)
- v1 data chains through v1->v2->v3 without data loss (NAV-01)
- DEFAULT_PROGRESS is version 3 (NAV-01)
</success_criteria>

<output>
After completion, create `.planning/phases/09-navigation-architecture-progress-foundation/09-01-SUMMARY.md`
</output>
