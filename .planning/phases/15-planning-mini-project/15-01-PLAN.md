---
phase: 15-planning-mini-project
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - learn/content/modules/planning-state/lessons/07-mini-project.json
  - learn/content/modules/planning-state/project/spec.json
autonomous: true
requirements: [MINI-01, MINI-02, MINI-03, MINI-04, MINI-06]

must_haves:
  truths:
    - "Mini-project lesson teaches the artifact-persistence pattern and provides a copyable template"
    - "Structural verification checks the skeptic workflow for read-previous and write-new patterns"
    - "Structural verification checks SKEPTIC-REVIEW.md for expected sections (date, findings)"
    - "Learner can run --verify --module=planning-state and get pass/fail feedback on all artifacts"
  artifacts:
    - path: "learn/content/modules/planning-state/lessons/07-mini-project.json"
      provides: "Mini-project lesson with artifact-persistence template"
      contains: "mini-project"
    - path: "learn/content/modules/planning-state/project/spec.json"
      provides: "Structural verification spec for mini-project artifacts"
      contains: "planning-state-project"
  key_links:
    - from: "learn/content/modules/planning-state/project/spec.json"
      to: "learn/lib/verifier.cjs"
      via: "runVerification loads spec.json and checks artifact patterns"
      pattern: "checks.*pattern"
    - from: "learn/content/modules/planning-state/lessons/07-mini-project.json"
      to: "learn/content/modules/planning-state/project/spec.json"
      via: "lesson references --verify command that loads spec.json"
      pattern: "verifyCommand"
---

<objective>
Create the Module 3 mini-project lesson and structural verification spec. The lesson teaches the artifact-persistence pattern (read-previous/write-new) and provides a copyable workflow template that extends the learner's existing skeptic workflow to produce persistent SKEPTIC-REVIEW.md artifacts. The spec.json defines structural checks for both the updated workflow and the artifact file.

Purpose: This is the hands-on capstone for Module 3. The learner applies the read-previous/write-new pattern they learned in Lesson 6 to build a real persistent-artifact workflow.
Output: Lesson 07 JSON file and spec.json verification file.
</objective>

<execution_context>
@C:/Users/18182/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/18182/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

@learn/content/modules/planning-state/module.json
@learn/content/modules/planning-state/lessons/06-bridge-to-practice.json

<!-- Prior module mini-project patterns to follow -->
@learn/content/modules/gsd-commands/lessons/06-mini-project.json
@learn/content/modules/gsd-commands/project/spec.json
@learn/content/modules/command-lifecycle/project/spec.json

<interfaces>
<!-- From learn/lib/verifier.cjs — spec.json format consumed by runVerification -->
spec.json shape:
{
  "id": string,           // e.g. "planning-state-project"
  "moduleId": string,     // e.g. "planning-state"
  "title": string,
  "description": string,
  "artifacts": [
    {
      "description": string,
      "path": string,     // supports ~/ for home dir
      "checks": [
        { "pattern": string (regex), "description": string }
      ]
    }
  ]
}

<!-- From lesson JSON format — content blocks -->
Lesson JSON shape:
{
  "id": string,
  "title": string,
  "lessonNumber": number,  // This is lesson 7
  "objective": string,
  "content": [ { type: "text"|"code"|"project", ... } ],
  "conceptMap": string,    // "mini-project" for this lesson
  "successCriteria": string
}

<!-- From Module 1 spec.json — existing skeptic workflow checks to extend -->
Module 1 already checks:
- skeptic.md command spec: frontmatter, objective, execution_context, @file workflow ref, context, process
- skeptic workflow: purpose section, process section
Module 3 must ADD checks for:
- workflow: read-previous pattern, write-new pattern
- SKEPTIC-REVIEW.md artifact: date/timestamp, findings section
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create spec.json with structural verification checks</name>
  <files>learn/content/modules/planning-state/project/spec.json</files>
  <action>
Create spec.json at learn/content/modules/planning-state/project/spec.json following the exact same structure as gsd-commands/project/spec.json and command-lifecycle/project/spec.json.

The spec must define these artifacts and structural checks:

**Artifact 1: Skeptic workflow (updated for artifact persistence)**
- path: "~/.claude/get-shit-done/workflows/skeptic.md"
- description: "Skeptic workflow with artifact persistence"
- checks:
  - Has purpose section: pattern `<purpose>[\s\S]*?</purpose>`
  - Has process section: pattern `<process>[\s\S]*?</process>`
  - Reads previous review artifact: pattern that matches reading/loading a previous SKEPTIC-REVIEW file (e.g. `SKEPTIC-REVIEW|previous.*(review|artifact)|read.*(previous|prior|existing).*(review|artifact)` — use a broad-enough regex that catches reasonable variations like "Read previous SKEPTIC-REVIEW.md" or "Load existing review artifact")
  - Writes new review artifact: pattern that matches writing/creating a SKEPTIC-REVIEW output (e.g. `write.*SKEPTIC-REVIEW|SKEPTIC-REVIEW.*write|create.*SKEPTIC-REVIEW|save.*SKEPTIC-REVIEW|output.*SKEPTIC-REVIEW` — again broad enough for reasonable variations)

**Artifact 2: SKEPTIC-REVIEW.md artifact file**
- path: ".planning/SKEPTIC-REVIEW.md"
- description: "Persistent skeptic review artifact"
- checks:
  - Has date/timestamp: pattern matching a date header or timestamp (e.g. `## \d{4}-\d{2}-\d{2}|Date:|Review Date:|timestamp:|\d{4}-\d{2}-\d{2}`)
  - Has findings section: pattern matching a findings/observations section (e.g. `## Findings|## Observations|## Review|## Critique|findings:|observations:`)

Set id to "planning-state-project", moduleId to "planning-state", title to "Build Persistent Skeptic Reviews", description to something like "Extend your skeptic workflow to produce persistent SKEPTIC-REVIEW.md artifacts that accumulate institutional knowledge across runs".

IMPORTANT: The regex patterns must be written as JSON strings with proper escaping (double backslashes for `\s`, `\S`, `\d`, etc.). Study the existing spec.json files for the exact escaping pattern — e.g. `[\\s\\S]` not `[\s\S]`.
  </action>
  <verify>
    <automated>node -e "const s=require('./learn/content/modules/planning-state/project/spec.json'); console.log('id:', s.id); console.log('artifacts:', s.artifacts.length); s.artifacts.forEach(a => { console.log(a.description + ':', a.checks.length, 'checks'); a.checks.forEach(c => { try { new RegExp(c.pattern); console.log('  OK:', c.description); } catch(e) { console.log('  INVALID REGEX:', c.description, e.message); process.exit(1); } }); })"</automated>
  </verify>
  <done>spec.json exists with 2 artifacts (workflow + review file), all regex patterns valid, patterns check for read-previous, write-new, date, and findings sections</done>
</task>

<task type="auto">
  <name>Task 2: Create Lesson 7 mini-project with artifact-persistence template</name>
  <files>learn/content/modules/planning-state/lessons/07-mini-project.json</files>
  <action>
Create lesson 07 JSON file following the exact same structure as gsd-commands/lessons/06-mini-project.json (Module 1's mini-project lesson). This is lesson number 7 in the planning-state module.

The lesson must follow template-first pedagogy: provide the artifact-writing workflow pattern as a copyable template (via C key clipboard copy), then ask the learner to customize it.

Content blocks in order:

1. **text block** — Introduction. Explain that in Modules 1 and 2, they built a skeptic command that prints findings and forgets them. Now they will apply the artifact persistence pattern from Lesson 6 (read-previous/write-new) to make skeptic produce a persistent SKEPTIC-REVIEW.md. This gives their skeptic command institutional memory — each run builds on the last. Reference that this is the same pattern STATE.md, SUMMARY.md, and RETROSPECTIVE.md all use.

2. **text block** — Verify/hint workflow explanation. Same pattern as Module 1 and 2: run verify to check structural progress, run hint for a nudge. Note that this project modifies their existing workflow file and creates a new artifact file, so `--verify --module=planning-state` is the command. If something goes wrong with the workflow, they can git checkout the original.

3. **text block** — Introduce the template. Explain they are about to see a workflow template that adds the artifact-persistence pattern to their existing skeptic workflow. Press C to copy, then integrate these additions into their existing workflow at ~/.claude/get-shit-done/workflows/skeptic.md. The key additions are: (a) a step that reads the previous SKEPTIC-REVIEW.md if it exists, and (b) a step that writes findings to SKEPTIC-REVIEW.md after analysis.

4. **code block** — The actual workflow template additions. This should show the READ and WRITE steps to add to the existing skeptic workflow process. Structure it as two new steps:

```
<step name="read_previous_reviews">
**Read previous SKEPTIC-REVIEW.md if it exists.**

Before analyzing the current phase, check for an existing review artifact:
- Read .planning/SKEPTIC-REVIEW.md if it exists
- Note previous findings, patterns, and recurring themes
- Use this historical context to make the current review more insightful
- If no previous review exists, proceed without historical context

This is the "read-previous" half of the artifact persistence pattern.
</step>

<step name="write_review_artifact">
**Write findings to SKEPTIC-REVIEW.md.**

After completing your analysis, persist findings to .planning/SKEPTIC-REVIEW.md:
- Add a new section with the current date as a heading (## YYYY-MM-DD)
- Record key findings and observations
- Note any patterns observed across multiple reviews (if previous reviews exist)
- Include specific, actionable recommendations

Format the file so it accumulates — each run APPENDS a new dated section rather than overwriting. This is the "write-new" half of the artifact persistence pattern.
</step>
```

5. **text block** — Customization encouragement. Same tone as Module 1: make it yours. Change the output format, add sections, reorganize. The structural requirements are: (a) the workflow must have a step that reads previous SKEPTIC-REVIEW.md, (b) the workflow must have a step that writes to SKEPTIC-REVIEW.md, and (c) running the command must produce a .planning/SKEPTIC-REVIEW.md file with a date heading and findings. Everything else is their creative decision.

6. **project block** — The formal project spec. Task: "Extend your skeptic workflow with artifact persistence — reading previous reviews and writing new findings to SKEPTIC-REVIEW.md." Deliverables:
   - Updated workflow at ~/.claude/get-shit-done/workflows/skeptic.md with read-previous and write-new steps
   - Persistent artifact at .planning/SKEPTIC-REVIEW.md with dated entries and findings (created by running the command)
   verifyCommand: "node learn/bin/gsd-learn.cjs --verify --module=planning-state"
   hintCommand: "node learn/bin/gsd-learn.cjs --hint --module=planning-state"

7. **text block** — Going further / completion. Suggest running /gsd:skeptic on their own project multiple times to see the review artifact accumulate. Celebrate: they now understand how GSD artifacts create institutional memory — and they have built it into a real command. Module 3 complete.

Set conceptMap to "mini-project" (matches the sectionMap in module.json). Set id to "planning-state-mini-project". Set lessonNumber to 7.
  </action>
  <verify>
    <automated>node -e "const l=require('./learn/content/modules/planning-state/lessons/07-mini-project.json'); console.log('id:', l.id); console.log('lessonNumber:', l.lessonNumber); console.log('blocks:', l.content.length); const types=l.content.map(c=>c.type); console.log('types:', types.join(', ')); const hasProject=types.includes('project'); console.log('has project block:', hasProject); if(!hasProject) process.exit(1); const p=l.content.find(c=>c.type==='project'); console.log('verifyCommand:', p.verifyCommand); console.log('hintCommand:', p.hintCommand); if(l.lessonNumber!==7) { console.log('ERROR: lessonNumber should be 7'); process.exit(1); }"</automated>
  </verify>
  <done>Lesson 07 exists with 7 content blocks including text, code (template), and project blocks. lessonNumber is 7, conceptMap is "mini-project", verifyCommand and hintCommand reference --module=planning-state. Template shows read-previous and write-new workflow steps.</done>
</task>

</tasks>

<verification>
1. `node -e "require('./learn/content/modules/planning-state/project/spec.json')"` — spec.json is valid JSON
2. `node -e "require('./learn/content/modules/planning-state/lessons/07-mini-project.json')"` — lesson JSON is valid
3. `node learn/bin/gsd-learn.cjs --verify --module=planning-state` — runs without crashing (will show FAIL since learner hasn't built artifacts yet, but should not error)
4. Spec.json artifact count is 2 (workflow + review file)
5. Lesson has project block with correct verify/hint commands
</verification>

<success_criteria>
- spec.json defines structural checks for both the updated skeptic workflow (read-previous + write-new patterns) and the SKEPTIC-REVIEW.md artifact (date + findings)
- Lesson 07 provides the artifact-persistence template as a copyable code block and explains the read-previous/write-new pattern
- Running --verify --module=planning-state executes without runtime errors
- All regex patterns in spec.json are syntactically valid
</success_criteria>

<output>
After completion, create `.planning/phases/15-planning-mini-project/15-01-SUMMARY.md`
</output>
