---
phase: 02-prompt-driven-content-pipeline
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - learn/lib/evaluator.cjs
  - learn/tests/evaluator.test.cjs
  - learn/content/rubric/rubric.json
  - learn/bin/generate-lessons.cjs
  - learn/content/prompts/generated/
  - learn/content/modules/command-lifecycle/lessons/01-welcome.json
  - learn/content/modules/command-lifecycle/lessons/02-entry-point.json
  - learn/content/modules/command-lifecycle/lessons/03-dispatch.json
  - learn/content/modules/command-lifecycle/lessons/04-tool-modules.json
  - learn/content/modules/command-lifecycle/lessons/05-state-and-config.json
  - learn/content/rubric/scores/iteration-01.json
autonomous: false
requirements: [CONT-03, CONT-05, MODL-01, MODL-02]

must_haves:
  truths:
    - "Evaluation rubric scores generated lessons on 5 dimensions with weighted pass/fail threshold"
    - "Pipeline script generates assembled prompts for all 5 Command Lifecycle lessons"
    - "Generated lesson JSON files conform to Phase 1 schema and are loadable by lessons.cjs"
    - "At least one round of prompt iteration has occurred with scores tracked"
    - "Command Lifecycle module has a conceptual overview lesson and source-dive lessons"
  artifacts:
    - path: "learn/lib/evaluator.cjs"
      provides: "Rubric evaluation scoring"
      exports: ["RUBRIC", "evaluateLesson", "recordIteration"]
    - path: "learn/content/rubric/rubric.json"
      provides: "Evaluation rubric definition"
      contains: "accuracy"
    - path: "learn/bin/generate-lessons.cjs"
      provides: "Pipeline entry point"
    - path: "learn/content/rubric/scores/iteration-01.json"
      provides: "First iteration scores"
    - path: "learn/tests/evaluator.test.cjs"
      provides: "Evaluator unit tests"
  key_links:
    - from: "learn/bin/generate-lessons.cjs"
      to: "learn/lib/parser.cjs"
      via: "requires parseSourceFile"
      pattern: "require.*parser"
    - from: "learn/bin/generate-lessons.cjs"
      to: "learn/lib/prompt-templates.cjs"
      via: "requires assemblePrompt"
      pattern: "require.*prompt-templates"
    - from: "learn/lib/evaluator.cjs"
      to: "learn/content/rubric/rubric.json"
      via: "loads rubric definition"
      pattern: "rubric\\.json"
    - from: "learn/content/modules/command-lifecycle/lessons/"
      to: "learn/lib/lessons.cjs"
      via: "loadModule reads generated lesson JSON"
      pattern: "loadModule"
---

<objective>
Build the evaluation rubric, pipeline script, and generate the Command Lifecycle module lessons through the prompt pipeline with at least one iteration of evaluation-driven prompt improvement.

Purpose: This plan completes the content pipeline by adding quality measurement (rubric), tying everything together (pipeline script), and producing real lesson content for the Command Lifecycle module.
Output: Evaluator module, rubric definition, pipeline script, 5 generated lesson JSON files, and iteration score records.
</objective>

<execution_context>
@C:/Users/18182/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/18182/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-prompt-driven-content-pipeline/02-RESEARCH.md
@.planning/phases/02-prompt-driven-content-pipeline/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (will exist when this plan executes) -->

From learn/lib/parser.cjs:
```javascript
/**
 * @param {string} filePath - Absolute path to .cjs file.
 * @returns {{ filePath, fileName, lineCount, moduleDoc, requires, exports, functions, sections, constants }}
 */
function parseSourceFile(filePath) { ... }
module.exports = { parseSourceFile };
```

From learn/lib/prompt-templates.cjs:
```javascript
/**
 * @param {string} templateName - 'overview' or 'source-dive'
 * @param {object} context - Parsed source data and lesson metadata
 * @returns {string} Assembled prompt text
 */
function assemblePrompt(templateName, context) { ... }
module.exports = { assemblePrompt };
```

From learn/lib/lessons.cjs:
```javascript
// Required lesson JSON fields:
const required = ['id', 'title', 'lessonNumber', 'objective', 'content', 'conceptMap', 'successCriteria'];
function loadModule(moduleId, contentDir) { ... }
module.exports = { loadModule };
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Evaluator module and rubric with TDD</name>
  <files>learn/lib/evaluator.cjs, learn/tests/evaluator.test.cjs, learn/content/rubric/rubric.json</files>
  <behavior>
    - RUBRIC constant has 5 dimensions: accuracy (0.25), clarity (0.20), depth (0.20), whyExplanations (0.20), conceptMapConnection (0.15)
    - evaluateLesson({accuracy: 5, clarity: 5, depth: 5, whyExplanations: 5, conceptMapConnection: 5}) returns weighted: 5.0, pass: true
    - evaluateLesson({accuracy: 1, clarity: 1, depth: 1, whyExplanations: 1, conceptMapConnection: 1}) returns weighted: 1.0, pass: false
    - evaluateLesson with weighted score exactly 3.5 returns pass: true (threshold is >=3.5)
    - evaluateLesson with weighted score 3.49 returns pass: false
    - recordIteration writes scores to learn/content/rubric/scores/iteration-NN.json
    - rubric.json matches RUBRIC constant structure with descriptions, weights, and scale labels
  </behavior>
  <action>
    RED: Create learn/tests/evaluator.test.cjs with tests for:
    1. Perfect scores (all 5s) produce weighted 5.0, pass true
    2. Minimum scores (all 1s) produce weighted 1.0, pass false
    3. Boundary: weighted exactly 3.5 passes, 3.49 fails
    4. Missing dimension defaults to score 1
    5. Dimensions object in result includes score, weight, contribution per dimension
    6. recordIteration creates numbered JSON file in scores directory

    GREEN: Create learn/content/rubric/rubric.json with the 5 scoring dimensions (accuracy, clarity, depth, whyExplanations, conceptMapConnection) with descriptions, weights, and 1-5 scale labels. passThreshold: 3.5, minimumPerDimension: 2.

    Create learn/lib/evaluator.cjs with:
    - RUBRIC constant loaded from rubric.json (or defined inline matching it)
    - evaluateLesson(scores) computes weighted score, returns { weighted, dimensions, pass }
    - recordIteration(iterationNumber, lessonScores, notes) writes JSON to learn/content/rubric/scores/iteration-{NN}.json
    - Follow GSD conventions: 'use strict', 2-space indent, single module.exports
  </action>
  <verify>
    <automated>node --test learn/tests/evaluator.test.cjs</automated>
  </verify>
  <done>Evaluator correctly scores lessons against rubric with 5 weighted dimensions. Pass/fail threshold at 3.5 works correctly. recordIteration persists scores to numbered JSON files.</done>
</task>

<task type="auto">
  <name>Task 2: Pipeline script and Command Lifecycle lesson generation</name>
  <files>learn/bin/generate-lessons.cjs, learn/content/prompts/generated/, learn/content/modules/command-lifecycle/lessons/01-welcome.json, learn/content/modules/command-lifecycle/lessons/02-entry-point.json, learn/content/modules/command-lifecycle/lessons/03-dispatch.json, learn/content/modules/command-lifecycle/lessons/04-tool-modules.json, learn/content/modules/command-lifecycle/lessons/05-state-and-config.json</files>
  <action>
    Create learn/bin/generate-lessons.cjs as the pipeline orchestration script. This script:

    1. Defines the LESSON_PLAN array for the Command Lifecycle module (5 lessons):
       - Lesson 1: type 'overview', title 'Welcome to GSD', template 'overview', no specific source files (uses module-level context)
       - Lesson 2: type 'source-dive', title 'Where Commands Start', template 'source-dive', source: get-shit-done/bin/gsd-tools.cjs, focus: 'entry point, require() imports, cwd/args setup'
       - Lesson 3: type 'source-dive', title 'Command Dispatch', template 'source-dive', source: get-shit-done/bin/gsd-tools.cjs, focus: 'switch statement routing, sub-command dispatch'
       - Lesson 4: type 'source-dive', title 'Tool Modules', template 'source-dive', sources: core.cjs + config.cjs + phase.cjs, focus: 'module boundaries, export patterns'
       - Lesson 5: type 'source-dive', title 'State and Configuration', template 'source-dive', sources: state.cjs + config.cjs, focus: 'STATE.md operations, config CRUD, persistence'

    2. For each lesson: calls parseSourceFile on listed sources, assembles context, calls assemblePrompt, writes assembled prompt to learn/content/prompts/generated/{NN}-{title}.txt

    3. IMPORTANT: The pipeline generates PROMPT TEXT FILES (not lesson JSON directly). The developer then feeds these prompts to an LLM to get lesson JSON. This is a build-time offline operation per the research anti-pattern guidance.

    4. After generating prompt text files, the script should also have a --from-json mode: `node learn/bin/generate-lessons.cjs --from-json learn/content/prompts/generated/responses/` that reads LLM response JSON files and validates them against the lesson schema before copying to the lessons directory. This handles Pitfall 2 (schema validation).

    Then, USE the pipeline to actually generate lessons:
    - Run generate-lessons.cjs to produce prompt text files
    - Since this executor IS an LLM, directly generate the 5 lesson JSON files by following the prompt instructions. Use ACTUAL GSD source code from the parsed files (read the real source files). Do NOT invent code.
    - Write each lesson JSON to learn/content/modules/command-lifecycle/lessons/ (replacing the Phase 1 hand-written versions)
    - Each lesson must include "why" explanations for design decisions (CONT-04)
    - Lesson 1 must be conceptual overview, Lessons 2-5 must be source-dive with real code (MODL-02)
    - All lessons must conform to the Phase 1 schema (7 required fields)
    - Validate by running loadModule('command-lifecycle') from lessons.cjs

    After generating lessons, score them using the evaluator:
    - Score each of the 5 lessons on all 5 rubric dimensions
    - Use recordIteration(1, scores, notes) to write learn/content/rubric/scores/iteration-01.json
    - This satisfies CONT-05 requirement for at least one evaluation round

    Script follows GSD conventions: 'use strict', #!/usr/bin/env node shebang, 2-space indent, single module.exports at end.
  </action>
  <verify>
    <automated>node learn/bin/generate-lessons.cjs && node -e "const { loadModule } = require('./learn/lib/lessons.cjs'); const m = loadModule('command-lifecycle'); if (m.lessons.length !== 5) { process.exit(1); } console.log('OK: ' + m.lessons.length + ' lessons loaded');"</automated>
  </verify>
  <done>Pipeline generates prompt text files for all 5 lessons. Command Lifecycle module has 5 generated lesson JSON files that pass schema validation via loadModule. Lesson 1 is conceptual overview, lessons 2-5 are source deep-dives with actual GSD code. iteration-01.json scores file exists.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify generated lesson quality</name>
  <files>learn/content/modules/command-lifecycle/lessons/, learn/content/rubric/scores/iteration-01.json</files>
  <action>
    Human reviews generated lesson content for quality and accuracy. All automation is complete in Tasks 1-2.

    What was built: Complete content pipeline with source parser, prompt templates, evaluator, pipeline script, and 5 generated Command Lifecycle lessons replacing the Phase 1 hand-written versions.
  </action>
  <verify>
    Human confirms lesson quality by navigating through lessons with `node learn/bin/gsd-learn.cjs` and reviewing rubric scores in learn/content/rubric/scores/iteration-01.json. Type "approved" to accept or describe issues for prompt iteration.
  </verify>
  <done>Human has reviewed all 5 generated lessons and confirmed they meet quality standards: accurate code, clear "why" explanations, proper overview-to-deep-dive progression.</done>
</task>

</tasks>

<verification>
Full test suite passes:
```bash
node --test learn/tests/parser.test.cjs learn/tests/prompt-templates.test.cjs learn/tests/evaluator.test.cjs
```

Pipeline generates valid lessons:
```bash
node learn/bin/generate-lessons.cjs
node -e "const { loadModule } = require('./learn/lib/lessons.cjs'); const m = loadModule('command-lifecycle'); console.log(m.lessons.length + ' lessons, all valid');"
```

Interactive tool works with generated content:
```bash
node learn/bin/gsd-learn.cjs --status
```
</verification>

<success_criteria>
- Evaluator scores lessons with 5 weighted dimensions, pass threshold at 3.5 (CONT-05)
- Pipeline script generates prompt text files and validates lesson JSON against schema (CONT-03)
- All 5 Command Lifecycle lessons generated, loadable, and renderable (MODL-01)
- Lesson 1 is conceptual overview, lessons 2-5 are source deep-dives (MODL-02)
- At least iteration-01.json scores file exists documenting quality evaluation (CONT-05)
- Human verification confirms lesson quality and accuracy
</success_criteria>

<output>
After completion, create `.planning/phases/02-prompt-driven-content-pipeline/02-02-SUMMARY.md`
</output>
