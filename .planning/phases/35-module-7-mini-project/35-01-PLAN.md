---
phase: 35-module-7-mini-project
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - learn/content/modules/workspaces-collaboration/lessons/08-mini-project.json
  - learn/content/modules/workspaces-collaboration/project/spec.json
  - learn/content/modules/workspaces-collaboration/project/hints.json
autonomous: true
requirements: [MINI-01, MINI-02, MINI-03]

must_haves:
  truths:
    - "Module 7 mini-project lesson presents the cross-AI review orchestrator challenge with clear objectives"
    - "Verification spec checks for review pipeline implementation (multi-runtime dispatch, result aggregation, output formatting)"
    - "5 progressive hints are accessible via H key guiding learner through the orchestrator build"
  artifacts:
    - path: "learn/content/modules/workspaces-collaboration/lessons/08-mini-project.json"
      provides: "Mini-project lesson with challenge, template, deliverables, verify/hint commands"
      contains: "cross-AI review orchestrator"
    - path: "learn/content/modules/workspaces-collaboration/project/spec.json"
      provides: "Verification spec with regex checks for review pipeline patterns"
      contains: "moduleId.*workspaces-collaboration"
    - path: "learn/content/modules/workspaces-collaboration/project/hints.json"
      provides: "5 progressive hints from vague to specific"
  key_links:
    - from: "learn/content/modules/workspaces-collaboration/lessons/08-mini-project.json"
      to: "spec.json and hints.json"
      via: "verifyCommand and hintCommand using --module=workspaces-collaboration"
      pattern: "--module=workspaces-collaboration"
    - from: "learn/content/modules/workspaces-collaboration/project/spec.json"
      to: "~/.claude/get-shit-done/workflows/skeptic.md"
      via: "artifact path reference for verification"
      pattern: "skeptic\\.md"
---

<objective>
Replace the skeleton Module 7 mini-project with a complete cross-AI review orchestrator challenge.

Purpose: Learner validates Module 7 understanding by extending skeptic with multi-runtime review dispatch, result aggregation, and formatted output -- applying workspace and collaboration concepts.
Output: Lesson JSON, verification spec, and 5 progressive hints.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@learn/content/modules/gsd2-agent-application/lessons/08-mini-project.json
@learn/content/modules/quality-feedback/lessons/08-mini-project.json
@learn/content/modules/gsd2-agent-application/project/spec.json
@learn/content/modules/gsd2-agent-application/project/hints.json
@learn/lib/verifier.cjs
@learn/lib/hints.cjs

<interfaces>
<!-- Verifier loads spec.json from learn/content/modules/{moduleId}/project/spec.json -->
<!-- Spec format: { id, moduleId, title, description, artifacts: [{ description, path, checks: [{ pattern, description }] }] } -->
<!-- Each check.pattern is a regex string tested against file content -->
<!-- Hints loaded from learn/content/modules/{moduleId}/project/hints.json -->
<!-- Hints format: string[] (ordered vague to specific, exactly 5 entries) -->
<!-- Lesson JSON uses "conceptMap": "mini-project" and successCriteria as a string -->
<!-- Lesson content blocks: type "text", "code", and "project" -->
<!-- Project block: { type: "project", task, deliverables[], verifyCommand, hintCommand, focus, bridge } -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create mini-project lesson, verification spec, and hints</name>
  <files>
    learn/content/modules/workspaces-collaboration/lessons/08-mini-project.json
    learn/content/modules/workspaces-collaboration/project/spec.json
    learn/content/modules/workspaces-collaboration/project/hints.json
  </files>
  <read_first>
    learn/content/modules/gsd2-agent-application/lessons/08-mini-project.json
    learn/content/modules/quality-feedback/lessons/08-mini-project.json
    learn/content/modules/gsd2-agent-application/project/spec.json
    learn/content/modules/gsd2-agent-application/project/hints.json
    learn/content/modules/workspaces-collaboration/lessons/08-mini-project.json
  </read_first>
  <action>
    **Per MINI-01: Replace skeleton 08-mini-project.json** with full mini-project lesson following the exact pattern from gsd2-agent-application and quality-feedback mini-projects. The lesson must:

    - id: "workspaces-collaboration-mini-project"
    - title: "Mini-Project: Cross-AI Review Orchestrator"
    - objective: Apply workspace and collaboration concepts to extend skeptic with a cross-AI review orchestrator that dispatches reviews to multiple AI runtimes, aggregates results, and formats combined output
    - 7 content blocks following the established pattern:
      1. text: Journey recap -- Module 1 command spec, Module 2 handler, Module 3 persistence, Module 4 orchestration, Module 5 quality verification, Module 6 dispatch loop. Gap: skeptic uses one AI runtime. Module 7 closes this by adding cross-AI review dispatch inspired by /gsd:review's multi-runtime pattern from Lesson 3.
      2. text: Verify/hint workflow instructions and git safety net (git checkout -- ~/.claude/get-shit-done/workflows/skeptic.md)
      3. text: Introduction to the template -- a cross-AI review orchestrator step that dispatches review requests to multiple AI runtimes (not just Claude), collects their findings, and merges them into unified output. Place after or within the dispatch loop from Module 6.
      4. code (language: xml): Template XML step named "cross_ai_review" with sections for:
         - Runtime Registry: list of AI runtimes to dispatch to (claude, cursor, gemini -- add/remove based on availability)
         - Dispatch to Runtimes: for each runtime, format a review prompt with the scoped context from the dispatch loop, invoke the runtime, collect its response
         - Result Aggregation: merge findings from all runtimes, deduplicate overlapping findings, note which runtime produced each finding
         - Output Formatting: produce a unified review report with per-runtime sections and a combined findings summary
      5. text: Six structural requirements that verification checks (per MINI-02):
         1. Runtime registry -- defines or lists multiple AI runtimes to dispatch to
         2. Multi-runtime dispatch -- sends review requests to more than one runtime
         3. Result collection -- collects or receives responses from dispatched runtimes
         4. Result aggregation -- merges or combines findings from multiple sources
         5. Runtime attribution -- tracks which runtime produced which finding
         6. Output formatting -- produces formatted combined review output
         Creative freedom for everything else.
      6. project block: task = "Extend your skeptic workflow with a cross-AI review orchestrator...", deliverables = updated skeptic.md + running /gsd:skeptic dispatches to multiple runtimes, verifyCommand = "node learn/bin/gsd-learn.cjs --verify --module=workspaces-collaboration", hintCommand = "node learn/bin/gsd-learn.cjs --hint --module=workspaces-collaboration"
      7. text: Module 7 completion message -- full arc across 7 modules recap, skeptic now does cross-AI review
    - conceptMap: "mini-project"
    - successCriteria: string describing all 6 checks passing

    **Per MINI-02: Create spec.json** in learn/content/modules/workspaces-collaboration/project/spec.json. Create the project/ directory first. Follow exact format from gsd2-agent-application/project/spec.json:
    - id: "workspaces-collaboration-project"
    - moduleId: "workspaces-collaboration"
    - title: "Add Cross-AI Review Orchestration to Skeptic"
    - description: concise description
    - artifacts array with one entry targeting ~/.claude/get-shit-done/workflows/skeptic.md
    - 6 checks with regex patterns:
      1. pattern: "runtime|provider|model|engine|ai.*(list|registry|config)|claude.*gemini|gemini.*claude|cursor.*claude|multiple.*ai" -- description: "Defines or lists multiple AI runtimes to dispatch reviews to"
      2. pattern: "dispatch.*runtime|send.*runtime|invoke.*(claude|gemini|cursor)|call.*(model|provider|runtime)|multi.*(runtime|model|provider)" -- description: "Dispatches review requests to multiple AI runtimes"
      3. pattern: "collect|receive|gather|response.*from|result.*from|output.*from|findings.*from" -- description: "Collects responses from dispatched runtimes"
      4. pattern: "aggregat|merg|combin|unif|consolidat|dedup|reconcil" -- description: "Aggregates or merges findings from multiple review sources"
      5. pattern: "attribut|source|origin|from.*(claude|gemini|cursor|runtime)|which.*runtime|runtime.*produc" -- description: "Tracks which runtime produced each finding"
      6. pattern: "format|report|output.*section|summary.*finding|present.*result|unified.*output" -- description: "Produces formatted combined review output"

    **Per MINI-03: Create hints.json** in learn/content/modules/workspaces-collaboration/project/hints.json. Array of exactly 5 strings, progressive from vague to specific:
      1. Vague conceptual nudge: Your skeptic currently dispatches to one AI. The cross-AI pattern from /gsd:review dispatches the same review prompt to multiple runtimes and combines their perspectives. Think about what your workflow needs to track which runtimes are available and route reviews to each.
      2. Components overview: A cross-AI review orchestrator has four parts: (1) a registry of AI runtimes to use, (2) dispatch logic that sends the review to each runtime, (3) collection of responses from all runtimes, (4) aggregation that merges findings with attribution. All four need to appear in your workflow.
      3. Integration guidance: Add a step (or extend existing steps) that lists available runtimes (claude, cursor, gemini), dispatches the current review context to each, collects their responses, and merges findings. Each finding should note which runtime produced it. The aggregation step should deduplicate overlapping findings from different runtimes.
      4. Structural outline: Create a cross_ai_review step that: (a) defines a runtime registry listing available AI runtimes, (b) for each runtime, formats and dispatches a review prompt with scoped context, (c) collects each runtime's response and findings, (d) aggregates findings across runtimes with deduplication and attribution, (e) formats a unified output report with per-runtime sections.
      5. Nearly complete solution: In your skeptic workflow, add a cross-AI review mechanism: (a) define a runtime registry (e.g., runtimes: claude, cursor, gemini -- each with invocation method), (b) for each runtime in the registry, dispatch the review prompt with scoped context from your dispatch loop, (c) collect each runtime's response into a results array, (d) aggregate by merging findings, deduplicating overlaps, and attributing each finding to its source runtime, (e) format a unified review report with a section per runtime and a combined findings summary, (f) output the formatted report. The structural checks verify these patterns exist -- the specific runtimes, invocation methods, dedup strategy, and report format are your creative decisions.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'),p=require('path');const l=JSON.parse(fs.readFileSync('learn/content/modules/workspaces-collaboration/lessons/08-mini-project.json','utf-8'));const s=JSON.parse(fs.readFileSync('learn/content/modules/workspaces-collaboration/project/spec.json','utf-8'));const h=JSON.parse(fs.readFileSync('learn/content/modules/workspaces-collaboration/project/hints.json','utf-8'));const ok=l.content.length>=6&&l.conceptMap==='mini-project'&&typeof l.successCriteria==='string'&&s.artifacts[0].checks.length===6&&s.moduleId==='workspaces-collaboration'&&h.length===5;console.log(ok?'PASS':'FAIL',{blocks:l.content.length,checks:s.artifacts[0].checks.length,hints:h.length})"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "cross-AI review orchestrator" learn/content/modules/workspaces-collaboration/lessons/08-mini-project.json returns >= 1
    - grep -c "mini-project" learn/content/modules/workspaces-collaboration/lessons/08-mini-project.json returns >= 1 (conceptMap)
    - grep -c "workspaces-collaboration" learn/content/modules/workspaces-collaboration/project/spec.json returns >= 1 (moduleId)
    - grep -c "skeptic" learn/content/modules/workspaces-collaboration/project/spec.json returns >= 1 (artifact path)
    - The spec.json has exactly 6 checks in artifacts[0].checks
    - The hints.json has exactly 5 entries
    - The lesson JSON has type "project" block with verifyCommand and hintCommand using --module=workspaces-collaboration
    - node learn/bin/gsd-learn.cjs --verify --module=workspaces-collaboration runs without crash (will show failures since learner hasn't built yet -- that is expected)
  </acceptance_criteria>
  <done>
    08-mini-project.json replaced with full cross-AI review orchestrator challenge (7 content blocks, project block with verify/hint commands, conceptMap "mini-project", string successCriteria). spec.json has 6 regex checks targeting skeptic.md for runtime registry, multi-runtime dispatch, result collection, aggregation, attribution, and output formatting. hints.json has 5 progressive hints from conceptual nudge to near-complete solution.
  </done>
</task>

</tasks>

<verification>
1. Lesson loads without JSON parse errors: node -e "JSON.parse(require('fs').readFileSync('learn/content/modules/workspaces-collaboration/lessons/08-mini-project.json','utf-8'))"
2. Spec loads and has correct structure: node -e "const s=JSON.parse(require('fs').readFileSync('learn/content/modules/workspaces-collaboration/project/spec.json','utf-8'));console.log(s.moduleId,s.artifacts[0].checks.length)"
3. Hints loads and has 5 entries: node -e "const h=JSON.parse(require('fs').readFileSync('learn/content/modules/workspaces-collaboration/project/hints.json','utf-8'));console.log(h.length)"
4. Verify command runs: node learn/bin/gsd-learn.cjs --verify --module=workspaces-collaboration (expected: failures since learner hasn't built yet)
</verification>

<success_criteria>
- Module 7 mini-project lesson presents the cross-AI review orchestrator challenge with template, 6 structural requirements, and verify/hint commands
- Verification spec checks skeptic.md for 6 patterns: runtime registry, multi-runtime dispatch, result collection, aggregation, attribution, output formatting
- 5 progressive hints guide learner from vague concept to near-complete solution
- All files are valid JSON and load without errors
</success_criteria>

<output>
After completion, create `.planning/phases/35-module-7-mini-project/35-01-SUMMARY.md`
</output>
