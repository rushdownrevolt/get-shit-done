---
phase: 18-module-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - learn/content/modules/agent-orchestration/module.json
  - learn/content/modules/agent-orchestration/concept-map.txt
  - learn/lib/progress.cjs
  - learn/tests/progress.test.cjs
autonomous: true
requirements: [INFR-01, INFR-02, INFR-03]
must_haves:
  truths:
    - "Module picker shows Agent Orchestration as Module 4 with correct recommended flag"
    - "Existing v4 progress files migrate to v5 with zero data loss"
    - "concept-map.txt shows lesson flow from orchestration overview through synthesis"
    - "Module directory has module.json, lessons/, project/ matching convention"
  artifacts:
    - path: "learn/content/modules/agent-orchestration/module.json"
      provides: "Module 4 registration with id, title, description, order, sectionMap"
      contains: "agent-orchestration"
    - path: "learn/content/modules/agent-orchestration/concept-map.txt"
      provides: "Visual lesson flow diagram"
      contains: "Orchestrator Pattern"
    - path: "learn/content/modules/agent-orchestration/lessons/"
      provides: "Empty lessons directory for Phase 19"
    - path: "learn/content/modules/agent-orchestration/project/"
      provides: "Empty project directory for Phase 20"
    - path: "learn/lib/progress.cjs"
      provides: "migrateV4toV5 function and updated loadProgress chain"
      exports: ["migrateV4toV5"]
  key_links:
    - from: "learn/lib/progress.cjs"
      to: "loadProgress"
      via: "migration chain v4->v5 in loadProgress()"
      pattern: "migrateV4toV5"
    - from: "learn/content/modules/agent-orchestration/module.json"
      to: "module auto-discovery"
      via: "scanner finds dirs with module.json"
      pattern: "agent-orchestration"
---

<objective>
Scaffold Module 4 "Agent Orchestration" with directory structure, module.json, concept map, and progress migration so the module system recognizes it and existing users can track progress.

Purpose: Foundation for Phase 19 lesson content — module must exist in the system before lessons can be added.
Output: Module directory with metadata, concept map, and v4->v5 progress migration.
</objective>

<execution_context>
@C:/Users/18182/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/18182/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Existing module.json convention (from planning-state/module.json): -->
```json
{
  "id": "planning-state",
  "title": "Planning & State",
  "description": "Understand how GSD structures projects with planning artifacts -- from PROJECT.md through milestones.",
  "order": 3,
  "sectionMap": {
    "overview": "Overview",
    "project": "PROJECT.md",
    "requirements-roadmap": "Requirements & Roadmap",
    "phase-lifecycle": "Phase Lifecycle",
    "state-milestones": "State & Milestones",
    "mini-project": "Mini-Project"
  }
}
```

<!-- Existing migration pattern (from learn/lib/progress.cjs): -->
```javascript
function migrateV3toV4(progress) {
  if (progress.version >= 4) { return progress; }
  return {
    version: 4,
    currentModule: progress.currentModule,
    currentLesson: progress.currentLesson,
    modules: { ...progress.modules },
  };
}
// In loadProgress(), chain: v1->v2->v3->v4, save if migrated
```

<!-- Existing concept-map.txt convention (from planning-state/concept-map.txt): -->
```
  /gsd:kickoff
        |
        v
  +------------------+
  | PROJECT.md       |
  | (What & Why)     |
  +------------------+
        |
  ... (ASCII flow diagram with boxes and arrows)
```

<!-- Progress test pattern (from learn/tests/progress.test.cjs): -->
```javascript
// Uses node:test, assert, tmpDir pattern
// Each migration has: transforms input, idempotent pass-through, empty modules
// loadProgress integration: auto-migrates from old version and writes back
```

<!-- DEFAULT_PROGRESS constant: -->
```javascript
const DEFAULT_PROGRESS = {
  version: 4,  // Will become 5
  currentModule: null,
  currentLesson: 0,
  modules: {},
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Module 4 directory structure and module.json</name>
  <files>learn/content/modules/agent-orchestration/module.json</files>
  <behavior>
    - module.json has id "agent-orchestration", title "Agent Orchestration", order 4
    - sectionMap has 8 entries: overview, subagent-types, wave-execution, orchestrator-pattern, checkpoints, auto-advance, synthesis, mini-project
    - lessons/ directory exists (empty, for Phase 19)
    - project/ directory exists (empty, for Phase 20)
  </behavior>
  <action>
Create directory `learn/content/modules/agent-orchestration/` with subdirs `lessons/` and `project/`.

Create `module.json` following the exact convention of existing modules:
```json
{
  "id": "agent-orchestration",
  "title": "Agent Orchestration",
  "description": "Learn how GSD orchestrates work through subagents -- wave-based parallelization, checkpoints, and auto-advance chains.",
  "order": 4,
  "sectionMap": {
    "overview": "Overview",
    "subagent-types": "Subagent Types",
    "wave-execution": "Wave Execution",
    "orchestrator-pattern": "Orchestrator Pattern",
    "checkpoints": "Checkpoints",
    "auto-advance": "Auto-Advance",
    "synthesis": "Synthesis",
    "mini-project": "Mini-Project"
  }
}
```

Add `.gitkeep` files in empty `lessons/` and `project/` directories so git tracks them.
  </action>
  <verify>
    <automated>node -e "const m = require('./learn/content/modules/agent-orchestration/module.json'); const assert = require('assert'); assert.strictEqual(m.id, 'agent-orchestration'); assert.strictEqual(m.order, 4); assert.strictEqual(Object.keys(m.sectionMap).length, 8); console.log('module.json OK')" && node -e "const fs = require('fs'); const assert = require('assert'); assert.ok(fs.existsSync('learn/content/modules/agent-orchestration/lessons')); assert.ok(fs.existsSync('learn/content/modules/agent-orchestration/project')); console.log('dirs OK')"</automated>
  </verify>
  <done>Module 4 directory exists with module.json (id: agent-orchestration, order: 4, 8 sectionMap entries), empty lessons/ and project/ subdirectories. Module auto-discovery scanner will find it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add v4-to-v5 progress migration</name>
  <files>learn/lib/progress.cjs, learn/tests/progress.test.cjs</files>
  <behavior>
    - migrateV4toV5 with v4 input returns version 5, preserves all existing module entries
    - migrateV4toV5 with v5 input returns unchanged (idempotent)
    - migrateV4toV5 with empty modules returns version 5 with empty modules
    - loadProgress with v4 data on disk auto-migrates to v5 and writes back
    - loadProgress with v5 data does not rewrite
    - Full chain test: v1 data auto-migrates through v1->v2->v3->v4->v5
    - DEFAULT_PROGRESS.version is 5
  </behavior>
  <action>
In `learn/lib/progress.cjs`:

1. Update `DEFAULT_PROGRESS.version` from 4 to 5.

2. Add `migrateV4toV5` function following the exact pattern of migrateV3toV4:
```javascript
function migrateV4toV5(progress) {
  if (progress.version >= 5) { return progress; }
  return {
    version: 5,
    currentModule: progress.currentModule,
    currentLesson: progress.currentLesson,
    modules: { ...progress.modules },
  };
}
```
This is a structural version bump to support Module 4 tracking, same pattern as v3->v4 for Module 3.

3. In `loadProgress()`, add the v4->v5 migration step after the v3->v4 step:
```javascript
if (progress.version < 5) {
  progress = migrateV4toV5(progress);
  saveProgress(cwd, progress);
}
```
IMPORTANT: The existing v3->v4 block already has a `saveProgress` call. Refactor so that save only happens once at the end if ANY migration occurred. Check `progress.version < 5` after all migrations and save once if version changed from original parsed version.

4. Export `migrateV4toV5` in module.exports.

In `learn/tests/progress.test.cjs`:

5. Update the `loadProgress()` default test to expect version 5 (not 4).

6. Update the `loadProgress()` invalid JSON test to expect version 5.

7. Update the `loadProgress()` missing fields test to expect version 5.

8. Add `describe('migrateV4toV5()')` with tests following the exact pattern of migrateV3toV4 tests:
   - v4 data with existing modules gets version bumped to 5, all module entries preserved
   - v5 data passes through unchanged (idempotent)
   - v4 data with no modules migrates cleanly to v5

9. Update the v1-on-disk integration test: expect version 5 (not 4).

10. Update the v2-on-disk integration test: expect version 5.

11. Update the v4-on-disk "does not rewrite" test: change to v5 data (version: 5) since v4 will now be migrated.

12. Add a v4-on-disk integration test: v4 file auto-migrates to v5 and writes back.

13. Update all other tests that reference `version: 4` in DEFAULT_PROGRESS expectations to `version: 5`.

14. Update isFirstRun tests to use version: 5 in test data.

15. Update saveProgress and round-trip tests to use version: 5.
  </action>
  <verify>
    <automated>node --test learn/tests/progress.test.cjs</automated>
  </verify>
  <done>v4->v5 migration works. DEFAULT_PROGRESS is version 5. loadProgress auto-migrates v4 files. All existing tests updated and passing. migrateV4toV5 exported.</done>
</task>

<task type="auto">
  <name>Task 3: Create concept map for agent orchestration lesson flow</name>
  <files>learn/content/modules/agent-orchestration/concept-map.txt</files>
  <action>
Create `concept-map.txt` in the agent-orchestration module directory following the ASCII box-and-arrow convention from existing modules.

The map should show the 7-lesson flow specified in the phase description:
1. Overview (the orchestration model)
2. Subagent Types (executor, planner, researcher, verifier, checker)
3. Wave Execution (parallel spawning, wave grouping)
4. Orchestrator Pattern (context budget, lean orchestrator)
5. Checkpoints (autonomous flags, human-in-the-loop)
6. Auto-Advance (plan->execute->verify piping)
7. Synthesis (full lifecycle connection)

Layout: Linear top-to-bottom flow with descriptive subtitles in each box, matching the style of planning-state/concept-map.txt. Use `+--` box borders and `|`/`v` arrows.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const c = fs.readFileSync('learn/content/modules/agent-orchestration/concept-map.txt', 'utf-8'); const assert = require('assert'); assert.ok(c.includes('Overview')); assert.ok(c.includes('Subagent Types')); assert.ok(c.includes('Wave Execution')); assert.ok(c.includes('Orchestrator Pattern')); assert.ok(c.includes('Checkpoints')); assert.ok(c.includes('Auto-Advance')); assert.ok(c.includes('Synthesis')); console.log('concept-map OK')"</automated>
  </verify>
  <done>concept-map.txt exists showing the 7-lesson flow from orchestration overview through synthesis in ASCII box diagram format.</done>
</task>

</tasks>

<verification>
1. Module auto-discovery finds Module 4: `node -e "const fs = require('fs'); const dirs = fs.readdirSync('learn/content/modules'); console.log(dirs); const m = require('./learn/content/modules/agent-orchestration/module.json'); console.log(m.title, 'order:', m.order)"`
2. Progress migration chain works: `node --test learn/tests/progress.test.cjs`
3. Concept map has all 7 lessons: verify command from Task 3
4. Directory structure complete: `ls learn/content/modules/agent-orchestration/`
</verification>

<success_criteria>
- Module 4 "agent-orchestration" discovered by module system (module.json exists with order: 4)
- v4 progress files auto-migrate to v5 on load with zero data loss
- concept-map.txt contains all 7 lesson nodes from Overview through Synthesis
- Directory structure: module.json, lessons/, project/, concept-map.txt
- All progress tests pass: `node --test learn/tests/progress.test.cjs`
</success_criteria>

<output>
After completion, create `.planning/phases/18-module-infrastructure/18-01-SUMMARY.md`
</output>
