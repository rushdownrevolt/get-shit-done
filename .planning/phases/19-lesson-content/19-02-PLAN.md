---
phase: 19-lesson-content
plan: 02
type: execute
wave: 2
depends_on: [19-01]
files_modified:
  - learn/content/modules/agent-orchestration/lessons/04-orchestrator-pattern.json
  - learn/content/modules/agent-orchestration/lessons/05-checkpoints.json
  - learn/content/modules/agent-orchestration/lessons/06-auto-advance.json
  - learn/content/modules/agent-orchestration/lessons/07-synthesis.json
autonomous: true
requirements: [LESS-04, LESS-05, LESS-06, LESS-07]

must_haves:
  truths:
    - "Lesson 4 teaches the orchestrator pattern: context budget, path-only delegation, lean orchestrator principle"
    - "Lesson 5 teaches checkpoints and gates: autonomous flags, human-in-the-loop, checkpoint continuation"
    - "Lesson 6 teaches auto-advance chains: plan->execute->verify piping, --auto flag, chain automation"
    - "Lesson 7 synthesizes the full lifecycle: how orchestration connects to planning, requirements, and milestones"
    - "All four lessons load with correct lessonNumber values 4-7 and conceptMap values matching module.json"
    - "Code blocks contain real snippets from GSD workflow files"
  artifacts:
    - path: "learn/content/modules/agent-orchestration/lessons/04-orchestrator-pattern.json"
      provides: "Lesson 4 - lean orchestrator design"
      contains: "context"
    - path: "learn/content/modules/agent-orchestration/lessons/05-checkpoints.json"
      provides: "Lesson 5 - checkpoints and human-in-the-loop"
      contains: "checkpoint"
    - path: "learn/content/modules/agent-orchestration/lessons/06-auto-advance.json"
      provides: "Lesson 6 - auto-advance chains"
      contains: "auto"
    - path: "learn/content/modules/agent-orchestration/lessons/07-synthesis.json"
      provides: "Lesson 7 - full lifecycle synthesis"
      contains: "lifecycle"
  key_links:
    - from: "04-orchestrator-pattern.json"
      to: "module.json sectionMap"
      via: "conceptMap field"
      pattern: "orchestrator-pattern"
    - from: "05-checkpoints.json"
      to: "module.json sectionMap"
      via: "conceptMap field"
      pattern: "checkpoints"
    - from: "06-auto-advance.json"
      to: "module.json sectionMap"
      via: "conceptMap field"
      pattern: "auto-advance"
    - from: "07-synthesis.json"
      to: "module.json sectionMap"
      via: "conceptMap field"
      pattern: "synthesis"
---

<objective>
Create lessons 4-7 for Module 4 (Agent Orchestration) covering the orchestrator pattern, checkpoints, auto-advance chains, and full lifecycle synthesis.

Purpose: These four lessons complete the Module 4 curriculum — from how orchestrators stay lean through human-in-the-loop checkpoints to automated phase chaining and finally synthesizing everything into the full GSD lifecycle. Each lesson uses real code from GSD's workflow files.
Output: Four lesson JSON files in learn/content/modules/agent-orchestration/lessons/
</objective>

<execution_context>
@C:/Users/18182/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/18182/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-lesson-content/19-01-SUMMARY.md

@learn/content/modules/agent-orchestration/module.json
@learn/content/modules/agent-orchestration/concept-map.txt
@learn/content/modules/agent-orchestration/lessons/01-overview.json
@learn/content/modules/agent-orchestration/lessons/03-wave-execution.json

<interfaces>
<!-- Lesson JSON format (established by Modules 1-3): -->
<!--
{
  "id": "string",           // matches sectionMap key in module.json
  "title": "string",
  "lessonNumber": number,
  "objective": "string",
  "content": [
    { "type": "text", "value": "string", "focus": "string", "bridge": "string" },
    { "type": "code", "language": "markdown|text|yaml", "value": "string", "highlight": [line_numbers] }
  ],
  "conceptMap": "string",   // must match sectionMap key in module.json
  "successCriteria": "string"
}
-->

<!-- module.json sectionMap keys for lessons 4-7: "orchestrator-pattern", "checkpoints", "auto-advance", "synthesis" -->

<!-- GSD source files to extract real code snippets from: -->
<!-- C:/Users/18182/.claude/get-shit-done/workflows/execute-phase.md — orchestrator behavior, checkpoint handling, auto-advance -->
<!-- C:/Users/18182/.claude/get-shit-done/workflows/execute-plan.md — executor patterns, checkpoint routing -->
<!-- C:/Users/18182/.claude/get-shit-done/workflows/plan-phase.md — orchestrator design, context budget -->
<!-- C:/Users/18182/.claude/get-shit-done/workflows/transition.md — phase completion, auto-advance chains -->
<!-- C:/Users/18182/.claude/get-shit-done/workflows/verify-phase.md — verification gates -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Lesson 4 - The Orchestrator Pattern</name>
  <files>learn/content/modules/agent-orchestration/lessons/04-orchestrator-pattern.json</files>
  <action>
Create lesson JSON file with:
- id: "orchestrator-pattern"
- title: "The Orchestrator Pattern"
- lessonNumber: 4
- conceptMap: "orchestrator-pattern"
- successCriteria: describing what the learner understands about lean orchestrator design

Content blocks (target 7-9 blocks, mix of text and code):

**Block 1 (text):** Bridge from Lesson 3 — waves show how plans execute in parallel, but why does the orchestrator itself work reliably across many plans? Because it follows a strict design principle: stay lean. The orchestrator never reads file contents, never writes code, never makes implementation decisions. It passes paths and reads summaries. Focus: "Lean by design, not by accident." Bridge to seeing the context budget.

**Block 2 (code, language: text):** Show the context budget concept. Read C:/Users/18182/.claude/get-shit-done/workflows/execute-phase.md and extract the comments about orchestrator context staying lean at ~10-15%. Also read execute-plan.md init_context step showing "paths only to minimize orchestrator context." Combine into a snippet showing the deliberate design choice.

**Block 3 (text):** Explain the context budget principle: Claude has a finite context window. If the orchestrator reads every file, it exhausts context before finishing. Instead, it reads only paths and metadata (frontmatter from PLAN.md files, existence checks on SUMMARY.md files). The actual file content is read by executors in their fresh context windows. This is why gsd-tools returns JSON with paths, not file contents. Focus: "Paths not content keeps orchestrator viable."

**Block 4 (code, language: text):** Show the init call from execute-phase.md that returns only paths and flags. Read the file and extract the INIT=$(node gsd-tools.cjs init execute-phase) call and the list of parsed fields (executor_model, phase_dir, plans, etc.). Highlight the "Parse JSON for:" line showing it is all metadata.

**Block 5 (text):** Explain path-only delegation: when the orchestrator spawns an executor, it passes a prompt containing file PATHS (@file references), not file contents. The executor reads those files itself. This means the orchestrator uses ~100-200 tokens per plan spawn, while the executor gets a fresh 200k context to work with. The orchestrator can manage 10+ plans without degrading. Focus: "Each executor gets full context, orchestrator stays thin."

**Block 6 (code, language: text):** Show the plan-phase.md orchestrator structure. Read C:/Users/18182/.claude/get-shit-done/workflows/plan-phase.md and extract the beginning showing its purpose (orchestrates gsd-phase-researcher, gsd-planner, gsd-plan-checker with revision loop). Show how plan-phase is itself an orchestrator that spawns planners and checkers. Highlight the orchestrator role names.

**Block 7 (text):** Explain that plan-phase is ALSO an orchestrator — it spawns the planner (which creates PLAN.md files), then the checker (which validates them), then routes back for revision if needed. This is the same pattern: coordinate, delegate, verify. GSD has multiple orchestrators, each following the same lean principle. Focus: "The pattern repeats at every level."

**Block 8 (text):** Bridge to next lesson — the orchestrator can run most plans autonomously, but some require human input. Next, we will see how checkpoints pause execution and resume after human decisions.

**IMPORTANT:** Read each source file and extract real snippets. Escape properly for JSON.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const lesson = JSON.parse(fs.readFileSync('learn/content/modules/agent-orchestration/lessons/04-orchestrator-pattern.json', 'utf8')); console.log('id:', lesson.id); console.log('lessonNumber:', lesson.lessonNumber); console.log('blocks:', lesson.content.length); console.log('conceptMap:', lesson.conceptMap); const codeBlocks = lesson.content.filter(b => b.type === 'code'); console.log('codeBlocks:', codeBlocks.length); if (lesson.lessonNumber !== 4) throw new Error('Wrong lesson number'); if (lesson.conceptMap !== 'orchestrator-pattern') throw new Error('Wrong conceptMap'); if (lesson.content.length < 6) throw new Error('Too few blocks'); if (codeBlocks.length < 2) throw new Error('Too few code blocks');"</automated>
  </verify>
  <done>Lesson 4 JSON exists with lessonNumber 4, conceptMap "orchestrator-pattern", 7+ blocks covering context budget, path-only delegation, and lean orchestrator principle with real workflow snippets</done>
</task>

<task type="auto">
  <name>Task 2: Create Lesson 5 - Checkpoints and Gates</name>
  <files>learn/content/modules/agent-orchestration/lessons/05-checkpoints.json</files>
  <action>
Create lesson JSON file with:
- id: "checkpoints"
- title: "Checkpoints and Gates"
- lessonNumber: 5
- conceptMap: "checkpoints"
- successCriteria: describing what the learner understands about human-in-the-loop checkpoints

Content blocks (target 7-9 blocks, mix of text and code):

**Block 1 (text):** Bridge from Lesson 4 — lean orchestrators run plans autonomously, but not ALL work can be fully automated. Visual verification, deployment approval, external service setup — these need human judgment. GSD handles this through checkpoints: structured pause points in plan execution. Focus: "Automation first, checkpoints for what requires human judgment." Bridge to seeing the autonomous flag.

**Block 2 (code, language: yaml):** Show two PLAN.md frontmatter examples side by side — one with autonomous: true and one with autonomous: false. Show how the autonomous flag signals whether the plan has checkpoint tasks. Then show a checkpoint task XML example with type="checkpoint:human-verify" and gate="blocking", containing what-built, how-to-verify (with numbered steps like visiting localhost and testing credentials), and resume-signal elements. IMPORTANT: When writing the JSON code block value string, use actual XML angle brackets in the text content — these are lesson content showing the learner what checkpoint XML looks like, not plan structure. Highlight the type and gate attribute lines.

**Block 3 (text):** Explain the three checkpoint types: checkpoint:human-verify (90% of cases — Claude automates, human confirms it works), checkpoint:decision (9% — human makes implementation choice), checkpoint:human-action (1% — truly unavoidable manual steps like email verification). The key rule: if Claude CAN do it via CLI/API, Claude MUST do it. Checkpoints verify AFTER automation, not replace it. Focus: "Checkpoints verify, they don't replace automation."

**Block 4 (code, language: text):** Show the checkpoint handling from execute-phase.md. Read C:/Users/18182/.claude/get-shit-done/workflows/execute-phase.md and extract the checkpoint_handling step. Show the flow: plan with autonomous: false -> executor runs until checkpoint -> returns structured state -> orchestrator presents to user -> user responds -> continuation agent spawned. Highlight the checkpoint type handling lines.

**Block 5 (text):** Explain the checkpoint flow in detail: when an executor hits a checkpoint task, it stops and returns its state (completed tasks, current blocker, what's awaited). The orchestrator presents this to the user with specific verification steps. The user responds (approved, issue description, or decision). Then a CONTINUATION agent is spawned — not a resume of the old agent, but a fresh agent that picks up from where the checkpoint was. This preserves the fresh-context principle. Focus: "Continuation, not resumption — fresh context even after checkpoints."

**Block 6 (code, language: text):** Show the executor routing from execute-plan.md. Read the file and extract the parse_segments routing table showing Pattern A (autonomous — single agent), Pattern B (segmented — pauses at checkpoints), Pattern C (main context — for decisions). Highlight the three patterns.

**Block 7 (text):** Explain how routing works: Pattern A plans get one executor that does everything. Pattern B plans get segmented execution — autonomous segments run as subagents, checkpoint segments pause for human input. Pattern C plans execute in the main orchestrator context (for complex decisions). The routing is automatic based on checkpoint types found in the plan. Focus: "Routing adapts execution to plan structure."

**Block 8 (text):** Bridge to next lesson — checkpoints create human-in-the-loop control. But what about fully automated workflows? Next, we will see how GSD chains phases together for hands-free execution with auto-advance.

**IMPORTANT:** Read each source file and extract real snippets. Escape properly for JSON.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const lesson = JSON.parse(fs.readFileSync('learn/content/modules/agent-orchestration/lessons/05-checkpoints.json', 'utf8')); console.log('id:', lesson.id); console.log('lessonNumber:', lesson.lessonNumber); console.log('blocks:', lesson.content.length); console.log('conceptMap:', lesson.conceptMap); const codeBlocks = lesson.content.filter(b => b.type === 'code'); console.log('codeBlocks:', codeBlocks.length); const hasCheckpoint = lesson.content.some(b => b.value && b.value.includes('checkpoint')); console.log('hasCheckpointRef:', hasCheckpoint); if (lesson.lessonNumber !== 5) throw new Error('Wrong lesson number'); if (lesson.conceptMap !== 'checkpoints') throw new Error('Wrong conceptMap'); if (lesson.content.length < 6) throw new Error('Too few blocks');"</automated>
  </verify>
  <done>Lesson 5 JSON exists with lessonNumber 5, conceptMap "checkpoints", 7+ blocks covering autonomous flags, three checkpoint types, continuation pattern, and execution routing with real workflow snippets</done>
</task>

<task type="auto">
  <name>Task 3: Create Lessons 6-7 - Auto-Advance and Synthesis</name>
  <files>
    learn/content/modules/agent-orchestration/lessons/06-auto-advance.json
    learn/content/modules/agent-orchestration/lessons/07-synthesis.json
  </files>
  <action>
Create TWO lesson JSON files:

### Lesson 6: Auto-Advance Chains

- id: "auto-advance"
- title: "Auto-Advance Chains"
- lessonNumber: 6
- conceptMap: "auto-advance"
- successCriteria: describing what the learner understands about automated phase chaining

Content blocks (target 7-9 blocks):

**Block 1 (text):** Bridge from Lesson 5 — checkpoints give human control when needed, but GSD can also run entire phase lifecycles hands-free. The auto-advance system chains plan -> execute -> verify -> transition automatically using the --auto flag. Focus: "From manual steps to automated pipelines."

**Block 2 (code, language: text):** Show the auto-advance configuration. Read C:/Users/18182/.claude/get-shit-done/workflows/execute-phase.md and find the auto-chain handling — the section about _auto_chain_active flag and workflow.auto_advance config. Also show the --auto flag clearing logic. Highlight the config-set and config-get lines.

**Block 3 (text):** Explain the two auto-advance mechanisms: (1) the --auto flag on a single invocation, which sets _auto_chain_active for that run, and (2) workflow.auto_advance in config.json, which is the persistent user preference. When either is true, the system auto-approves checkpoints: human-verify gets "approved", decisions get the first option, only human-action pauses. Focus: "Two levels: per-run flag and persistent setting."

**Block 4 (code, language: text):** Show the transition workflow chaining. Read C:/Users/18182/.claude/get-shit-done/workflows/transition.md and extract the verify_completion step showing how completion check works (count PLANs vs SUMMARYs), the yolo auto-approval, and the update_roadmap_and_state step. Highlight the "all plans complete" detection and the gsd-tools phase complete call.

**Block 5 (text):** Explain the chain: execute-phase completes all plans -> transition marks phase done and advances STATE.md -> if auto-advance is on, the next phase starts automatically. This creates a pipeline: plan Phase 5 -> execute Phase 5 -> verify Phase 5 -> transition to Phase 6 -> execute Phase 6, all without user interaction. The --no-transition flag breaks the chain if you want to stop after execution. Focus: "The full chain: execute -> verify -> transition -> next."

**Block 6 (code, language: text):** Show the auto-mode checkpoint handling from execute-phase.md. Read the file and extract the section showing how auto-mode handles each checkpoint type (human-verify -> auto-approve, decision -> first option, human-action -> still pauses). Highlight the auto-approve and auto-select lines.

**Block 7 (text):** Bridge to final lesson — auto-advance completes the automation story. In the next lesson, we will zoom out and see how orchestration fits into the complete GSD lifecycle, connecting everything from project definition to milestone completion.

### Lesson 7: Synthesis

- id: "synthesis"
- title: "The Full Lifecycle"
- lessonNumber: 7
- conceptMap: "synthesis"
- successCriteria: describing what the learner can explain about the complete orchestration lifecycle

Content blocks (target 7-9 blocks):

**Block 1 (text):** This final lesson connects all the pieces. You have learned: the orchestration model (Lesson 1), the five agent types (Lesson 2), wave execution (Lesson 3), lean orchestrator design (Lesson 4), checkpoints (Lesson 5), and auto-advance (Lesson 6). Now we will see how these compose into the complete GSD lifecycle. Focus: "From parts to whole."

**Block 2 (code, language: text):** Show the complete lifecycle as an ASCII diagram showing the full command chain: /gsd:kickoff -> PROJECT.md, /gsd:requirements -> REQUIREMENTS.md, /gsd:roadmap -> ROADMAP.md, /gsd:plan-phase [orchestrator] spawning researcher/planner/checker, /gsd:execute-phase [orchestrator] discovering plans and spawning executors in waves with checkpoint handling, /gsd:verify-phase spawning verifier, /gsd:transition advancing state, /gsd:complete-milestone archiving. Highlight the orchestrator lines.

**Block 3 (text):** Walk through a concrete example: building a user authentication feature. /gsd:plan-phase reads the phase goal from ROADMAP.md, spawns a planner that creates 2 plans (schema + API in wave 1, UI in wave 2). /gsd:execute-phase discovers the 2 plans, groups them, spawns executors. Wave 1 executor creates the database schema and API routes, commits each task. Wave 2 executor creates the login form, wires it to the API. Both produce SUMMARY.md files. Focus: "Concrete example: auth feature end-to-end."

**Block 4 (code, language: text):** Show what the execute-phase orchestrator actually sees during this flow. Create a realistic execution trace showing: Execution Plan header with phase name and wave count, wave table with plans and descriptions, Wave 1 spawning and completion with spot-check, Wave 2 spawning. Highlight the wave headers.

**Block 5 (text):** Explain the key principles that make this work: (1) Fresh context per agent — quality never degrades. (2) Path-only delegation — orchestrator stays lean. (3) Pre-computed waves — no runtime dependency resolution. (4) Atomic commits — each task is independently verifiable. (5) Spot-check verification — trust but verify. These are not aspirational — they are enforced by the workflow structure itself. Focus: "Five principles that make orchestration reliable."

**Block 6 (code, language: text):** Show the quality degradation curve that motivates fresh context. This is from plan-phase.md — use it verbatim: Context Usage | Quality | Claude's State table showing 0-30% PEAK, 30-50% GOOD, 50-70% DEGRADING, 70%+ POOR. Highlight the DEGRADING and POOR rows.

**Block 7 (text):** Explain why this matters: without fresh context per agent, a long-running session would hit 70%+ context by the third plan, producing rushed, minimal output. With orchestration, every plan gets 0-30% context — peak quality every time. This is not theoretical — it is measurable in execution times and output quality across GSD's own development (35+ plans executed with consistent 2-4 minute completion times). Focus: "Fresh context = peak quality, every time."

**Block 8 (text):** Final synthesis: GSD's orchestration is not magic — it is structured decomposition. Break work into phases. Break phases into plans. Break plans into tasks. Assign waves by dependency. Spawn agents with fresh context. Verify results. Advance automatically. Every piece you have learned in this module is a specific mechanism serving this decomposition. In the next step (the mini-project), you will apply these concepts yourself.

**IMPORTANT:** Read workflow source files for any real snippets needed. The quality degradation curve is from the plan-phase.md system prompt — use it verbatim. Escape properly for JSON.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); ['06-auto-advance', '07-synthesis'].forEach((name, i) => { const lesson = JSON.parse(fs.readFileSync('learn/content/modules/agent-orchestration/lessons/' + name + '.json', 'utf8')); console.log(name, '- id:', lesson.id, 'num:', lesson.lessonNumber, 'blocks:', lesson.content.length, 'map:', lesson.conceptMap); const expected = [['auto-advance', 6, 'auto-advance'], ['synthesis', 7, 'synthesis']][i]; if (lesson.id !== expected[0]) throw new Error('Wrong id for ' + name); if (lesson.lessonNumber !== expected[1]) throw new Error('Wrong number for ' + name); if (lesson.conceptMap !== expected[2]) throw new Error('Wrong conceptMap for ' + name); if (lesson.content.length < 6) throw new Error('Too few blocks for ' + name); });"</automated>
  </verify>
  <done>Lessons 6 and 7 JSON files exist with correct lessonNumbers (6, 7), conceptMaps ("auto-advance", "synthesis"), 7+ blocks each covering auto-advance chains and full lifecycle synthesis with real workflow snippets</done>
</task>

</tasks>

<verification>
- All four lesson JSON files are valid and parseable
- lessonNumbers are 4, 5, 6, 7 respectively
- conceptMap values match module.json sectionMap keys: "orchestrator-pattern", "checkpoints", "auto-advance", "synthesis"
- Code blocks contain real GSD workflow content (not invented examples)
- Lessons flow: orchestrator pattern -> checkpoints -> auto-advance -> synthesis
- Each lesson bridges to the next; Lesson 7 bridges to mini-project
- All 7 lessons together cover the complete orchestration lifecycle
</verification>

<success_criteria>
- Lesson 4 teaches lean orchestrator design with real plan-phase.md and execute-phase.md snippets
- Lesson 5 teaches checkpoints with real checkpoint handling code
- Lesson 6 teaches auto-advance with real transition.md and execute-phase.md snippets
- Lesson 7 synthesizes the full lifecycle with a concrete example and the quality degradation curve
- All four lessons load correctly in the learning shell
- Content blocks contain actual GSD source code, not invented examples
</success_criteria>

<output>
After completion, create `.planning/phases/19-lesson-content/19-02-SUMMARY.md`
</output>
