---
phase: 19-lesson-content
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - learn/content/modules/agent-orchestration/lessons/01-overview.json
  - learn/content/modules/agent-orchestration/lessons/02-subagent-types.json
  - learn/content/modules/agent-orchestration/lessons/03-wave-execution.json
autonomous: true
requirements: [LESS-01, LESS-02, LESS-03]

must_haves:
  truths:
    - "Lesson 1 introduces the orchestration model: orchestrators coordinate, executors build, fresh context per agent"
    - "Lesson 2 teaches the five subagent types (executor, planner, researcher, verifier, checker) with real config snippets"
    - "Lesson 3 teaches wave execution: plan discovery, wave grouping by depends_on, parallel spawning, spot-check verification"
    - "All three lessons load via loadModule('agent-orchestration') with correct lessonNumber values 1-3"
    - "Code blocks contain real snippets from GSD workflow files, not invented examples"
  artifacts:
    - path: "learn/content/modules/agent-orchestration/lessons/01-overview.json"
      provides: "Lesson 1 - orchestration model overview"
      contains: "orchestrat"
    - path: "learn/content/modules/agent-orchestration/lessons/02-subagent-types.json"
      provides: "Lesson 2 - subagent type definitions"
      contains: "gsd-executor"
    - path: "learn/content/modules/agent-orchestration/lessons/03-wave-execution.json"
      provides: "Lesson 3 - wave-based parallel execution"
      contains: "wave"
  key_links:
    - from: "01-overview.json"
      to: "module.json sectionMap"
      via: "conceptMap field matching sectionMap key"
      pattern: "overview"
    - from: "02-subagent-types.json"
      to: "module.json sectionMap"
      via: "conceptMap field matching sectionMap key"
      pattern: "subagent-types"
    - from: "03-wave-execution.json"
      to: "module.json sectionMap"
      via: "conceptMap field matching sectionMap key"
      pattern: "wave-execution"
---

<objective>
Create lessons 1-3 for Module 4 (Agent Orchestration) covering the orchestration model overview, subagent types, and wave execution.

Purpose: These first three lessons build the learner's foundation — from "what is orchestration and why" through the specific agent roles to understanding how work gets parallelized across waves. Each lesson uses real code from GSD's workflow files.
Output: Three lesson JSON files in learn/content/modules/agent-orchestration/lessons/
</objective>

<execution_context>
@C:/Users/18182/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/18182/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-module-infrastructure/18-01-SUMMARY.md

@learn/content/modules/agent-orchestration/module.json
@learn/content/modules/agent-orchestration/concept-map.txt

<interfaces>
<!-- Lesson JSON format (established by Modules 1-3): -->
<!--
{
  "id": "string",           // matches sectionMap key in module.json
  "title": "string",
  "lessonNumber": number,
  "objective": "string",
  "content": [
    { "type": "text", "value": "string", "focus": "string", "bridge": "string" },
    { "type": "code", "language": "markdown|text|yaml", "value": "string", "highlight": [line_numbers] }
  ],
  "conceptMap": "string",   // must match sectionMap key in module.json
  "successCriteria": "string"
}
-->

<!-- module.json sectionMap keys for lessons 1-3: "overview", "subagent-types", "wave-execution" -->

<!-- GSD source files to extract real code snippets from: -->
<!-- C:/Users/18182/.claude/get-shit-done/workflows/execute-phase.md — orchestrator behavior, wave execution, agent spawning -->
<!-- C:/Users/18182/.claude/get-shit-done/workflows/execute-plan.md — executor behavior, subagent routing -->
<!-- C:/Users/18182/.claude/get-shit-done/workflows/plan-phase.md — planner agent, research agent spawning -->
<!-- C:/Users/18182/.claude/get-shit-done/workflows/verify-phase.md — verifier agent behavior -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Lesson 1 - The Orchestration Model (Overview)</name>
  <files>learn/content/modules/agent-orchestration/lessons/01-overview.json</files>
  <action>
Create lesson JSON file with:
- id: "overview"
- title: "The Orchestration Model"
- lessonNumber: 1
- conceptMap: "overview"
- successCriteria: describing what the learner understands about orchestration after this lesson

Content blocks (target 7-9 blocks, mix of text and code):

**Block 1 (text):** Introduce the core problem: GSD breaks work into phases and plans, but who executes them? Not a single long-running agent — that would exhaust context. GSD uses an orchestration model where a coordinator agent spawns specialized subagents, each with fresh context. Focus: "Why orchestration instead of one big agent." Bridge to the core principle.

**Block 2 (code, language: text):** Show the core_principle from execute-phase.md. Read the actual file at C:/Users/18182/.claude/get-shit-done/workflows/execute-phase.md and extract the content of the `<core_principle>` tag verbatim. Highlight the key line about "Orchestrator coordinates, not executes."

**Block 3 (text):** Explain the three key insights from that principle: (1) orchestrator coordinates but never writes code itself, (2) each subagent loads full context fresh, (3) orchestrator stays lean at ~10-15% context usage. This means the orchestrator can manage many plans without degrading. Focus: "Lean orchestrator = consistent quality."

**Block 4 (code, language: text):** Show the execute-phase.md `<purpose>` tag content. Read from the actual file. This establishes the high-level flow: wave-based parallel execution with lean orchestrator delegation.

**Block 5 (text):** Walk through the execution flow at a high level: discover plans -> analyze dependencies -> group into waves -> spawn agents per wave -> handle checkpoints -> collect results. Each step is a discrete responsibility. The orchestrator never drifts into implementation. Focus: "Discrete responsibilities prevent scope creep."

**Block 6 (code, language: text):** Show the agent spawning snippet from execute-phase.md step "execute_waves" — the Task() call that spawns a gsd-executor subagent. Read from the actual file (around line 110-145). Include the subagent_type, model, prompt structure, files_to_read, and success_criteria. Highlight the subagent_type and files_to_read lines.

**Block 7 (text):** Explain what we just saw: the orchestrator passes paths, not content. Each executor reads files itself with fresh 200k context. This is the "path-only delegation" pattern — the orchestrator tells agents WHERE to look, not WHAT to do. The plan file IS the instruction. Bridge to next lesson: "Now you know the orchestration model. Next, we'll meet the specific agent types that play each role."

**IMPORTANT:** Code blocks must contain actual content from GSD's workflow files. Read each source file and extract real snippets. Escape all quotes and special characters properly for JSON string values. Newlines in code block values use \n.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const lesson = JSON.parse(fs.readFileSync('learn/content/modules/agent-orchestration/lessons/01-overview.json', 'utf8')); console.log('id:', lesson.id); console.log('lessonNumber:', lesson.lessonNumber); console.log('blocks:', lesson.content.length); console.log('conceptMap:', lesson.conceptMap); const codeBlocks = lesson.content.filter(b => b.type === 'code'); console.log('codeBlocks:', codeBlocks.length); if (lesson.lessonNumber !== 1) throw new Error('Wrong lesson number'); if (lesson.conceptMap !== 'overview') throw new Error('Wrong conceptMap'); if (lesson.content.length < 6) throw new Error('Too few blocks'); if (codeBlocks.length < 2) throw new Error('Too few code blocks');"</automated>
  </verify>
  <done>Lesson 1 JSON exists with lessonNumber 1, conceptMap "overview", 7+ content blocks with real GSD workflow snippets showing the orchestration model, core principle, and agent spawning pattern</done>
</task>

<task type="auto">
  <name>Task 2: Create Lesson 2 - Subagent Types</name>
  <files>learn/content/modules/agent-orchestration/lessons/02-subagent-types.json</files>
  <action>
Create lesson JSON file with:
- id: "subagent-types"
- title: "Subagent Types"
- lessonNumber: 2
- conceptMap: "subagent-types"
- successCriteria: describing what the learner knows about the five subagent roles

Content blocks (target 8-10 blocks, mix of text and code):

**Block 1 (text):** Bridge from Lesson 1 — the orchestrator spawns agents, but not all agents are the same. GSD has five specialized subagent types, each designed for a specific role. The type determines what context the agent loads and what it is responsible for producing. Focus: "Specialization prevents bloat." Bridge to seeing the types.

**Block 2 (code, language: text):** Show the executor subagent routing from execute-plan.md. Read C:/Users/18182/.claude/get-shit-done/workflows/execute-plan.md and extract the parse_segments step showing the routing table (Pattern A/B/C for autonomous/segmented/main execution). Highlight the pattern descriptions.

**Block 3 (text):** Explain the executor (gsd-executor): the workhorse. It reads a PLAN.md, executes tasks sequentially, commits each task atomically, creates SUMMARY.md. Three routing patterns: Pattern A (fully autonomous — single agent does everything), Pattern B (segmented — pauses at checkpoints), Pattern C (main context — for plans requiring decisions). Focus: "Executors build things."

**Block 4 (code, language: text):** Show the planner agent spawning from plan-phase.md. Read C:/Users/18182/.claude/get-shit-done/workflows/plan-phase.md and extract the section where it spawns the gsd-planner subagent (the Task() call with the planner prompt including phase context, ROADMAP, requirements). Highlight the subagent_type line.

**Block 5 (text):** Explain the planner (gsd-planner): reads phase goals from ROADMAP.md, decomposes into tasks with dependencies, produces PLAN.md files with frontmatter (wave, depends_on, files_modified). The planner is THIS — the very agent generating plans right now in the GSD system. Focus: "Planners decompose goals into executable tasks."

**Block 6 (code, language: text):** Show the researcher agent spawning from plan-phase.md. Read the file and find where gsd-phase-researcher is spawned (if research is needed before planning). Show the Task() call structure. If not a separate spawn, show the research-phase.md purpose tag instead by reading C:/Users/18182/.claude/get-shit-done/workflows/research-phase.md.

**Block 7 (text):** Explain the researcher (gsd-phase-researcher): investigates technical approaches before planning. Produces RESEARCH.md or DISCOVERY.md with library evaluations, architecture patterns, and recommendations. Only spawned when discovery level is 2+. Focus: "Researchers prevent blind implementation."

**Block 8 (text):** Explain the verifier and checker roles. The verifier (from verify-phase.md) runs after execution to check must_haves against reality — do the artifacts exist, do the key links work, are the truths observable? The checker (gsd-plan-checker) reviews PLAN.md quality before execution — task completeness, dependency correctness, scope sanity. Together they close the quality loop. Focus: "Verifiers and checkers close the loop."

**Block 9 (code, language: text):** Show a summary table of all five types. Create a clean ASCII table:
Type | Spawned By | Reads | Produces
gsd-executor | execute-phase | PLAN.md | SUMMARY.md + commits
gsd-planner | plan-phase | ROADMAP.md | PLAN.md files
gsd-phase-researcher | plan-phase | external docs | RESEARCH.md
gsd-verifier | verify-phase | SUMMARY.md | VERIFICATION.md
gsd-plan-checker | plan-phase | PLAN.md | checker report
Highlight the Type column lines.

**Block 10 (text):** Bridge to next lesson — now that you know WHO the agents are, the next lesson covers HOW they execute in parallel through the wave system.

**IMPORTANT:** Read each source file and extract real snippets. Escape properly for JSON.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const lesson = JSON.parse(fs.readFileSync('learn/content/modules/agent-orchestration/lessons/02-subagent-types.json', 'utf8')); console.log('id:', lesson.id); console.log('lessonNumber:', lesson.lessonNumber); console.log('blocks:', lesson.content.length); console.log('conceptMap:', lesson.conceptMap); const codeBlocks = lesson.content.filter(b => b.type === 'code'); console.log('codeBlocks:', codeBlocks.length); const hasExecutor = lesson.content.some(b => b.value && b.value.includes('gsd-executor')); console.log('hasExecutorRef:', hasExecutor); if (lesson.lessonNumber !== 2) throw new Error('Wrong lesson number'); if (lesson.conceptMap !== 'subagent-types') throw new Error('Wrong conceptMap'); if (lesson.content.length < 8) throw new Error('Too few blocks'); if (!hasExecutor) throw new Error('Missing executor reference');"</automated>
  </verify>
  <done>Lesson 2 JSON exists with lessonNumber 2, conceptMap "subagent-types", 8+ blocks covering all five subagent types (executor, planner, researcher, verifier, checker) with real workflow snippets</done>
</task>

<task type="auto">
  <name>Task 3: Create Lesson 3 - Wave Execution</name>
  <files>learn/content/modules/agent-orchestration/lessons/03-wave-execution.json</files>
  <action>
Create lesson JSON file with:
- id: "wave-execution"
- title: "Wave Execution"
- lessonNumber: 3
- conceptMap: "wave-execution"
- successCriteria: describing what the learner understands about wave-based parallel execution

Content blocks (target 8-10 blocks, mix of text and code):

**Block 1 (text):** Bridge from Lesson 2 — agents know their roles, but how does the orchestrator decide what runs when? Through waves. Plans declare dependencies via depends_on in their frontmatter. The orchestrator reads these, groups plans into waves, and runs each wave's plans in parallel. Focus: "Dependencies drive execution order." Bridge to seeing the mechanism.

**Block 2 (code, language: yaml):** Show a PLAN.md frontmatter example with wave and depends_on fields. Use a realistic example:
```
---
phase: 05-feature
plan: 01
wave: 1
depends_on: []
files_modified: [src/models/user.ts]
autonomous: true
---
```
Then show plan 02 with wave: 1 and depends_on: [], then plan 03 with wave: 2 and depends_on: [05-01, 05-02]. Highlight the wave and depends_on lines.

**Block 3 (text):** Explain wave assignment: plans with no dependencies start in wave 1. Plans depending on wave 1 plans go to wave 2. The formula is simple: wave = max(dependency waves) + 1. This is computed during planning, not at runtime. The wave number is baked into frontmatter. Focus: "Wave numbers are pre-computed from dependency analysis."

**Block 4 (code, language: text):** Show the discover_and_group_plans step from execute-phase.md. Read C:/Users/18182/.claude/get-shit-done/workflows/execute-phase.md and extract the phase-plan-index call and the reporting table format showing Wave | Plans | What it builds. Highlight the gsd-tools phase-plan-index line.

**Block 5 (text):** Explain what happens at runtime: the orchestrator calls phase-plan-index which reads all PLAN.md frontmatter, groups by wave number, and returns a structured index. Plans with has_summary: true are skipped (already completed). The orchestrator then iterates waves sequentially, but within each wave, plans run in parallel. Focus: "Sequential waves, parallel plans within each wave."

**Block 6 (code, language: text):** Show the execute_waves step from execute-phase.md — the section that describes spawning executor agents per wave and the completion reporting. Read the actual file and extract the "For each wave" numbered steps showing: describe what's being built, spawn executor agents, wait for completion, report. Highlight the "Spawn executor agents" and "Wait for all agents" lines.

**Block 7 (text):** Explain the spot-check verification after each wave. The orchestrator does not blindly trust agent reports. It checks: do the SUMMARY files exist? Are there git commits matching the plan ID? Is there a Self-Check: FAILED marker? This lightweight verification catches failures without full re-execution. Focus: "Trust but verify — spot-checks catch failures fast."

**Block 8 (code, language: text):** Show the spot-check code from execute-phase.md. Read the file and find the section about verifying first 2 files from key-files.created and checking git log. Include the failure handling option ("Retry plan?" or "Continue with remaining waves?"). Highlight the verification lines.

**Block 9 (text):** Tie it together: wave execution gives GSD its speed. Independent plans run simultaneously, each with fresh context. The orchestrator stays lean — it discovers, groups, spawns, and verifies, never touching code itself. Bridge to next lesson: "You've seen how plans execute in waves. Next, we'll examine the orchestrator's own design — why it deliberately stays thin."

**IMPORTANT:** Read execute-phase.md and extract real snippets. Escape properly for JSON.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const lesson = JSON.parse(fs.readFileSync('learn/content/modules/agent-orchestration/lessons/03-wave-execution.json', 'utf8')); console.log('id:', lesson.id); console.log('lessonNumber:', lesson.lessonNumber); console.log('blocks:', lesson.content.length); console.log('conceptMap:', lesson.conceptMap); const codeBlocks = lesson.content.filter(b => b.type === 'code'); console.log('codeBlocks:', codeBlocks.length); const hasWave = lesson.content.some(b => b.value && b.value.includes('wave')); console.log('hasWaveRef:', hasWave); if (lesson.lessonNumber !== 3) throw new Error('Wrong lesson number'); if (lesson.conceptMap !== 'wave-execution') throw new Error('Wrong conceptMap'); if (lesson.content.length < 7) throw new Error('Too few blocks'); if (codeBlocks.length < 3) throw new Error('Too few code blocks');"</automated>
  </verify>
  <done>Lesson 3 JSON exists with lessonNumber 3, conceptMap "wave-execution", 8+ blocks covering plan discovery, wave grouping, parallel spawning, and spot-check verification with real execute-phase.md snippets</done>
</task>

</tasks>

<verification>
- All three lesson JSON files are valid and parseable
- lessonNumbers are 1, 2, 3 respectively
- conceptMap values match module.json sectionMap keys: "overview", "subagent-types", "wave-execution"
- Code blocks contain real GSD workflow content (not invented examples)
- Lessons flow: orchestration model -> subagent roles -> wave execution
- Each lesson bridges to the next
</verification>

<success_criteria>
- Lesson 1 teaches the orchestration model with real execute-phase.md snippets
- Lesson 2 teaches all five subagent types with real workflow file snippets
- Lesson 3 teaches wave execution with real execute-phase.md snippets
- All three lessons load correctly in the learning shell
- Content blocks contain actual GSD source code, not invented examples
</success_criteria>

<output>
After completion, create `.planning/phases/19-lesson-content/19-01-SUMMARY.md`
</output>
