---
phase: 06-module-1-mini-project
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - learn/content/modules/gsd-commands/project/spec.json
  - learn/content/modules/gsd-commands/project/hints.json
  - learn/content/modules/gsd-commands/lessons/06-mini-project.json
  - learn/content/modules/gsd-commands/concept-map.txt
  - learn/content/modules/gsd-commands/module.json
autonomous: true
requirements: [MOD1-05, MOD1-07, MOD1-08]

must_haves:
  truths:
    - "Learner can navigate to Lesson 6 and see the /gsd:skeptic mini-project instructions"
    - "Learner can run --verify and get structural checks against their command.md + workflow.md files"
    - "Learner can run --hint and receive 5 progressively detailed hints"
    - "Concept map shows mini-project node when viewing Lesson 6"
  artifacts:
    - path: "learn/content/modules/gsd-commands/project/spec.json"
      provides: "Structural verification spec for skeptic command + workflow"
      contains: "gsd-commands-project"
    - path: "learn/content/modules/gsd-commands/project/hints.json"
      provides: "5 progressive hints for markdown mini-project"
      min_lines: 5
    - path: "learn/content/modules/gsd-commands/lessons/06-mini-project.json"
      provides: "Mini-project lesson with task, deliverables, verify/hint commands"
      contains: "mini-project"
    - path: "learn/content/modules/gsd-commands/concept-map.txt"
      provides: "Updated concept map with mini-project node"
      contains: "Mini-Project"
    - path: "learn/content/modules/gsd-commands/module.json"
      provides: "Updated sectionMap with mini-project key"
      contains: "mini-project"
  key_links:
    - from: "learn/content/modules/gsd-commands/lessons/06-mini-project.json"
      to: "learn/content/modules/gsd-commands/project/spec.json"
      via: "verifyCommand triggers verifier.cjs which loads spec.json from module project/ dir"
      pattern: "gsd-learn.cjs --verify"
    - from: "learn/content/modules/gsd-commands/lessons/06-mini-project.json"
      to: "learn/content/modules/gsd-commands/project/hints.json"
      via: "hintCommand triggers hints.cjs which loads hints.json from module project/ dir"
      pattern: "gsd-learn.cjs --hint"
    - from: "learn/content/modules/gsd-commands/project/spec.json"
      to: "~/.claude/commands/gsd/skeptic.md"
      via: "artifact path with tilde expansion in verifier.cjs"
      pattern: "~/.claude/commands/gsd/skeptic.md"
---

<objective>
Create the Module 1 capstone mini-project: learner builds /gsd:skeptic (a command spec + workflow pair that critiques a GSD phase). Includes structural verification spec, progressive hints, lesson content, and concept map update.

Purpose: This is the final lesson in Module 1, proving the learner can apply everything from Lessons 1-5 to build a real, usable GSD command from markdown files alone.
Output: 3 new content files (spec.json, hints.json, 06-mini-project.json) + 2 updated metadata files (concept-map.txt, module.json)
</objective>

<execution_context>
@C:/Users/18182/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/18182/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-module-1-mini-project/06-CONTEXT.md
@.planning/phases/06-module-1-mini-project/06-RESEARCH.md

<interfaces>
<!-- Reference patterns from command-lifecycle module (Module 2) -->

From learn/content/modules/command-lifecycle/project/spec.json:
```json
{
  "id": "command-lifecycle-project",
  "moduleId": "command-lifecycle",
  "title": "Build a GSD Echo Command",
  "description": "...",
  "artifacts": [
    {
      "description": "Echo handler module",
      "path": "get-shit-done/bin/lib/echo.cjs",
      "checks": [
        { "pattern": "module\\.exports", "description": "File exports a module" }
      ]
    }
  ]
}
```

From learn/content/modules/command-lifecycle/project/hints.json:
```json
[
  "Think about the journey a command takes...",
  "Lesson 4 (Tool Modules) showed you exactly how...",
  "Look at get-shit-done/bin/lib/ -- ...",
  "Your module needs to export a function that follows...",
  "Create lib/echo.cjs with a cmdEcho function..."
]
```

From learn/content/modules/command-lifecycle/lessons/06-mini-project.json:
```json
{
  "id": "mini-project",
  "title": "Mini-Project: Build a GSD Command",
  "lessonNumber": 6,
  "objective": "Apply everything from lessons 1-5 to add a new command to GSD",
  "content": [
    { "type": "text", "value": "...", "focus": "...", "bridge": "..." },
    { "type": "text", "value": "...", "focus": "...", "bridge": "..." },
    { "type": "project", "task": "...", "deliverables": ["..."],
      "verifyCommand": "node learn/bin/gsd-learn.cjs --verify",
      "hintCommand": "node learn/bin/gsd-learn.cjs --hint",
      "focus": "...", "bridge": "..." }
  ],
  "conceptMap": "mini-project",
  "successCriteria": "Running --verify shows all checks passing..."
}
```

From learn/content/modules/gsd-commands/module.json:
```json
{
  "id": "gsd-commands",
  "title": "GSD Commands & Workflows",
  "order": 1,
  "sectionMap": {
    "overview": "Overview",
    "command-spec": "Command Spec",
    "workflow": "Workflow",
    "dispatch": "Dispatch Chain",
    "bridge": "Bridge"
  }
}
```

CRITICAL regex detail from verifier.cjs line 60:
```javascript
pattern: new RegExp(c.pattern)  // No flags! No 's' flag!
// Therefore use [\s\S]*? instead of .* for multiline matching
```

Every content item (including type "project") MUST have both "focus" (string) and "bridge" (string) fields -- lessons.cjs validates this and throws on missing fields.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create spec.json and hints.json for /gsd:skeptic verification</name>
  <files>learn/content/modules/gsd-commands/project/spec.json, learn/content/modules/gsd-commands/project/hints.json</files>
  <action>
Create the `learn/content/modules/gsd-commands/project/` directory and add two files.

**spec.json** -- Follow the exact structure from command-lifecycle/project/spec.json. Two artifacts:

Artifact 1 -- Skeptic command spec at `~/.claude/commands/gsd/skeptic.md`:
Checks covering ALL structural elements taught in Lessons 2-4:
- YAML frontmatter block: `---[\s\S]*?---`
- name field: `name:\s*gsd:skeptic`
- description field: `description:`
- allowed-tools field: `allowed-tools:`
- objective section: `<objective>[\s\S]*?</objective>`
- execution_context section: `<execution_context>[\s\S]*?</execution_context>`
- Cross-file wiring (@file reference to workflow): `@.*workflows/skeptic\.md`
- context section: `<context>[\s\S]*?</context>`
- process section: `<process>[\s\S]*?</process>`

Artifact 2 -- Skeptic workflow at `~/.claude/get-shit-done/workflows/skeptic.md`:
- purpose section: `<purpose>[\s\S]*?</purpose>`
- process section: `<process>[\s\S]*?</process>`

Use `id: "gsd-commands-project"`, `moduleId: "gsd-commands"`, `title: "Build /gsd:skeptic"`, and a description like "Create a command spec + workflow pair that critiques a GSD phase".

IMPORTANT: All regex patterns use `[\s\S]*?` (NOT `.*`) for multiline matching because verifier.cjs uses `new RegExp(pattern)` without the `s` flag.

**hints.json** -- Array of exactly 5 strings, escalating from conceptual to step-by-step, focused on MARKDOWN structure (not Node.js). Follow this escalation pattern:
1. Conceptual reframe -- remind learner they already know the structure from Lessons 2-4 (two files, two layers)
2. Point to relevant lesson -- re-read Lessons 2 and 3 for the exact anatomy of each file type
3. Point to location -- the two files go at specific paths: `~/.claude/commands/gsd/skeptic.md` and `~/.claude/get-shit-done/workflows/skeptic.md`
4. Name the structural elements -- command spec needs frontmatter (name, description, allowed-tools), XML sections (objective, execution_context with @file reference, context, process); workflow needs purpose and process sections
5. Near-complete walkthrough -- create skeptic.md at the command path with YAML frontmatter containing `name: gsd:skeptic`, then add each XML section; create workflow skeptic.md with purpose and process sections; the execution_context must contain `@...workflows/skeptic.md` to wire them together
  </action>
  <verify>
    <automated>node -e "const s=require('./learn/content/modules/gsd-commands/project/spec.json'); const h=require('./learn/content/modules/gsd-commands/project/hints.json'); console.assert(s.artifacts.length===2,'need 2 artifacts'); console.assert(s.artifacts[0].checks.length>=8,'command spec needs 8+ checks'); console.assert(s.artifacts[1].checks.length>=2,'workflow needs 2+ checks'); console.assert(h.length===5,'need 5 hints'); console.assert(s.artifacts[0].path.startsWith('~/'),'must use tilde paths'); console.log('spec.json and hints.json valid')"</automated>
  </verify>
  <done>spec.json has 2 artifacts with full structural coverage (8+ checks on command spec, 2+ on workflow, including cross-file wiring check). hints.json has exactly 5 progressive hints focused on markdown structure.</done>
</task>

<task type="auto">
  <name>Task 2: Create mini-project lesson and update concept map</name>
  <files>learn/content/modules/gsd-commands/lessons/06-mini-project.json, learn/content/modules/gsd-commands/concept-map.txt, learn/content/modules/gsd-commands/module.json</files>
  <action>
**06-mini-project.json** -- Follow the exact schema from command-lifecycle/lessons/06-mini-project.json. Required fields:
- `id`: "mini-project"
- `title`: "Mini-Project: Build /gsd:skeptic"
- `lessonNumber`: 6
- `objective`: Something like "Apply everything from Lessons 1-5 to build a real GSD command that critiques phases"
- `conceptMap`: "mini-project"
- `successCriteria`: Describe what --verify passing looks like + encourage running /gsd:skeptic for real

Content array with 3+ items. EVERY content item MUST have both `focus` (string, 3-8 words) and `bridge` (string) fields -- lessons.cjs throws on missing fields.

Content item 1 (type: "text"): Introduction framing -- learner has traced the two-layer architecture, studied both file anatomies, and followed the dispatch chain. Now they build something real. Two files in their live GSD install that create a working /gsd:skeptic command. Mention this is not hypothetical -- the command will actually work in Claude Code.

Content item 2 (type: "text"): Verify/hint workflow -- explain the --verify and --hint commands. Note that unlike Module 2's echo command (Node.js files in the codebase), this project creates markdown files in their GSD install directory. If something goes wrong, they can delete the files and start over.

Content item 3 (type: "project"): The project spec itself:
- task: Build /gsd:skeptic -- a command that takes a phase number argument and critiques that phase's plan/progress. Two files: command spec at `~/.claude/commands/gsd/skeptic.md` and workflow at `~/.claude/get-shit-done/workflows/skeptic.md`. Base version requires a phase argument. The critique content is the learner's creative contribution -- verification only checks structure.
- deliverables: List the two files with their full tilde paths + mention the @file wiring between them
- verifyCommand: "node learn/bin/gsd-learn.cjs --verify" (no --module flag, default is correct)
- hintCommand: "node learn/bin/gsd-learn.cjs --hint"

Content item 4 (type: "text"): "Going Further" bonus section -- suggest auto-detecting the current phase instead of requiring an argument. Hint that STATE.md tracks the current phase. Mention the workflow could read it using the Read tool. No separate verification for this -- it is an optional enhancement. This is the last bridge in Module 1, so the bridge text should reference completing the module and what comes next in Module 2.

**concept-map.txt** -- Add a Mini-Project node after the Bridge node at the bottom of the existing chain. Keep the same ASCII box style. The chain should end with Mini-Project as the final node:
```
  +------------------+
  | Mini-Project     |
  | Build /gsd:skeptic|
  | (apply all)      |
  +------------------+
```

**module.json** -- Add `"mini-project": "Mini-Project"` to the sectionMap object. Keep all existing entries. Read the file first, then update.
  </action>
  <verify>
    <automated>node --test learn/tests/lessons.test.cjs 2>&1 | tail -20</automated>
  </verify>
  <done>Lesson 6 loads successfully via loadModule('gsd-commands') with all required fields validated. Concept map shows mini-project node. module.json sectionMap includes "mini-project" key. Full test suite passes including the new lesson.</done>
</task>

</tasks>

<verification>
Run the full test suite to confirm all content integrates correctly:
```bash
node --test learn/tests/lessons.test.cjs learn/tests/verifier.test.cjs learn/tests/hints.test.cjs
```

All existing tests must continue to pass. The lessons test validates that Lesson 6 loads with correct schema (id, title, lessonNumber, objective, content with focus+bridge on every item, conceptMap, successCriteria).
</verification>

<success_criteria>
1. `node learn/bin/gsd-learn.cjs --verify` loads spec.json from gsd-commands/project/ and returns check results (will show failures since skeptic files don't exist yet -- that is correct behavior)
2. `node learn/bin/gsd-learn.cjs --hint` returns the first hint from hints.json
3. Lesson 6 appears when navigating the gsd-commands module (lessonNumber: 6, "Lesson 6 of 6")
4. All tests pass: `node --test learn/tests/*.test.cjs`
</success_criteria>

<output>
After completion, create `.planning/phases/06-module-1-mini-project/06-01-SUMMARY.md`
</output>
