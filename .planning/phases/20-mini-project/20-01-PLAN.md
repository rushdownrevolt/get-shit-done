---
phase: 20-mini-project
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - learn/content/modules/agent-orchestration/project/spec.json
  - learn/content/modules/agent-orchestration/project/hints.json
  - learn/content/modules/agent-orchestration/lessons/08-mini-project.json
autonomous: true
requirements: [MINI-01, MINI-02, MINI-03]

must_haves:
  truths:
    - "Mini-project lesson appears as lesson 08 in Module 4 and renders in the lesson display engine"
    - "Verification checks test orchestration concepts (subagent delegation, wave structure) not just file existence"
    - "5 hints progress from conceptual nudge to near-solution without giving away the answer"
    - "Pressing H on mini-project step reveals hints; verify and hint commands work with --module=agent-orchestration"
  artifacts:
    - path: "learn/content/modules/agent-orchestration/project/spec.json"
      provides: "Artifact verification specs with regex checks for orchestration patterns"
      contains: "artifacts"
    - path: "learn/content/modules/agent-orchestration/project/hints.json"
      provides: "5 progressive hints array"
      min_lines: 5
    - path: "learn/content/modules/agent-orchestration/lessons/08-mini-project.json"
      provides: "Mini-project lesson with project-type content block"
      contains: "mini-project"
  key_links:
    - from: "learn/content/modules/agent-orchestration/lessons/08-mini-project.json"
      to: "learn/content/modules/agent-orchestration/project/spec.json"
      via: "verifyCommand referencing --module=agent-orchestration"
      pattern: "--verify.*--module=agent-orchestration"
    - from: "learn/content/modules/agent-orchestration/module.json"
      to: "learn/content/modules/agent-orchestration/lessons/08-mini-project.json"
      via: "sectionMap mini-project entry already exists"
      pattern: "mini-project"
---

<objective>
Create the Module 4 mini-project: spec, hints, and lesson file that exercise the learner's orchestration knowledge by extending the skeptic command with subagent orchestration.

Purpose: The mini-project is the capstone of Module 4. The learner applies orchestration concepts (subagent spawning, wave structure, orchestrator delegation) by adding a multi-agent review layer to their skeptic workflow. This continues the Module 1-3 pattern of progressively extending the same skeptic command.

Output: Three content files -- spec.json (verification), hints.json (progressive guidance), 08-mini-project.json (lesson) -- all in the agent-orchestration module directory.
</objective>

<execution_context>
@C:/Users/18182/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/18182/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

Existing mini-project patterns to follow exactly:
@learn/content/modules/planning-state/project/spec.json
@learn/content/modules/planning-state/project/hints.json
@learn/content/modules/planning-state/lessons/07-mini-project.json
@learn/content/modules/command-lifecycle/project/spec.json
@learn/content/modules/agent-orchestration/module.json

<interfaces>
<!-- spec.json format (from planning-state module): -->
{
  "id": "{moduleId}-project",
  "moduleId": "{moduleId}",
  "title": "...",
  "description": "...",
  "artifacts": [
    {
      "description": "...",
      "path": "path/to/file",
      "checks": [
        { "pattern": "regex", "description": "Human-readable check description" }
      ]
    }
  ]
}

<!-- hints.json format: JSON array of exactly 5 strings, progressing conceptual -> specific -->

<!-- Lesson JSON format (from planning-state 07-mini-project.json): -->
{
  "id": "{moduleId}-mini-project",
  "title": "Mini-Project: ...",
  "lessonNumber": N,
  "objective": "...",
  "content": [
    { "type": "text", "value": "...", "focus": "...", "bridge": "..." },
    { "type": "code", "language": "xml", "value": "...", "focus": "..." },
    { "type": "project", "task": "...", "deliverables": [...], "verifyCommand": "...", "hintCommand": "...", "focus": "...", "bridge": "..." },
    { "type": "text", "value": "...", "focus": "...", "bridge": "..." }
  ],
  "conceptMap": "mini-project",
  "successCriteria": "..."
}

<!-- module.json sectionMap already includes "mini-project": "Mini-Project" -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create spec.json and hints.json for orchestration mini-project</name>
  <files>learn/content/modules/agent-orchestration/project/spec.json, learn/content/modules/agent-orchestration/project/hints.json</files>
  <action>
Create the mini-project verification spec and progressive hints for Module 4.

**Mini-project concept:** The learner extends their skeptic workflow to use subagent orchestration. Instead of one monolithic review, skeptic defines parallel review "subagents" (as workflow steps organized in waves) that each examine a different aspect -- e.g., architecture review, convention compliance, dependency analysis. The workflow then aggregates findings from all subagents into the final SKEPTIC-REVIEW.md. This exercises orchestration knowledge: subagent delegation, wave-based parallel structure, and orchestrator aggregation.

**spec.json** (id: "agent-orchestration-project", moduleId: "agent-orchestration"):
- Title: "Build Orchestrated Skeptic Reviews"
- Description: "Extend your skeptic workflow with subagent-style parallel review -- multiple review aspects organized in waves, with an orchestrator step that aggregates findings"

Artifacts to verify (1 artifact -- the skeptic workflow):

Artifact 1: Skeptic workflow with orchestration layer
- path: "~/.claude/get-shit-done/workflows/skeptic.md"
- checks (these test orchestration CONCEPTS, not just file existence):
  1. pattern: `<purpose>[\s\S]*?</purpose>` -- "Has purpose section"
  2. pattern: `<process>[\s\S]*?</process>` -- "Has process section"
  3. pattern: `(wave|Wave|WAVE)\s*(1|2|one|two)|parallel.*review|concurrent.*review|simultaneous.*review` -- "Defines wave-based or parallel review structure"
  4. pattern: `(subagent|sub-agent|review.*agent|agent.*review|aspect|facet|dimension|lens)\s*(:|--|for|analyze|check|examine)|architecture.*review|convention.*review|dependency.*review|security.*review|quality.*review` -- "Defines multiple review subagents or aspects"
  5. pattern: `aggregat|combine|merge|synthesiz|consolidat|collect.*finding|gather.*finding|unif` -- "Has orchestrator step that aggregates subagent findings"
  6. pattern: `SKEPTIC-REVIEW|skeptic-review|review.*artifact|write.*finding|persist.*finding` -- "Writes aggregated findings to review artifact"

**hints.json** -- Array of exactly 5 strings, progressing from conceptual to specific:

1. (Conceptual - "what") "Your skeptic workflow currently does one big review. The orchestration model splits work across specialized agents that run in parallel. Think about what different ASPECTS of a project a skeptic could review independently -- architecture, conventions, dependencies -- each as its own 'subagent' step."

2. (Structural - "where") "The orchestration pattern has three layers: define the subagents (what each reviews), organize them into waves (which run in parallel), and aggregate the results (orchestrator collects findings). All three layers go inside your workflow's process section as new steps."

3. (Pattern - "how") "Add steps that each focus on one review aspect. For example: one step reviews architecture decisions, another checks convention compliance, another analyzes dependencies. Mark which steps can run in the same wave (parallel). Then add an aggregation step that combines all findings before writing to SKEPTIC-REVIEW.md."

4. (Specific - "show me") "In your workflow's process section, add 2-3 steps like: step 'review_architecture' examines project structure and design decisions, step 'review_conventions' checks coding standards and naming patterns, step 'review_dependencies' analyzes external dependencies. Group these as 'Wave 1' (they are independent and can run in parallel). Then add a step 'aggregate_findings' that combines results from all review steps into the final output."

5. (Near-solution - "almost there") "In your skeptic workflow at ~/.claude/get-shit-done/workflows/skeptic.md, add these steps inside the process section: (a) 2-3 review subagent steps, each named review_* and focused on one aspect (architecture, conventions, dependencies). Mark them as Wave 1 since they are independent. (b) An aggregate_findings step that collects findings from all review subagents and writes the combined analysis to SKEPTIC-REVIEW.md. The wave structure and aggregation pattern are what verification checks for -- the specific review aspects are your creative choice."
  </action>
  <verify>
    <automated>node -e "const s=require('./learn/content/modules/agent-orchestration/project/spec.json'); const h=require('./learn/content/modules/agent-orchestration/project/hints.json'); console.assert(s.moduleId==='agent-orchestration','wrong moduleId'); console.assert(s.artifacts.length>=1,'no artifacts'); console.assert(s.artifacts[0].checks.length>=5,'need 5+ checks'); console.assert(Array.isArray(h)&&h.length===5,'need 5 hints'); console.log('PASS: spec has',s.artifacts[0].checks.length,'checks, hints has',h.length,'entries')"</automated>
  </verify>
  <done>spec.json has moduleId "agent-orchestration", at least 1 artifact with 5+ regex checks testing orchestration concepts (waves, subagents, aggregation). hints.json has exactly 5 progressive strings from conceptual to near-solution.</done>
</task>

<task type="auto">
  <name>Task 2: Create 08-mini-project.json lesson file</name>
  <files>learn/content/modules/agent-orchestration/lessons/08-mini-project.json</files>
  <action>
Create the mini-project lesson as lesson 08 in Module 4. Follow the EXACT structure of learn/content/modules/planning-state/lessons/07-mini-project.json.

**Lesson metadata:**
- id: "agent-orchestration-mini-project"
- title: "Mini-Project: Orchestrated Skeptic Reviews"
- lessonNumber: 8
- conceptMap: "mini-project"

**Content blocks (7-8 blocks, matching Module 3 mini-project structure):**

Block 1 (type: "text"): Motivation -- Recap what the learner built in Modules 1-3 (skeptic command spec, handler, persistent reviews). Now the problem: skeptic does one monolithic review. Real GSD orchestrators split work across specialized subagents running in parallel waves. The learner will apply orchestration patterns to make skeptic delegate reviews to multiple specialized "subagent" steps.

Block 2 (type: "text"): Verification and hints -- Explain how to check progress with --verify and --hint commands. Mention git checkout safety net for the workflow file. Same pattern as Module 3.

Block 3 (type: "text"): Introduce the template -- Explain the learner will add orchestration structure to their existing skeptic workflow. The key additions: (a) multiple review subagent steps each focused on one aspect, (b) wave annotations marking which steps run in parallel, (c) an aggregation step that combines findings. Press C to copy the template.

Block 4 (type: "code", language: "xml"): The orchestration template to integrate. Provide 2-3 review subagent steps and an aggregation step as XML workflow steps. Example:

```xml
<!-- Wave 1: Parallel review subagents (independent, can run simultaneously) -->

<step name="review_architecture">
**Review project architecture and design decisions.**

Examine the project structure, file organization, and architectural patterns:
- Are components properly separated?
- Do dependencies flow in the right direction?
- Are there circular dependencies or god objects?

Record findings for aggregation.
</step>

<step name="review_conventions">
**Review coding conventions and standards compliance.**

Check naming patterns, file structure conventions, and coding standards:
- Do files follow the project's naming conventions?
- Are exports and imports consistent?
- Is error handling following established patterns?

Record findings for aggregation.
</step>

<!-- Wave 2: Aggregation (depends on Wave 1 completing) -->

<step name="aggregate_findings">
**Aggregate findings from all review subagents.**

Collect and synthesize findings from review_architecture and review_conventions:
- Combine findings into a unified analysis
- Identify cross-cutting concerns found by multiple reviewers
- Prioritize issues by severity
- Write the aggregated analysis to SKEPTIC-REVIEW.md with a dated section
</step>
```

Block 5 (type: "text"): Customization encouragement -- Tell learner to make it their own. They can add more review subagents (security, dependencies, testing), change the wave structure, customize the aggregation format. The structural requirements verification checks: (1) multiple review aspects defined, (2) wave-based parallel structure, (3) aggregation step combining findings, (4) writes to SKEPTIC-REVIEW.md.

Block 6 (type: "project"): The project task block.
- task: "Extend your skeptic workflow with subagent-style orchestration -- multiple specialized review steps organized in waves, with an aggregator that combines findings."
- deliverables: ["Updated workflow at ~/.claude/get-shit-done/workflows/skeptic.md with review subagent steps organized in waves and an aggregation step", "Running /gsd:skeptic produces aggregated multi-aspect findings in SKEPTIC-REVIEW.md"]
- verifyCommand: "node learn/bin/gsd-learn.cjs --verify --module=agent-orchestration"
- hintCommand: "node learn/bin/gsd-learn.cjs --hint --module=agent-orchestration"

Block 7 (type: "text"): Module completion -- Celebrate completing Module 4. Recap the journey: from understanding the orchestration model, through subagent types, wave execution, lean orchestrators, checkpoints, auto-advance, to full lifecycle synthesis. Now they have applied it themselves. They understand how GSD breaks complex work into specialized agents, runs them in parallel waves, and aggregates results. This is the pattern behind every /gsd: command they will ever build.

**successCriteria:** "Running --verify --module=agent-orchestration shows all checks passing for the skeptic workflow (purpose, process, wave structure, review subagents, aggregation, artifact persistence). Your skeptic command now orchestrates multiple review perspectives in parallel, just like GSD orchestrates executors."
  </action>
  <verify>
    <automated>node -e "const l=require('./learn/content/modules/agent-orchestration/lessons/08-mini-project.json'); console.assert(l.lessonNumber===8,'wrong lessonNumber'); console.assert(l.conceptMap==='mini-project','wrong conceptMap'); console.assert(l.content.some(b=>b.type==='project'),'missing project block'); console.assert(l.content.some(b=>b.type==='code'),'missing code block'); console.assert(l.content.length>=6,'need 6+ blocks'); console.log('PASS: lesson has',l.content.length,'blocks, includes project and code blocks')"</automated>
  </verify>
  <done>08-mini-project.json loads as valid JSON, has lessonNumber 8, conceptMap "mini-project", contains a project-type block with verifyCommand and hintCommand referencing --module=agent-orchestration, provides a code template with orchestration patterns, and has 7+ content blocks with text/code/project mix.</done>
</task>

</tasks>

<verification>
All three content files exist and are valid JSON:
- `node -e "require('./learn/content/modules/agent-orchestration/project/spec.json')"`
- `node -e "require('./learn/content/modules/agent-orchestration/project/hints.json')"`
- `node -e "require('./learn/content/modules/agent-orchestration/lessons/08-mini-project.json')"`

Module sectionMap already includes "mini-project" entry (confirmed in module.json).

Spec checks test orchestration concepts (waves, subagents, aggregation) not just file existence.
</verification>

<success_criteria>
1. spec.json exists with moduleId "agent-orchestration" and regex checks exercising orchestration knowledge
2. hints.json has exactly 5 progressive hints from conceptual to near-solution
3. 08-mini-project.json renders as lesson 8 with project block, code template, and verify/hint commands
4. All files follow the exact format conventions established by Modules 1-3 mini-projects
</success_criteria>

<output>
After completion, create `.planning/phases/20-mini-project/20-01-SUMMARY.md`
</output>
