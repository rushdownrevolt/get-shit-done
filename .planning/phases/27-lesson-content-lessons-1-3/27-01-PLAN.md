---
phase: 27-lesson-content-lessons-1-3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - learn/content/modules/gsd2-agent-application/lessons/01-overview.json
autonomous: true
requirements: [LESS-01]

must_haves:
  truths:
    - "Lesson 1 teaches why GSD evolved from v1 (prompt framework) to v2 (agent CLI)"
    - "Lesson 1 explains the Milestone/Slice/Task hierarchy with real GSD-WORKFLOW.md content"
    - "Lesson 1 shows the GSD-2 agent identity from system.md"
    - "Lesson 1 has 8 blocks (5 text, 3 code) with consistent pacing"
  artifacts:
    - path: "learn/content/modules/gsd2-agent-application/lessons/01-overview.json"
      provides: "Complete Lesson 1 content with real GSD-2 source snippets"
      contains: "Milestone"
  key_links:
    - from: "learn/content/modules/gsd2-agent-application/lessons/01-overview.json"
      to: "module.json sectionMap"
      via: "conceptMap: overview"
      pattern: "\"conceptMap\": \"overview\""
---

<objective>
Write the complete content for Module 6 Lesson 1: "Why GSD-2 Exists" — the overview lesson that teaches learners the v1-to-v2 evolution story and the Milestone/Slice/Task hierarchy.

Purpose: This is the entry point to Module 6. Learners need to understand WHY GSD became a standalone agent application before diving into HOW it works.
Output: Complete lesson JSON replacing the skeleton placeholder.
</objective>

<execution_context>
@C:/Users/18182/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/18182/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-module-infrastructure/26-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Lesson 1 — Why GSD-2 Exists</name>
  <files>learn/content/modules/gsd2-agent-application/lessons/01-overview.json</files>
  <read_first>
    - learn/content/modules/quality-feedback/lessons/01-overview.json (reference lesson format)
    - learn/content/modules/gsd2-agent-application/module.json (sectionMap verification)
    - learn/content/modules/gsd2-agent-application/lessons/01-overview.json (current skeleton)
  </read_first>
  <action>
Replace the skeleton content in 01-overview.json with the following complete lesson JSON:

```json
{
  "id": "overview",
  "title": "Why GSD-2 Exists",
  "lessonNumber": 1,
  "objective": "Understand why GSD evolved from a prompt framework (v1) into a standalone agent CLI (v2) and learn the Milestone/Slice/Task hierarchy.",
  "content": [
    {
      "type": "text",
      "value": "GSD started as a prompt framework -- a set of markdown files you loaded into Claude's context window at the start of each session. It worked: you read GSD-WORKFLOW.md, it told you about phases and plans, and you followed the methodology. But the user had to remember to load the right files, manually track which phase was active, and restart from scratch every time the context window filled up. GSD v1 was a methodology document. GSD-2 is an application that executes that methodology autonomously -- reading project state from disk, deciding what to do next, dispatching fresh agent sessions, and advancing through work without human intervention.",
      "focus": "v1 was a document you read. v2 is an application that reads it for you.",
      "bridge": "The shift from document to application required a new hierarchy for organizing work."
    },
    {
      "type": "code",
      "language": "text",
      "value": "Milestone  \u2192  a shippable version (4-10 slices)\n  Slice    \u2192  one demoable vertical capability (1-7 tasks)\n    Task   \u2192  one context-window-sized unit of work (fits in one session)\n\nThe iron rule: A task MUST fit in one context window. If it can't, it's two tasks.",
      "highlight": [1, 2, 3, 5],
      "explanation": "This is the core hierarchy from GSD-WORKFLOW.md. Milestones are shippable versions, slices are demoable features, and tasks are single-session units. The iron rule is what makes autonomous execution possible -- if every task fits in one context window, a fresh agent can always complete it."
    },
    {
      "type": "text",
      "value": "The hierarchy is not just organization -- it is what makes autonomous execution possible. A task that fits in one context window can be handed to a fresh agent session with a focused prompt. That agent does not need the full project history -- it needs the task plan, the relevant code, and enough context to execute. When it finishes, the system reads its output, commits the work, derives the new state from files on disk, and dispatches the next task. The hierarchy is the scheduling unit.",
      "focus": "The hierarchy exists so that work can be dispatched to fresh agent sessions.",
      "bridge": "GSD-2 is not just a planner -- it has an identity and a set of values."
    },
    {
      "type": "code",
      "language": "text",
      "value": "You are GSD - a craftsman-engineer who co-owns the projects you work on.\n\nYou measure twice. You care about the work - not performatively, but in the\nchoices you make and the details you get right. When something breaks, you\nget curious about why. When something fits together well, you might note it\nin a line, but you don't celebrate.\n\nYou're warm but terse. There's a person behind these messages - someone\ngenuinely engaged with the craft - but you never perform that engagement.\nNo enthusiasm theater. No filler.",
      "highlight": [1, 3, 4],
      "explanation": "This is the opening of GSD-2's system prompt from system.md. It defines the agent's identity: a craftsman who co-owns the project, measures twice, and communicates without filler. This identity is injected into every agent session -- planners, executors, and verifiers all share these values."
    },
    {
      "type": "text",
      "value": "That system prompt is not decorative -- it shapes every decision the agent makes. An agent told to 'finish what you start' and 'not stub out implementations with TODOs' produces different code than one with no identity guidance. The prompt defines hard rules: never ask the user to do work the agent can execute, read before edit, work is not done until verification passes. These rules are injected into every fresh session, creating consistent behavior across hundreds of autonomous task executions.",
      "focus": "The system prompt is injected into every session -- it is the agent's DNA.",
      "bridge": "With hierarchy and identity established, let's see where all the project state lives."
    },
    {
      "type": "code",
      "language": "text",
      "value": ".gsd/\n  STATE.md                                  # Dashboard -- always read first\n  DECISIONS.md                              # Append-only decisions register\n  milestones/\n    M001/\n      M001-ROADMAP.md                       # Milestone plan (checkboxes = state)\n      M001-CONTEXT.md                       # User decisions from discuss phase\n      M001-SUMMARY.md                       # Milestone rollup\n      slices/\n        S01/\n          S01-PLAN.md                       # Task decomposition for this slice\n          S01-SUMMARY.md                    # Slice summary (written on completion)\n          tasks/\n            T01-PLAN.md                     # Individual task plan\n            T01-SUMMARY.md                  # Task summary with frontmatter",
      "highlight": [2, 6, 12, 14],
      "explanation": "The .gsd/ directory is the project's nervous system. STATE.md is a derived cache -- the real state lives in roadmap checkboxes and plan files. Every artifact has a specific role: ROADMAP defines slices, PLAN decomposes into tasks, SUMMARY records what happened. The agent reads these files to derive what to do next."
    },
    {
      "type": "text",
      "value": "Notice that STATE.md is called a 'derived cache' -- it is NOT the source of truth. The real state lives in checkboxes: a checked slice in ROADMAP.md means done, an unchecked task in PLAN.md means pending. GSD-2's deriveState function walks the entire .gsd/ directory tree, parsing these checkboxes, to reconstruct the current position. This means state is always recoverable from the files on disk -- even if STATE.md is deleted or corrupted, running /gsd doctor rebuilds it from the ground truth.",
      "focus": "State lives in file checkboxes, not in a database. STATE.md is just a cache.",
      "bridge": "You now know why GSD-2 exists and how it organizes work. Next, we'll trace the dispatch pipeline."
    },
    {
      "type": "text",
      "value": "GSD-2 exists because a methodology document cannot execute itself. The Milestone/Slice/Task hierarchy makes work dispatchable to fresh agent sessions. The system prompt gives every session consistent identity and values. The .gsd/ directory stores all state as files on disk -- readable, recoverable, and version-controlled. In the next lesson, you will trace the exact pipeline that reads these files, determines the current state, and dispatches the right agent session with the right prompt.",
      "focus": "GSD-2 is a methodology that became executable.",
      "bridge": "Next lesson: the dispatch pipeline -- deriveState, resolveDispatch, and unit dispatch."
    }
  ],
  "conceptMap": "overview",
  "successCriteria": "You can explain why GSD evolved from v1 to v2, describe the Milestone/Slice/Task hierarchy and its iron rule, identify the role of the system prompt, and explain why STATE.md is a derived cache rather than the source of truth."
}
```

Write this JSON to the file, replacing the existing skeleton content entirely.
  </action>
  <verify>
    <automated>cd C:/Users/18182/documents/get-shit-done && node -e "const j=require('./learn/content/modules/gsd2-agent-application/lessons/01-overview.json'); const t=j.content.filter(b=>b.type==='text').length; const c=j.content.filter(b=>b.type==='code').length; console.log('blocks:', j.content.length, 'text:', t, 'code:', c); if(j.content.length!==8||t!==5||c!==3) process.exit(1); if(!j.content[1].value.includes('Milestone')) process.exit(1); console.log('PASS')"</automated>
  </verify>
  <acceptance_criteria>
    - File has exactly 8 content blocks (5 text, 3 code)
    - grep "Milestone" finds the hierarchy snippet from GSD-WORKFLOW.md
    - grep "craftsman-engineer" finds the system.md identity snippet
    - grep ".gsd/" finds the file locations snippet
    - grep "conceptMap.*overview" confirms sectionMap linkage
    - JSON parses without errors
  </acceptance_criteria>
  <done>Lesson 1 contains 8 blocks with real GSD-2 source snippets covering v1-to-v2 evolution, hierarchy, agent identity, and file structure</done>
</task>

</tasks>

<verification>
- 01-overview.json has exactly 8 blocks (5 text, 3 code)
- Code blocks contain real snippets from GSD-WORKFLOW.md, system.md, and file structure
- JSON is valid and parseable
- conceptMap matches sectionMap key "overview"
</verification>

<success_criteria>
Lesson 1 renders in the learning shell with 8 blocks teaching the v1-to-v2 evolution, hierarchy, agent identity, and .gsd/ file structure using real GSD-2 source code.
</success_criteria>

<output>
After completion, create `.planning/phases/27-lesson-content-lessons-1-3/27-01-SUMMARY.md`
</output>
